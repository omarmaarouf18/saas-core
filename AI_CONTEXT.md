# AI Context: Quick Delivery

> [!IMPORTANT]
> **Branch Workflow**: `main` = stable/deployable, kept in sync via reviewed merges from `logic-exploitation`. All active development happens on `logic-exploitation`. Emergency hotfixes for live production CD/deploy pipeline failures may be committed directly to `main` when needed immediately to unblock deployment, but MUST be synced back to `logic-exploitation` via fast-forward merge as the very next action afterward so the branches do not diverge.
>
> **Never use `git push --force` or `git push --force-with-lease` on `logic-exploitation` or `main` under any circumstances without explicit, per-instance human approval in the task itself.** If a push is rejected because the remote branch has diverged (e.g. another session/agent pushed in the meantime), STOP and report the divergence — do not force-resolve by overwriting remote history. Pull, inspect what changed, and merge/rebase cleanly, or ask for guidance if the correct resolution isn't obvious. This applies even to fixing a self-made mistake (e.g. wanting to amend a commit that was already pushed) — prefer a new corrective commit over rewriting pushed history.

This file serves as a persistent, model-agnostic, single source of truth for the technical stack, architecture, feature status, security logs, and current gaps of the Quick Delivery platform. Any agent or developer modifying this repository must update this document in the same commit. (Note: The repository and codebase are internally named saas-core for historical reasons.)

---

## Project Summary

The Quick Delivery platform is a multi-tenant SaaS application providing a marketplace services directory, real-time messaging, and job tracking. 

* **Tech Stack Deviation Note**: The codebase is implemented as **Go microservices** running on a containerized environment (Docker Compose) and backing onto MongoDB. This deviates from an original planning diagram that specified a Node.js and Socket.io stack. The microservices architecture and real-time messaging functionality were preserved, but migrated to Go for type safety and performance.

---

## Documentation System

To prevent documentation drift, the repository enforces structural and automated checks that must pass CI before any PR or merge:

1. **Auto-Generated Content**: 
   * **Endpoint Mapping**: The HTTP endpoint table in [docs/APPLICATION_MAP.md](docs/APPLICATION_MAP.md) is auto-generated from `RegisterRoutes` AST declarations across Go services. Edits inside the `<!-- GENERATED:ENDPOINTS:START -->` and `<!-- GENERATED:ENDPOINTS:END -->` markers will be overwritten and must never be edited by hand.
   * **Regenerating**: Run `make docs` to regenerate the endpoint table and refresh the Git commit short SHA note.
2. **Structurally-Checked Content**:
   * **File Indexing**: Every `.dart` file under `frontend/lib/` (screens, providers, models) must be mentioned in [docs/frontend/STATUS.md](docs/frontend/STATUS.md).
   * **Dependency Version Constraints**: Any package dependency constraints mentioned in [DESIGN.md](DESIGN.md) (e.g. `flutter_client_sse`) must match `frontend/pubspec.yaml` exactly.
3. **Manual Content (Enforced via Commit policy)**:
   * **Changelogs, ADRs, & Runbooks**: Additions to `docs/changelog/` (with valid 40-character hex commit SHAs), ADRs under `docs/adr/`, and README guidelines remain manual but must be updated in the same commit.
4. **Verifying Locally**:
   * Run `make docs-check` to execute the complete freshness and drift-catching verification suite locally.
   * Run `make setup` once after cloning to activate the `.githooks/pre-push` hook, which automatically blocks pushes that fail `gofmt`, changelog SHA validation, or `go build/vet/test` for any module. Run `make ci` to execute the same gate manually.

## Tooling & CI Security Scanner

The CI/CD pipeline and local pre-push hooks run a full-scope, unfiltered security scan using **gosec** (`gosec ./...`).

> [!IMPORTANT]
> **Gosec Taint Analysis Rules**:
> The CI workflow installs gosec via `go install github.com/securego/gosec/v2/cmd/gosec@latest`, which fetches a development build ("Gosec: dev" built from the latest master branch). This build includes newer taint-analysis rules beyond the last tagged release that may not be documented in the public `RULES.md`.
> 
> Future contributors must be aware of these rules and resolve them appropriately:
> * **`G704` (CWE-918)**: SSRF via taint analysis (triggered when unsanitized variables propagate to outgoing HTTP requests).
> * **`G705` (CWE-79)**: XSS via taint analysis (triggered when unescaped parameters propagate to HTTP responses).
> * **`G706` (CWE-117)**: Log injection via taint analysis (triggered when potentially untrusted parameters write directly to logs).
> 
> **Resolving Findings**:
> * **Genuine Fixes**: Apply domain prefix validation for SSRF, sanitize inputs inline by stripping carriage returns and newlines (`\r`/`\n`) for log injection, and use structured sanitizers for XSS.
> * **False Positives**: Justify explicitly using a `//nolint:gosec -- <reason>` comment (with both `#nosec` and `//nolint:gosec` headers) on the exact warning line. Do not narrow the scan scope or invent dummy rule IDs to bypass checks.
> 
> *Refer to [docs/changelog/security-fixes.md](docs/changelog/security-fixes.md) for specific G704, G705, and G706 fixes applied in the codebase.*

## CI/CD & Pre-Push Parity

To ensure that code which passes local checks is guaranteed to pass CI (and vice versa), the local pre-push hook ([.githooks/pre-push](file:///mnt/windows_data/CS tools/Antigravity/SaaS prototype/.githooks/pre-push)) and the GitHub Actions workflow ([ci.yml](file:///mnt/windows_data/CS tools/Antigravity/SaaS prototype/.github/workflows/ci.yml)) are synchronized to execute the same checks:

1. **Lint & Formatting**:
   - Go Formatting (`gofmt -l .`)
   - Dart Formatting (`dart format --set-exit-if-changed lib/`)
   - Changelog Commit SHA check (ensures SHAs referenced in `docs/changelog/` exist in Git)
2. **Build, Vet, & Test** (for all microservices and `shared/infra`):
   - `go build ./...`
   - `go vet ./...`
   - `go test ./... -count=1`
3. **Security & Vulnerability Scans**:
   - `govulncheck` (checks Go dependencies for known vulnerabilities; local execution filters for uncalled packages and stdlib vulnerabilities due to Go version mismatch, printing a loud warning in the terminal for each skipped standard library vulnerability)
   - `gosec` (runs full default Go security scans)
4. **Frontend Analysis**:
   - `flutter analyze` (strict checks)
   - `flutter test`

> [!IMPORTANT]
> **Strict Parity Principle**:
> If a check is ever added to `ci.yml`, it must be added to `.githooks/pre-push` in the same commit, and vice versa. Divergence between these two was a root cause of confusing CI failures earlier in this project's history.

> [!NOTE]
> **Pre-Push Hook Activation & Fresh Clone Enforcement**:
> Local git hooks (`.githooks/pre-push`) are NOT automatically activated on fresh clones because `core.hooksPath` is a local git configuration. To prevent commits or pushes that bypass local validation on fresh clones or new agent sessions, all primary `Makefile` targets (`setup`, `docs`, `docs-check`, `ci`, `commit`, `push`) execute an automated `ensure-hooks` prerequisite step that checks `git config --get core.hooksPath` and automatically runs `git config core.hooksPath .githooks` before proceeding. Furthermore, the `CLAUDE.md` auto-commit policy requires `git config core.hooksPath .githooks` as the literal first command before `git add` in every commit sequence.

---

## Architecture Table

The platform uses a microservices architecture coordinated via a reverse-proxy API Gateway.

| Service | Default Port | Role | Data Store / Key Libraries |
| :--- | :--- | :--- | :--- |
| **api-gateway** | `8080` | Reverse proxy/routing edge for clients. Implements edge rate limiting and path rewriting. | Go Standard Library |
| **auth-service** | `3002` | Handles tenant registration, password hashing (bcrypt), multi-role login, and encrypted 2FA OTPs. | MongoDB, `golang.org/x/crypto` |
| **chat-service** | `3001` | Real-time WebSocket hub for messaging, segregated per job. | MongoDB, `github.com/gorilla/websocket` |
| **notification-service** | `3004` | Real-time client alerts via Server-Sent Events (SSE). | Go Standard Library |
| **user-service** | `3003` | Services directory (using spatial indexes), job tracking, e-wallets, ledger, and subscriptions. | MongoDB (with `2dsphere` index) |
| **shared/infra** | `N/A` | Compile-time shared library module for cross-cutting infrastructure packages. | `github.com/redis/go-redis/v9`, `github.com/sony/gobreaker/v2`, `github.com/golang-jwt/jwt/v5` |

* **Shared Infrastructure Module (`shared/infra`)**: Created to eliminate duplicate copy-pasted codebase files (saving ~2,150 duplicate lines of code) without compromising microservice independence. It acts purely as a compile-time dependency, similar to third-party modules (no shared runtime processes, databases, or in-memory state). It contains:
  * `shared/infra/resilience`: HTTP client wrapper for retries, exponential backoff, jitter, and circuit breaking.
  * `shared/infra/ratelimit`: Redis-backed sliding window and login-lockout rate limiting.
  * `shared/infra/tlsutil`: TLS config loaders for mutual TLS (mTLS) server and client setup.
  * `shared/infra/jwtutil`: Cryptographically signed JSON Web Token (JWT) generation and validation helpers.

* **Security Decisions & Architecture Records**: Refer to [docs/adr/README.md](docs/adr/README.md) as the source of truth for security-boundary decisions going forward. Numbered ADR files detail exact context, implementation decisions, and consequences. Future contributors must document significant design and security changes as ADRs.
* **Realtime Hub Horizontal Scaling Architecture**: Real-time hubs (`chat-service` and `notification-service`) use Redis Pub/Sub for cross-replica event fan-out (see [ADR-0005](docs/adr/0005-realtime-hub-horizontal-scaling.md)). Phase 1 migration for `notification-service` (`2794adc70d938df54f97fad003f210eadc9d115f`) and Phase 2 migration for `chat-service` (`203111290514c71a25de3ada16bd43a46fac8f68`) are fully implemented, verified, and committed. Both services dynamically support Redis Pub/Sub cross-replica fan-out and degrade gracefully to local-only delivery if Redis is unreachable.
* **Negotiable Transport Pricing Architecture**: Transport/Rides category pricing model documented in [ADR-0006](docs/adr/0006-negotiable-transport-pricing.md) (`47edf8498d1d294dc00a04845f50fb17ae423fe1`). Data model foundation committed in `beac4e3030b1909cbc08ee6cdefe32923198b185`. Handler wiring, endpoints (`propose-price`, `respond-price`), lazy proposal expiry, and test suite committed in `036901ba292a73d2bf67cd14603f643d5d25e74c`. Defines single-shot take-it-or-leave-it fare proposals $[0.5 \times P_{\text{system}}, 1.5 \times P_{\text{system}}]$, single 5-minute proposal timeout, pre-selected employee assignment, owner-governed vehicle/amenity multipliers, and deferred wallet escrow locking upon reaching `AgreedPrice`.
* **Delivery & Shipping GPS Reconciliation Architecture**: Documented in [ADR-0007](docs/adr/0007-delivery-shipping-gps-reconciliation.md) (`e0c3ab014b45c2eafffd70b33b2bffd7fb2fc270`). Defines a two-phase architecture for Delivery and Shipping categories only: Phase 0 hardens `UpdateJobLocation` to evaluate cumulative speed from job start (closing the slow-drip coordinate drift accumulation gap while inheriting single-instance in-memory throttle state), and Phase 1 introduces `CompleteJob` settlement reconciliation using cumulative Haversine waypoint distance with a guaranteed payout floor ($\max(LockedEscrowAmount, ActualAmount)$) and manual review flagging via `models.JobStatusEscrowReconciliationRequired`.
* **Live Employee Map Tracking Architecture**: Documented in [ADR-0008](docs/adr/0008-live-employee-map-tracking.md) (`c14e7e8e82b69715ffae7b5fca4d941d3410ae43`). Defines zero-cost frontend rendering using `flutter_map` with OpenStreetMap / Carto raster tiles (avoiding GCP billing registration risks), live location streaming by reusing `chat-service` WebSocket channels (ADR-0005) with native `location_update` payload fields, hybrid initial state hydration via HTTP GET endpoints, identity-scoped tenant fleet views (owner sees active fleet employees) vs single-job customer views, and explicit non-goals (geocoding, routing/OSRM, place autocomplete deferred to future ADRs).
* **Zero-Commission Subscription-Only Revenue Model**: Documented in [ADR-0017](docs/adr/0017-zero-commission-subscription-only-revenue-model.md). Establishes 0% platform transaction commission across all payment methods (SaaS subscription fee as sole platform revenue source), pure log-entry COD collection without wallet balance mutation, centralized electronic payment credits with tenant owner withdrawal/payout request capability and Flutter UI (`OwnerProvider`, `WalletScreen`, `StatusBadge`, `PayoutRequest`) complete and verified (`c16838aab11a404786efd3aab5153dbe5872e0c0`), and remediation removing legacy fee deduction paths.

---

## Feature Status

Features are classified into three groups: Done & Verified, Explicitly Deferred by Decision, and Not Started Yet. Only features verified directly against the repository are marked as verified.

### 1. Done & Verified

The detailed project history is distributed across categorized changelog files. Please consult the specific category files for complete details (including file/line references, commit SHAs, and verification details):

*   [Security Fixes](docs/changelog/security-fixes.md) — 104 entries detailing vulnerabilities found and fixed (including Owner-Authenticated Employee Provisioning, see [ADR-0001](docs/adr/0001-owner-authenticated-employee-provisioning.md), Employee Assignment Tenant Binding, see [ADR-0003](docs/adr/0003-employee-assignment-tenant-binding-check.md), Customer Booking Employee Pre-Assignment Gating, see [ADR-0004](docs/adr/0004-customer-booking-employee-assignment-order.md), and Document Encryption at Rest).
*   [New Features](docs/changelog/new-features.md) — 56 net-new capabilities (e.g. complaint ticketing, KYB uploads, location tracking, Redis rate limiters, username propagation, version gating, zero-commission model, owner payout requests).
*   [Infrastructure & Tooling](docs/changelog/infrastructure.md) — 49 tooling, CI/CD hardening, module refactoring, and onboarding CLI tools (including strict CD preflight validation per ADR-0015).
*   [Bug Fixes](docs/changelog/bug-fixes.md) — 55 corrections to non-security behavior (e.g. deactivation grace, CORS ordering, random notification IDs, token refresh panic, frontend consistency audit batches 1–5, SegmentedButton contrast).
*   [Documentation](docs/changelog/documentation.md) — 34 documentation-only updates (e.g. Business Logic Audit Report, Application Map auto-sync, Frontend Consistency Audit, Four-Repo Deployment Map).
*   [Localization Infrastructure](docs/changelog/localization-infrastructure.md) — 4 entries covering Egyptian Colloquial Arabic (ar_EG) i18n and RTL layout architecture.
*   [Refactoring](docs/changelog/refactoring.md) — 2 entries documenting handler modularization and architecture alignments.

### 2. Explicitly Deferred by Decision

* **CloudWatch Event Rate Limiting/Batching**
  * **Decision**: Log shipping performs a separate CloudWatch Logs `PutLogEvents` API call per event rather than using client-side batching or queuing.
  * **Reasoning**: Accepted as a known limitation in the initial rollout. A genuine high-volume abuse burst could hit CloudWatch's per-stream PutLogEvents throttling limit and silently drop events at exactly the moment they matter most. Batching/queuing events client-side is flagged for future implementation.
* **E-Wallet and Bank Card Payment Flows**
  * **Decision**: Only Cash on Delivery (`cod`) is allowed as a payment method for now; other methods are rejected during tracking/booking. Similarly, the public `/users/wallet/deposit` endpoint is gated to reject deposits in non-local/non-test environments. An intentional testing exception is permitted in `TrackJob` where non-COD payment methods (such as `wallet`) are allowed only when `APP_ENV` is `local` or `test` to support integration testing of the escrow lock/release/refund paths; all other environments hard-reject non-COD payments.
  * **Reasoning**: The interface for Stripe or other bank card gateways is placeholder-only. To prevent actual security vulnerabilities or un-auditable fund flows, all payment pathways besides `cod` (outside of local/test environments) and all real environment wallet deposits are explicitly blocked. Bypassing this block in local or test environments is required to verify the correctness of the escrow logic (see [ADR-0002](docs/adr/0002-per-job-escrow-integrity.md)).
* **Manual KYC Approval Process (Ops Runbook)**
  * **Decision**: Initial KYC status approval for tenant owners is deliberately not automated or exposed via API endpoints and must be handled manually directly in the database.
  * **Reasoning**: To maintain security and avoid exposing administrative endpoints that could be targeted by attackers. In contrast, KYB/KYE document reviews are performed via the `/auth/kyb-kye/review` endpoint, which is secured by individual reviewer credentials and internal network token validations.
* **Mock OTP SMS/Email Dispatcher**
  * **Decision**: SMS dispatching is stubbed using a mock that logs OTPs to stdout, used ONLY in local environments (`APP_ENV=local|dev|development`), where OTPs are additionally exposed directly in response payloads as `dev_otp`. In every non-local environment, `RESEND_API_KEY` is REQUIRED: email OTPs are dispatched via the Resend REST API (`ResendDispatcher`). If `RESEND_API_KEY` is unset outside local environments, `auth-service` now FAILS FAST at startup instead of silently falling back to the stdout-printing mock (which would leak every OTP code into container logs). `RESEND_FROM_EMAIL` remains a required companion setting whenever `RESEND_API_KEY` is set, failing fast at startup if omitted.
  * **Reasoning**: Speeds up development and avoids dependencies on external paid SMS gateways during local testing, while providing a real email dispatch path via Resend for production. The previous silent production fallback to `MockSMSDispatcher` was removed as an OTP-leakage risk (selection logic in `selectOTPDispatcher`, verified by `TestSelectOTPDispatcher`).
* **Query Parameter Transport of Session/Signed Tokens**
  * **Decision**: Transporting authentication tokens via query parameters for WebSocket connections (`chat-service`), SSE connections (`notification-service`), and document viewing (`auth-service/documents/view`) is accepted as an intentional design tradeoff.
  * **Reasoning**: This matches standard patterns for browser APIs (like WebSockets and EventSource/SSE) which do not natively support setting custom headers during the initial handshake.
  * **Observability Verification**: The `api-gateway` access logging (`Logging` middleware) and proxy error handlers log only the URL path (`r.URL.Path`) rather than full URLs, natively ensuring query parameter tokens are redacted and never leaked to persistent logs.
* **Rating Edits / Updates**
  * **Decision**: Editing or updating an existing rating is deliberately not allowed. The endpoint blocks duplicate ratings, and no PATCH/update rating endpoint is provided.
  * **Reasoning**: To prevent data manipulation. Allowing users to modify ratings later is flagged as a potential future feature with its own authorization checks.
* **KYB/KYE Object Storage Provider**
  * **Decision**: Local-disk storage is implemented for local development (storing files securely and generating short-lived view tokens).
  * **Reasoning**: To prevent picking a cloud provider/credentials arbitrarily without user input. A future pass will wire a real S3/compatible provider behind the `storage.Storage` interface.
* **Structured National ID Text Search**
  * **Decision**: Storing national ID numbers as structured, searchable, or encrypted text to match/prevent duplicate accounts is not implemented.
  * **Reasoning**: The ID number lives inside the uploaded document images. Extracting or indexing structured text is flagged for future fraud prevention passes.
* **4th Service Category for Cargo/Goods Transport**
  * **Decision**: A 4th service category for cargo/goods transport (distinct from passenger "Ride") is deferred and not scoped for the current launch. The customer-facing home screen displays exactly 3 service tiles (Delivery, Ride, Shipping), where "Ride" maps to the backend's `transport` category.
  * **Reasoning**: Keeping the scope limited to a 3-category directory for launch. If added later, cargo/goods transport will require a brand new backend category value distinct from `transport` (which is reserved for passenger rides) to avoid ambiguity.

* **Mock Reply Feature in Notifications**
  * **Decision**: The "Reply" button on notification cards in `notifications_screen.dart` marks the notification as read and shows a `SnackBar` stating that the feature is in beta and local-only. It does not send any backend messages.
  * **Reasoning**: Accepted as a frontend-only demo interaction placeholder until a direct messaging or ticket-reply endpoint is architected for notifications.
* **Courier Profile Standalone Screen**
  * **Decision**: A standalone courier profile screen is deferred. Driver details and verified badges are rendered inline within `job_status_screen.dart` and `customer_job_map_screen.dart`.
  * **Reasoning**: Matches the mobile ergonomics of the customer flow without adding redundant screen hops.
* **Admin Payout Request Approval & Fulfillment Endpoint**
  * **Decision**: Admin-side payout request fulfillment/approval endpoint is deferred to ADR-0018 / Support Agent Console. Submitted payout requests from owners remain in `"requested"` status in the consumer application.
  * **Reasoning**: Administrative payment processing and bank ledger execution are isolated to the Support Agent Console.
* **Business Owner Configuration (Custom Pricing, Operating Hours, Geofencing)**
  * **Custom & Negotiable Pricing**: Custom or negotiable base pricing per owner beyond fixed `TenantBasePrice` and `TenantPricePerKM` values set during service creation.
  * **Business Operating Hours**: Operating hours defining days and times an owner's services are available for booking.
  * **Service Coverage Area & Geographic Radius**: Service coverage areas or geographic radii limiting which customer locations an owner's services are bookable for.
  * **Schema Absence Note**: None of these fields are currently represented in MongoDB `Service` or `Job` models; deferred to future backend service capability expansion.

---

## Known Open Items / Unverified Claims

Only features verified directly against the running application are marked as verified (✅). The following items are implemented but remain unverified end-to-end or represent accepted security risks:

* **Verified Owner Employee Listing Endpoint**: `GET /auth/employees` is implemented, IDOR-protected, rate-limited (30 req/min), and verified via unit tests (`TestGetEmployees`) and Docker E2E curl testing. Tenant owners can list all employees registered under their account (including frozen accounts) while sensitive fields (`password`, `otp_code`, `id_front_doc`) are omitted.
* **Verified Customer-Initiated Employee Assignment Gating**: Customer-initiated bookings with employee pre-assignment are fully supported and verified. The handler logic resolves the owner ID from the service tenant ID prior to the employee verification check, ensuring active role and tenant binding validations are correctly performed. Valid active employees succeed, while deactivated employees, mismatching tenants, and invalid roles are successfully gated and rejected.
* **Verified Escrow Logic & COD Completion (Isolated per Job & Concurrency Hardened)**: Escrow locking, release splits, and refunds are verified end-to-end via integration tests. Escrow balances are isolated per job record in the database. Furthermore, rollback/deletion on TrackJob database persistence failures and fail-closed behavior on unrecorded/zero-value escrow amounts are verified. If `RollbackEscrow` itself fails after `UpdateJobLockedEscrow` error during `TrackJob`, the job record is preserved in MongoDB under status `escrow_reconciliation_required` with `ReconciliationNote` and `EscrowFailureReason` rather than deleted, guaranteeing stuck escrow funds remain queryable for manual operator resolution. In addition, `TrackJob` enforces request idempotency via `Idempotency-Key` / `X-Idempotency-Key` headers (and `idempotency_key` JSON parameter); duplicate requests return the existing job (200 OK) without creating extra jobs or double-locking escrow. Concurrency tests simulating simultaneous complete/cancel, double-complete, and double-cancel on the same job (for both escrow-based and COD payment methods) confirm that exactly one request succeeds, verifying that race conditions are closed.
* **Verified Admin/Owner Escrow Reconciliation Review Endpoints & Frontend Screen**: `GET /users/jobs/reconciliation-queue` and `POST /users/jobs/reconciliation-resolve` backend endpoints and Flutter frontend (`ReconciliationProvider`, `OwnerReconciliationQueueScreen`, `ReconciliationJob` model) are fully implemented, owner-role gated (`resolveTokenWithRole`), tenant-isolated with IDOR protection, and rate-limited (30 req/min each — the queue via `user:reconciliation`, resolve via `user:reconciliation_resolve`; an earlier revision of this entry claimed both were rate-limited when only the queue actually was — corrected with the resolve limiter's addition, verified by `TestResolveReconciliation_RateLimiting`). Tenant owners can query jobs in status `escrow_reconciliation_required` (with human-readable failure reason mapping like "Distance mismatch — under 70% of booked distance", `ReconciliationNote`, and `LockedEscrowAmount`) and resolve stuck escrow via `release_to_employee` (executing profit split payout) or `refund_to_customer` (refunding locked escrow). Interactive confirmation dialogs prevent accidental fund movement. Resolutions update job status atomically, record resolution notes, update wallets/ledger, ship audit events (`ESCROW_RECONCILIATION_RESOLVED`), surface 409 conflict & 429 rate-limit responses, and update the queue dynamically. Verified via backend integration tests and Flutter widget tests (`test/reconciliation_queue_test.dart` 5/5 pass). (Commit `cd94f15851ca13b561f05aad65b424e598461f22`).
* **Fail-Closed Rate Limiting on Redis Unavailability**: If the shared Redis instance becomes unavailable at runtime, all rate limiters across all microservices (api-gateway, auth-service, chat-service, notification-service, user-service) will fail-closed. This means incoming traffic is restricted/blocked and authentication attempts are denied with critical security logs (`[SECURITY CRITICAL]`), rather than allowing un-throttled traffic to bypass security boundaries.
* **Dev-Grade mTLS CA**: The certificates generated for mTLS use a dev-grade local Root CA. For production, a real internal CA (e.g. AWS Private CA, HashiCorp Vault PKI, or cert-manager on Kubernetes) must be integrated.
* **Client-Submitted Booking Coordinates for Pricing & Dual-Layer Speed Plausibility Checks**: Distance-based pricing/escrow calculations rely on coordinates submitted by the client at job booking time. Recomputed amounts on `CompleteJob` and `CancelJob` use only the initial booking coordinates (`job.Location`). In addition, live location broadcasts from employees in `UpdateJobLocation` are gated by dual-layer speed plausibility checks (rejecting updates with `IMPLAUSIBLE_SPEED_DETECTED` audit logs):
  1. *Per-Step Speed Check*: Calculates Haversine distance between consecutive location updates divided by time elapsed since last update (`now.Sub(lastTime)`), rejecting step jumps implying `>150.0 km/h` (`MaxReasonableSpeedKmh`).
  2. *Cumulative Route Speed Check (Phase 0, Commit 12dd2f3)*: Calculates total cumulative Haversine distance across all recorded waypoints (`job.Location`, `job.Waypoints...`, and candidate coordinate) divided by total time elapsed since job creation (`now.Sub(job.CreatedAt)`), rejecting cumulative routes implying `>150.0 km/h` (`MaxReasonableSpeedKmh`).
* **Verified Owner and Customer Job Listing Endpoints**: `GET /users/jobs/owner` and `GET /users/jobs/mine` are implemented, tenant-isolated, rate-limited, and verified via dedicated unit tests (`TestGetJobsByOwner`, `TestGetJobsByCustomer`) and Docker E2E curl testing against the live stack. Security is anchored by identity-scoped authorization (database queries filter strictly by the authenticated JWT claims identity) combined with parameter validation (explicit target `owner_id` / `user_id` query parameters, if passed, are checked against JWT claims and trigger 403 `[IDOR DETECTED]` logs on mismatch). Owners list tenant jobs via `GET /users/jobs/owner` (with `OwnerJobResponse` DTO), customers list booking history via `GET /users/jobs/mine` (with `CustomerJobResponse` DTO), and rate limiting blocks excessive requests with 429.
* **Verified Rate Limiting Across Rating Endpoints (Item #3)**: `POST /users/jobs/rate` (`RateJob`) and `GET /users/ratings` (`GetRatings`) invoke IP-keyed rate limiting via `u.limiter.CheckAndRecord` matching `TrackJob` and `WalletDeposit`, preventing spam and enumeration with HTTP 429 responses. Verified via `TestRateJobAndGetRatings_RateLimiting`.
* **Verified Redis-Backed TrackJob Idempotency & 24h TTL (Item #2 Remediation)**: Replaced the single-instance in-memory `idempotencyJobs` map with Redis-backed key storage (`idempotency:job:<userID>:<key> -> job_id`) with 24-hour TTL (`24 * time.Hour`). Removed `idempotencyMu` and `idempotencyJobs` fields from `UserService`. Cross-replica duplicate detection and key TTL expiration verified via multi-instance unit test (`TestTrackJob_RedisBackedIdempotency_MultiInstanceAndTTL`).
* **Verified Post-Authentication Per-User Namespaced Idempotency Replay (IDOR Disclosure Fix)**: Hardened `TrackJob` (`services/user-service/internal/handlers/jobs_handlers.go`) so the Redis idempotency replay lookup executes only AFTER the caller's identity is resolved from a signed JWT via `resolveTokenWithRole`, and keys are namespaced per user (`idempotency:job:<userID>:<key>`). Previously the lookup ran before any authentication, so any unauthenticated caller supplying a victim's client-chosen idempotency key received the victim's full job object (coordinates, payment method, IDs) with 200 OK. Added regression test `TestTrackJob_IdempotencyReplayRequiresAuthentication` asserting 401 + no job disclosure for unauthenticated replay and intact 200 OK duplicate behavior for authorized replay. Verified via `go test ./...` in `user-service` (100% pass).
* **Verified Token Role Gating in resolveTokenWithRole & Removal of Dead Wrapper (Item #4 & Task 2)**: Audited all call sites in `handlers.go` and enforced explicit variadic role checks (`resolveTokenWithRole`) across all 18 endpoint token resolution locations (`CreateService`, `TrackJob`, `GetJob`, `CompleteJob`, `CancelJob`, `UpdateJobLocation`, `GetWallet`, `WalletDeposit`, `GetSubscription`, `UpdateSubscription`, `RateJob`, `GetRatings`). Removed the unused legacy `resolveToken` wrapper function. Verified via grep audit and comprehensive role enforcement test suite (`TestRoleEnforcement_*`).
* **Verified Public/Owner Rating Summary Access Model (Item #5)**: `GetRatings` (`GET /users/ratings`) authenticates the caller via JWT token while accepting candidate employee/user target IDs in query parameter `user_id`. This enables owners to view candidate employee ratings before hiring without requiring possession of the candidate's JWT token. Verified via `TestUserServiceHandlers/GetRatings`.
* **Verified Dual-Layer Rate Limiting for GetLedger (Item #6)**: `GetLedger` (`GET /users/ledger`) enforces both IP-based and tenant-based rate limiting via `u.limiter.CheckAndRecord`, matching `WalletDeposit`'s financial security layer. Verified via `TestGetLedger_RateLimiting`.
* **Verified Test Payment Bypass Gating & Production Safeguard (Item #7)**: Test payment method bypasses (in `TrackJob` and `WalletDeposit`) require the explicit environment variable `ALLOW_TEST_PAYMENT_BYPASS=true` in addition to non-production environment settings (`APP_ENV=test` or `local`). Configuring `ALLOW_TEST_PAYMENT_BYPASS=true` when `APP_ENV=production` fails service startup immediately. Verified via `TestLoad_AllowTestPaymentBypass` and `TestTestPaymentBypass_Gating`.
* **Verified RateJob Comment Sanitization & Length Bound (Item #8)**: `RateJob` (`POST /users/jobs/rate`) enforces a 1000-character maximum length limit on incoming comments and sanitizes comments server-side using `html.EscapeString`. In addition, frontend rendering in `frontend/lib` uses standard Flutter `Text` widgets, ensuring comments are rendered strictly as plain text. Verified via `TestRateJob_CommentSanitizationAndLengthLimit`.
* **Verified Redis-Backed Location Update Throttle State (ADR-0005 Correction Remediation)**: Replaced in-memory `locationInFlight` and `locationLastUpdate` maps in `user-service` (`UpdateJobLocation`) with Redis-backed atomic key evaluation (`loc:inflight:<job_id>` and `loc:lastupdate:<job_id>`) using an atomic Lua script. Closed cross-replica throttling gaps and verified fail-closed behavior on Redis unavailability. Verified via multi-instance unit test.
* **Known Limitation: notification-service Redis Pub/Sub Self-Delivery Dependency**: In `notification-service` (`services/notification-service/internal/hub/hub.go`), `Broadcast()` relies on the instance's own pattern subscription (`PSubscribe("notify:*")`) to loop messages back to local clients on the origin replica. Every notification incurs a Redis Pub/Sub network round-trip. If the publishing instance's subscriber connection experiences a transient hiccup at the moment of publish, local clients on the origin instance could miss an alert. (Documented in [ADR-0005](docs/adr/0005-realtime-hub-horizontal-scaling.md)).
* **Verified Go Version Standardization & Automated Drift Guard**: All sources of truth (`go.work`, 7 `go.mod` files, `.github/workflows/ci.yml`, and 5 `Dockerfile`s) are standardized to Go 1.26 with explicit toolchain pinning (`toolchain go1.26.6`, `golang:1.26.6-alpine`, `go-version: '1.26.6'`). An automated drift guard step in `.githooks/pre-push` and `ci.yml` enforces strict Go language floor, toolchain directive, and patch version consistency across all files.
* **Verified Resend Email OTP Delivery End-to-End (`info@logiclinkeg.tech` / `ResendDispatcher`)**: Queried Resend API `/domains` endpoint and confirmed verified sending status (`status: verified`, `sending: enabled`) for domain `logiclinkeg.tech`. Real end-to-end email OTP dispatching was verified from sender `info@logiclinkeg.tech` via `ResendDispatcher` (`github.com/resend/resend-go/v3`) against live Resend API infrastructure (`Resend email ID: 3f0843d1-5682-455d-9ebe-00fcec6590be`). Verified full lifecycle: user signup (`POST /api/v1/auth/signup`), `dev_otp` suppression in non-local mode (`APP_ENV=production`), AES-256-GCM OTP ciphertext storage at rest in MongoDB, live email delivery, 2FA authentication via `POST /api/v1/auth/verify-otp` (HTTP 200 OK + JWT issued), and confirmed graceful error logging on invalid API keys without crashing `auth-service`.

---

## Request Field Naming Compatibility (JWT Integration)

To address naming clarity issues where request fields expected signed JWT tokens rather than raw database IDs, the backend supports newer `_token` field aliases alongside legacy `_id` / raw parameter names:
| Legacy Field Name | New Preferred Alias | Affected Endpoints |
|---|---|---|
| `user_id` / `id` | `user_token` | GET /auth/user, GET /auth/user/public-profile, POST /users/jobs/track, GET /users/ratings |
| `owner_id` | `owner_token` | POST /users/services, POST /users/jobs/track |
| `employee_id` | `employee_token` | POST /users/jobs/track, GET /users/jobs/get |
| `requester_id` | `requester_token` | GET /auth/audit-log, GET /auth/user/public-profile, GET /users/jobs/get, POST /users/jobs/complete, POST /users/jobs/cancel, POST /users/subscription, POST /users/jobs/location/update |
| `tenant_id` | `tenant_token` | GET /users/wallet, POST /users/wallet/deposit, GET /users/ledger, GET /users/subscription, POST /users/subscription |
| `rated_by` / `rated_user` | `rated_by_token` / `rated_user_token` | POST /users/jobs/rate |

The Go backend handles both legacy and preferred naming conventions compatibly (preferring `_token` if both are supplied). *Note: The Flutter frontend has not yet been updated to use the preferred fields.*

---

## Standing Working Rule

This file is a persistent document tracking the real state of the repository.

> [!IMPORTANT]
> **Every code change must update `AI_CONTEXT.md` in the SAME commit.**
> Any pull request or commit that changes application behavior, adds endpoints, modifies security boundaries, or changes the tech stack but does not update `AI_CONTEXT.md` is considered incomplete. 
> Keep the feature status and unverified claims list strictly in sync with actual code implementations.
> **Commit SHA must be the real, full git hash of the commit being documented, captured via `git rev-parse HEAD` immediately after committing — never a placeholder.**
> **docs/APPLICATION_MAP.md must be updated in the same commit whenever a change adds, removes, renames, or changes the auth/permission requirements of an HTTP endpoint, or changes an inter-service call path. The "as of Git commit" note at the top must be refreshed to the new commit's short SHA in that same commit.**
> **Auto-Commit Policy**: After completing any logical unit of work (e.g. a bug fix, security fix, or feature phase) and verifying that the corresponding test suites pass, immediately stage files, commit using conventional style (`fix:`, `feat:`, `docs:`, `ci:`), and push to origin. Before committing any Go file changes, run `gofmt -w` on the modified files (or `gofmt -l .` repo-wide) and confirm no output, since CI enforces gofmt as a blocking gate. Never batch multiple unrelated changes into one commit or wait until the end of the session to commit them.
> **Dependency Drift Prevention**: Any change to shared/infra that adds a new external dependency must be followed by `go mod tidy && go build ./...` in every service that imports shared/infra, and a full `docker compose down && build --no-cache && up` verification, before considering the change complete — go.sum drift can pass CI's module resolution while still breaking local/production Docker builds.
> **Environment Verification Disclosure**: Any report or documentation claiming test suite pass status must explicitly specify whether verification was performed against local execution, GitHub Actions CI execution, or both, to prevent environment divergence.
> **Backend Contract Verification**: Before building a frontend feature that depends on a specific backend contract (channel name, endpoint, payload shape), verify that contract actually exists in the current code first (grep for it), and if it doesn't, stop and inform the user instead of building against assumed or planned contracts.


* **Verified Endpoint Map Regeneration & Role Description Alignment (Task 1)**: Regenerated `docs/APPLICATION_MAP.md` via `make docs` and updated `KnownEndpoints` in `shared/infra/docgen/generator.go` to reflect exact role requirements following Gap 1 remediation (`resolveTokenWithRole`). Verified via `make docs-check`.
* **ADR-0007 Phase 1 Actual Distance Settlement & Reconciliation Review Flag**: Implemented GPS trail actual distance calculation, guaranteed floor payout `max(LockedEscrowAmount, A_actual)`, and under-distance manual review flag (`ActualDistance < 0.70 * BookedDistance`) transitioning jobs to `escrow_reconciliation_required` in `user-service` (`CompleteJob`). For non-COD escrow jobs, payouts exceeding `LockedEscrowAmount` are capped at `LockedEscrowAmount` per ADR-0002 to preserve wallet isolation and emit `ESCROW_LIMIT_EXCEEDED` operator audit logs. Verified via `TestCompleteJob_ADR0007_Phase1_SettlementAndReconciliation`. (Commit `44e51b2efdc925c6862df04658b73429a9680154`).
* **E2E Integration Test Redis Port Resolution & Nil Safety Guards**: Resolved CI gate failure in `adr0006_e2e_integration_test.go` and `adr0007_e2e_integration_test.go` caused by hardcoded `localhost:6380` Redis addresses. Updated Redis initialization to dynamically parse `REDIS_URI` / `REDIS_ADDR` with fallback to default port `6379`, added `Ping` connectivity checks to skip gracefully if Redis is unavailable, and added explicit HTTP response status assertions and nil checks before dereferencing struct fields. Verified via local Go test execution.
* **ADR-0008 Backend Live Employee Map Tracking & End-to-End Verification**: Extended `canAccessChannel` in `chat-service` to authorize `fleet:<owner_id>` for owner role users. Wired `UpdateJobLocation` in `user-service` to broadcast `location_update` messages to both `job:<id>` and `fleet:<owner_id>` channels via `chat-service`. Added `active_only=true` query filtering to `GET /users/jobs/owner` for initial fleet hydration. Built `TestADR0008_E2E_LiveEmployeeMapTracking` integration test confirming real WebSocket connections, `fleet:<owner_id>` channel authorization, and real-time location broadcast delivery. Updated ADR-0008 status to Accepted. (Commit `17ebd52e1d88246c5141f793abfd08c5515cc133`).
* **Verified Centralized Friendly Error Messages Utility & Raw Error Leakage Prevention**: Created `frontend/lib/core/error_messages.dart` (`friendlyErrorMessage(Object? error)`) mapping `ApiClientException` status codes (400, 401, 403, 404, 409, 429, 5xx) to friendly English error messages without branching on backend raw error strings or leaking internal error details. Replaced raw error displays across all frontend providers and screens with `friendlyErrorMessage(e)` while preserving developer debug logging via `debugPrint`. Verified via `dart format --set-exit-if-changed lib/`, `flutter analyze` (0 issues), and `flutter test` (all tests passed including 4 table-driven friendly error unit tests). (Commit `fc4456efed1f6f86787943ad15133a7b9f0845e1`).
* **Verified Flutter Client Backend Connectivity Setup Guide (CONNECTING_TO_BACKEND.md)**: Produced `frontend/CONNECTING_TO_BACKEND.md` covering backend health checks (`/health`), `API_BASE_URL` resolution via `--dart-define`, per-platform host configurations (Android Emulator `10.0.2.2`, physical Android `adb reverse tcp:8080 tcp:8080`, iOS Simulator `localhost`, physical iOS `192.168.x.x`), HTTPS dev cert bypass mechanics (`DevHttpOverrides`), clean rebuild workflow using verified Application ID / Bundle ID (`com.saascore.frontend`), real-time log streaming, and troubleshooting matrix. Added pointers in `README.md` and `frontend/README.md`. Verified via `make docs-check`. (Commit `c054907795e113e7d47edbd4776e6a52536032f1`).
* **Verified Flutter KYB/KYE Document Upload Screen**: Implemented `KycDocumentUploadScreen` (`frontend/lib/screens/kyc_document_upload_screen.dart`), updated `UserProfile` (`frontend/lib/models/user_profile.dart`), extended `AuthProvider` (`frontend/lib/providers/auth_provider.dart`), and wired banner prompts + AppBar actions in `home_screen.dart`. Dynamically renders role-conditional document upload slots (4 slots for owner: `id_front`, `id_back`, `selfie`, `business_proof`; 3 slots for employee: `id_front`, `id_back`, `selfie`). Integrates `StatusBadge` for KYC status displays, shows prominent rejection reason banner when status is `rejected`, enforces client-side file size ($\le 10$MB) and MIME format rules prior to network calls, handles per-slot upload loading and error states independently, and locks/disables upload actions when status is `approved`. Verified via `flutter analyze` (clean 0 issues) and `flutter test` (`test/kyc_document_upload_screen_test.dart` 5/5 pass). (Commit `05cbbc7136de2a6a630ff2ebe16a72426ce289aa`).
* **Verified Backend Forgot Password & Reset Password Endpoints**: Implemented `POST /auth/forgot-password` and `POST /auth/reset-password` in `auth-service` reusing existing AES-256-GCM encrypted OTP store methods (`SetOTP` / `VerifyOTP`). `POST /auth/forgot-password` provides anti-enumeration 200 OK responses, IP/email rate limiting, 5-minute encrypted OTP generation, and dispatch via `a.dispatcher.Dispatch`. `POST /auth/reset-password` validates `email`/`otp`/`new_password`, enforces rate limit lockouts, calls `store.VerifyOTP`, bcrypt-hashes passwords, updates user record, and clears OTP fields to prevent replay attacks. Built 6 unit tests covering anti-enumeration response parity, rate limiting, password reset & old password invalidation, generic wrong OTP error & rate-limit failure recording, missing password validation, and OTP reuse prevention. Regenerated `docs/APPLICATION_MAP.md` via `make docs`. (Commit `ee443e39358b7f4c06fe7eb2f2a57b2f4e4c8d79`).
* **ADR-0009 Negotiable Transport Pricing Atomic Compare-and-Swap TOCTOU Hardening**: Remediated TOCTOU race conditions in `RespondPrice` and `ProposePrice` in `user-service`. Added status-conditioned compare-and-swap update filters to `UpdateJobAgreedPrice`, `UpdateJobCancellation`, and `UpdateJobPriceProposal` (`status: AwaitingPriceResponse`), returning `job_state_changed` on zero matched documents. Wired `RespondPrice` to execute automatic compensating `performRollbackEscrow` and return `409 Conflict` on `job_state_changed`. Built concurrency race tests (`TestRespondPrice_ConcurrencyRace_DoubleEscrowPrevention` and `TestProposePrice_ConcurrencyRace_OverwrittenProposalPrevention`). Published ADR-0009 (`docs/adr/0009-atomic-compare-and-swap-transport-pricing.md`). (Commit `ffb75f989bd25ed0ac5bb54f46094a66ecad0080`).
* **ADR-0009 Hardening (Gap 3: Job State Changed Rollback Failure Retry & Reconciliation Fallback)**: Hardened `RespondPrice` (`user-service`). When `UpdateJobAgreedPrice` returns `job_state_changed` post-escrow lock, if initial `performRollbackEscrow` fails, handler retries `performRollbackEscrow` once. If retry fails, handler invokes `u.store.UpdateJobReconciliation` to mark job `models.JobStatusEscrowReconciliationRequired` with reconciliation notes while maintaining HTTP `409 Conflict` (`job_state_changed`) client response. Built unit test `TestRespondPrice_JobStateChanged_RollbackFailure_ReconciliationFallback`. Appended follow-up note to ADR-0009. (Commit `42e5843b3471bb272f1deef9846ad14b79414e35`).
* **Verified ListServices Spatial Coordinate Bounds Validation**: Resolved "Flagged for Next Pass" item 3 in `docs/audit/shared-module-review.md`. Added `isValidCoordinate(refLat, refLon)` bounds validation (`-90 <= lat <= 90`, `-180 <= lon <= 180`) to `ListServices` in `user-service` when `near_by=true` or when explicit `lat`/`lon` parameters are provided. On invalid coordinates, logs `[SECURITY WARNING]`, ships audit event (`INVALID_COORDINATES_DETECTED`), and returns HTTP 400 (`invalid_coordinates`). Built 4 unit tests in `list_services_test.go`. (Commit `679667f9c994edd582d5c6d79226e78d1b277320`).
* **Verified Android Emulator End-to-End Backend Connectivity**: Verified Android Emulator connectivity end-to-end against the live Docker Compose backend using `API_BASE_URL=https://10.0.2.2:8080/api/v1`. Confirmed backend health check (`curl -k https://localhost:8080/health` -> `{"status":"ok"}`), debug-mode self-signed certificate bypass (`DevHttpOverrides` / `bypassBadCertificate` in `kDebugMode`), and exercised actual API Gateway route `GET /api/v1/users/services` returning HTTP 200 OK with 15 seeded service entries. Verified via local execution against live backend stack. (Commit `807f98bbca30bf072824b11892a60af02ff310ba`).
* **Verified Android Gradle compileSdk Pinning for AAR Metadata Alignment**: Resolved Android APK build failure (`checkDebugAarMetadata... Dependency 'flutter_plugin_android_lifecycle' requires 'compileSdkVersion' 36 or higher`) by pinning `compileSdk = 36` in `frontend/android/app/build.gradle.kts`. Updated troubleshooting matrix in `frontend/CONNECTING_TO_BACKEND.md`. Verified via `flutter analyze` and `flutter test`. (Commit `c1f00d3d026207acb4f78741afaae121c07afa88`).
* **Verified AI_CONTEXT.md Orphaned Commit SHA Narrative Formatting Mitigation**: Truncated full 40-character orphaned commit SHA in historical narrative text in `docs/changelog/bug-fixes.md` to short ref `05b5ca7...` to prevent global CI regex scanning from matching non-citation narratives as live commit references. Verified via whole-repo markdown SHA verification check (0 BLOCKED lines) and `make docs-check`. (Commit `3a032f138f47d0a9b140f12518e9ee59fe4ce266`).
* **Verified Markdown Commit SHA Validation Regex Narrowing & No-Amend Policy**: Narrowed regex scanning in `.githooks/pre-push` and `.github/workflows/ci.yml` to citation lines (`Commit SHA`, `Commit:`, `Related Commit SHA`, `Fix Commit SHA`, `(Commit `) to eliminate false positives on historical reflog narratives. Added rule #6 to `CLAUDE.md` prohibiting `git commit --amend` on already-cited commits. Verified via whole-repo SHA check (229 citations verified, 0 false positives) and `make docs-check`. (Commit `3a032f138f47d0a9b140f12518e9ee59fe4ce266`).
* **Verified Restored Full-Coverage Markdown Commit SHA Validation & Non-Citation Truncation Rule**: Superseded the citation-marker narrowing and two-tier warning pass by restoring strict repo-wide 40-character hex SHA scanning (blocking on any unresolvable 40-char SHA) in `.githooks/pre-push` and `.github/workflows/ci.yml` to close citation keyword coverage gaps. Added rule #7 to `CLAUDE.md` mandating that non-authoritative historical/orphaned commit mentions in prose be written truncated (`05b5ca7...`). Verified via repo-wide check (0 BLOCKED lines) and fabricated hash regression check (BLOCKED, exit 1). (Commit `c14bac58208173e914b376cf0bb8c89b4f90f433`).
* **Verified Negotiable Transport Pricing UI (ADR-0006)**: Added API client methods `proposePrice` (`POST /users/jobs/propose-price`) and `respondPrice` (`POST /users/jobs/respond-price`) in `ApiClient` and `MarketplaceProvider`. Built negotiation counter-offer UI with $[0.5 \times P_{\text{suggested}}, 1.5 \times P_{\text{suggested}}]$ validation in `JobStatusScreen` for transport-category jobs in `awaiting_price_response` state. Added incoming proposal card displaying proposer role, proposed fare, original system fare, and percentage difference comparison. Handled 409 `job_state_changed` conflict responses with user-friendly notification and auto-refresh. Integrated 5-minute negotiation window timer with live ticker and explicit expired banner. Verified via `flutter analyze` (0 issues) and `flutter test` (`test/negotiable_transport_pricing_test.dart` 5/5 pass, entire suite 40/40 pass).
* **Verified FCM Push Notification Device Token Storage (Commit 1)**: Added `DeviceToken` struct and `DeviceTokens []DeviceToken` slice to `models.User` in `auth-service`. Added `UpsertDeviceToken` and `RemoveDeviceToken` methods to MongoDB store with duplicate prevention. Added `POST /auth/device-token` and `DELETE /auth/device-token` endpoints in `auth-service` supporting token registration, update, multi-device registration, and unregistration. Built regression test suite (`TestDeviceToken_RegistrationAndUpsert`, `TestDeviceToken_Unauthorized`). Regenerated `docs/APPLICATION_MAP.md` via `make docs`. Verified via `go test ./...` (100% pass) and `make docs-check`. (Commit `a557bf2b05fe3f2da89479ea0e55d48b958cea3e`).
* **Verified FCM Push Notification Dispatch in notification-service (Commit 2)**: Added lightweight FCM HTTP v1 REST client (`fcm.Client`) in `notification-service` supporting optional credentials (`FCM_SERVICE_ACCOUNT_JSON` / `FCM_PROJECT_ID`) with fail-safe no-op fallback. Added `HTTPDeviceTokenFetcher` for cross-service device token lookups via internal `auth-service` calls (`GET /auth/user?id=<user_id>`). Integrated non-blocking parallel `PushDispatcher` into `SSEHub` (`go dispatchPush`) that delivers FCM push notifications alongside SSE broadcasts without blocking real-time streams. Implemented automatic stale token cleanup (`UNREGISTERED` / `404 NOT_FOUND` responses trigger `DELETE /auth/device-token` call to `auth-service`). Built comprehensive unit test suite in `fcm_test.go` and `hub_test.go` (`TestFCMPush_Success`, `TestFCMPush_StaleTokenHandling`, `TestFCMPush_DisabledMode`, `TestSSEHub_FCMPushParallelDeliveryAndStaleTokenCleanup`, `TestSSEHub_FCMFailureDoesNotFailSSEBroadcast`). Regenerated `docs/APPLICATION_MAP.md` via `make docs`. Verified via `go test ./...` (100% pass) and `make docs-check`. (Commit `c8993f96384d6681d4c51caec9d02b61052352f8`).
* **FCM Push Notification Frontend Registration (Commit 3) — CORRECTED, END-TO-END UNVERIFIED**: Added `firebase_messaging` to `pubspec.yaml` (+ direct `firebase_core ^2.32.0`). Added `registerDeviceToken` and `unregisterDeviceToken` methods to `ApiClient`. Built `PushNotificationService` with `PushMessagingAdapter` wired into `AuthProvider` auto-login, login/signup success, and logout flows. **Honesty correction**: the original `DefaultPushMessagingAdapter` was a stub fabricating `'fcm-device-token-<timestamp>'` tokens with an empty foreground handler — earlier "[VERIFIED]" claims for this item were false and every login registered a garbage device token to auth-service. The adapter is now genuinely backed by `firebase_messaging` (`getToken`/`deleteToken`/`FirebaseMessaging.onMessage`) and degrades to safe no-ops (returning `null`, registering nothing) when `Firebase.apps` is empty. End-to-end push delivery is explicitly UNVERIFIED until Firebase project configuration (`google-services.json` / `GoogleService-Info.plist` + `Firebase.initializeApp()`) lands in the mobile builds. Verified via `flutter analyze` (0 issues), `test/default_push_adapter_test.dart` (no fabricated tokens without Firebase), and `test/fcm_push_registration_test.dart` (7/7 pass).
* **Verified Production Infrastructure & Database Auth Hardening**: Remediated public database access vulnerability by enforcing MongoDB root authentication (`MONGO_INITDB_ROOT_USERNAME` / `MONGO_INITDB_ROOT_PASSWORD`) and Redis authentication (`REDIS_PASSWORD` / `--requirepass`). Restricted Mongo and Redis port bindings in `docker-compose.yml` to localhost (`127.0.0.1:27017:27017` and `127.0.0.1:6380:6379`), preventing public internet exposure while preserving internal `saas-net` bridge networking across all 5 microservices. Updated environment variable templates and secrets documentation in `infrastructure/.env.example`. Authored `docs/DEPLOYMENT.md` deployment guide covering VPS prerequisites, secrets generation, mTLS cert generation, health verification, Caddy TLS reverse proxy configuration, UFW firewall rules, logs, service management, and `mongodump` manual database backups. Verified via `docker compose config`, local stack startup with authentication enabled (`saas-mongo` and `saas-redis` reporting `healthy`, all services running), `curl -k https://localhost:8080/health` (`{"status":"ok"}`), and `.githooks/pre-push` gate (100% pass). (Commit `49572882a98844de0f688fa97275c9a835dc93ce`).
* **Verified Two-Repo Deployment Pipeline & GHCR Publishing Setup**: Created dedicated private production deployment repository `omarmaarouf18/saas-core-deploy` containing image-referenced `docker-compose.yml`, production `.env.example`, `Caddyfile`, and deployment README. Added `.github/workflows/build-and-publish.yml` in primary repo (`saas-core`) triggering on push to `main` to build microservices' `prod` stage Docker images, publish them to GHCR (`ghcr.io/omarmaarouf18/saas-core-<service>:${SHA}` and `:latest`), and sync new image tags to `saas-core-deploy` via `DEPLOY_REPO_PAT`. Updated service Dockerfiles for root-context compilation resolving `shared/infra`. Updated `docs/DEPLOYMENT.md` detailing two-repo workflow. Verified via local `prod` stage Docker builds, successful `saas-core-deploy` repository creation and initial push, `make docs-check`, and `.githooks/pre-push` gate (100% pass). (Commit `bbdaaa6b508061e9300e154b8a347358898e064d`).
* **Verified End-to-End CI/CD Pipeline and Git Hooks Technical Documentation**: Authored `docs/CI_CD_AND_HOOKS.md` detailing the mechanics, execution order, and verification rules of the local pre-push hook (`.githooks/pre-push`), GitHub Actions CI (`.github/workflows/ci.yml`), build-and-publish workflow (`.github/workflows/build-and-publish.yml`), two-repo deployment architecture (`omarmaarouf18/saas-core-deploy`), and known limitations (such as pre-push hook bypass on unconfigured fresh clones and GHCR package authentication). Updated `README.md` documentation index. Verified via `make docs-check` and `.githooks/pre-push` gate (100% pass).
* **Verified Release & Server Deployment Runbook (RUNBOOK.md)**: Authored `docs/RUNBOOK.md` detailing the operational release lifecycle from feature branch preparation (`logic-exploitation`), CI validation, merging to `main`, automatic GHCR container image building & `saas-core-deploy` tag synchronization, production server deployment steps, and single-command rollback procedures. Updated `README.md` documentation index. Verified via `make docs-check` and `.githooks/pre-push` gate (100% pass).
* **Verified Azure VM Production Deployment Documentation**: Updated `docs/DEPLOYMENT.md` to reflect real production deployment on Azure VM with custom domain. Added Microsoft Azure cloud-specific provisioning section (`Standard_B2s_v2` SKU sizing, NSG inbound rules for SSH/HTTP/HTTPS/API Gateway, Azure Policy region assignment discovery), containerized Caddy Docker Compose sibling option (`docker-compose.caddy.yml`, `network_mode: host`), Custom Domain & DNS configuration notes (Vercel DNS panel distinction vs platform domain assignment, 1-2h propagation verification with `nslookup`/dnschecker.org), and a dedicated Troubleshooting section (stale container name conflicts, missing `--env-file .env`, `curl` (35) connection resets during `air` cold compilation). Verified via `make docs-check`.
* **Verified Four-Repository Multi-Artifact Release Pipeline & ADR-0010 Architecture**: Synchronized platform deployment architecture across 4 decoupled GitHub repositories: `omarmaarouf18/saas-core` (Go/Flutter monorepo), `omarmaarouf18/saas-core-deploy` (VPS Docker Compose deployment), `omarmaarouf18/quick-delivery-mobile` (standalone Flutter mobile APK builds & releases), and `omarmaarouf18/logiclinc` (Vercel marketing website with live `app-release.json` updates). Configured 3 dedicated PAT secrets (`DEPLOY_REPO_PAT` in `saas-core`, `MOBILE_REPO_PAT` in `saas-core` with both `Contents` and `Workflows` read/write permissions for subtree pushes touching `.github/workflows/`, and `LOGICLINC_REPO_PAT` in `quick-delivery-mobile` for website release publishing). Authored `docs/adr/0010-separate-repos-for-deployment-artifacts.md` documenting decision rationale (clean pull surfaces, zero application source code exposure on VPS/mobile/web platforms), cross-repo PAT management friction, failure modes (checkout credential overrides, workflow scope rejections), secret handling discipline (local environment variable ingestion, zero token echoing), and trade-offs. Updated `docs/CI_CD_AND_HOOKS.md` and `docs/DEPLOYMENT.md`. Verified via end-to-end execution of `sync-mobile-frontend.yml` (run `30613645327`), `build-apk.yml` (run `30613691636`), live `app-release.json` verification on `logiclinc`, `make docs-check`, and `.githooks/pre-push` gate (100% pass).
* **Verified Containerized Caddy Integration in Docker Compose & ADR-0011**: Resolved Production Caddy Orphan Container Incident by adding containerized `caddy` service (`image: caddy:2-alpine`, `container_name: saas-caddy`, `restart: unless-stopped`, ports `80:80`/`443:443`/`443:443/udp`) to `infrastructure/docker-compose.yml` on `saas-net` network with `api-gateway` dependency and persistent volume mounts (`./Caddyfile:/etc/caddy/Caddyfile:ro`, `caddy_data:/data`, `caddy_config:/config`). Authored `docs/adr/0011-containerized-caddy-in-compose-stack.md` and updated `docs/DEPLOYMENT.md` deployment guide. (Commit `ef4bd21b7a0c2d1e721027c5d1852b77cf7b42c7`).
* **Verified Repository Ecosystem Map (REPOSITORY_MAP.md) & Production Troubleshooting Hardening**: Authored `docs/REPOSITORY_MAP.md` onboarding guide mapping the 4-repo architecture (`saas-core`, `saas-core-deploy`, `quick-delivery-mobile`, `logiclinc`) and production VM target (`quickdelivery-vm`), Mermaid pipeline trigger flowchart, PAT secret scope security matrix (`DEPLOY_REPO_PAT`, `MOBILE_REPO_PAT`, `LOGICLINC_REPO_PAT`), branch deployment rules (`main` vs `logic-exploitation`), and operator triage runbook. Updated `docs/DEPLOYMENT.md` §10 troubleshooting (correcting §10.3 to reflect static binary runtime, adding §10.4 MongoDB `storedKey mismatch` password change resolution, and §10.5 orphaned `saas-caddy` realign procedure). Updated `README.md` documentation index.
* **Verified GitHub App Installation Token Migration (`quick-delivery-automation`) & Deploy-Repo Incident Rollback**: Configured GitHub App `quick-delivery-automation` secrets (`APP_ID` and `APP_PRIVATE_KEY`) across `omarmaarouf18/saas-core` and `omarmaarouf18/quick-delivery-mobile`. Migrated all multi-repo workflows (`build-and-publish.yml`, `sync-mobile-frontend.yml`, and `frontend/.github/workflows/build-apk.yml`) from legacy PATs (`DEPLOY_REPO_PAT`, `MOBILE_REPO_PAT`, `LOGICLINC_REPO_PAT`) to dynamic short-lived GitHub App installation tokens via `actions/create-github-app-token@v1` scoped per repository (`saas-core-deploy`, `quick-delivery-mobile`, `logiclinc`). Audited workflows to confirm zero remaining PAT references, deleted legacy PAT secrets from GitHub Secrets across all repos, and revoked tokens at the GitHub level. Documented incident where temporary `workflow_dispatch` on `build-and-publish.yml` (Run ID `30684940738`) pushed out-of-policy dev branch image tags (`375a017`) to `saas-core-deploy:main`. Immediately executed rollback on `saas-core-deploy:main` (reverting image tags back to `main` HEAD `efbe55b` via commit `c0b3f6a` (saas-core-deploy)), removed `workflow_dispatch` from `build-and-publish.yml` restoring strict push-to-main-only trigger policy, and secured local key file in `~/.config/github-apps/`. Updated `docs/CI_CD_AND_HOOKS.md`, `RUNBOOK.md`, and `docs/adr/0010-separate-repos-for-deployment-artifacts.md`. Verified via local hooks, `make push`, and live workflow executions.
* **Verified Production Self-Hosted Runner Continuous Deployment (`saas-core-deploy`)**: Provisioned dedicated low-privilege system user (`deploybot`) with `docker` group membership on the production VM host for automated single-VM continuous deployment. Registered repository-scoped GitHub Actions self-hosted runner (`[self-hosted, saas-vm]`) under `deploybot` on `omarmaarouf18/saas-core-deploy`. Authored `.github/workflows/deploy.yml` in `saas-core-deploy` executing `docker compose config --quiet` validation, `docker compose pull`, `docker compose up -d --remove-orphans`, 12-attempt health check against API Gateway `/health`, and container log dump on failure. Updated `docs/DEPLOYMENT.md`, `docs/RUNBOOK.md`, and `docs/CI_CD_AND_HOOKS.md` documenting outbound-only polling security architecture, `docker` socket privilege boundary, manual fallback procedures, and explicit single-VM scope limitation.
* **Verified UpdateJobLocation Entry-Guard Restructuring & TOCTOU Race Condition Fix**: Restructured `UpdateJobLocation` in `user-service` (`services/user-service/internal/handlers/handlers.go`). Moved the rate-limiting / throttle reservation check (`checkLocationThrottleScript` on Redis primary, `tryReserveLocationThrottleInMemory` on nil-Redis fallback) to the immediate entry point of the handler (right after JSON decoding, before database queries or subscription tier checks). Created `tryReserveLocationThrottleInMemory` helper executing check-and-set inside a single `sync.Mutex` lock to guarantee atomic in-flight reservation. Ensured `clearInFlight()` is called on every subsequent validation failure path (invalid token, bad coordinates, job missing, access denied, job inactive, upgrade required) to prevent in-flight lock deadlocks. (Commit `118c40d47a231a558201069bb485f271ab697b4e`).
* **Verified UpdateJobLocation `requireTier` Control-Flow Return Fix & Regression Test**: Resolved a critical control-flow defect in `user-service` (`handlers.go`). When `requireTier` returned a generic internal error (non-`ErrUpgradeRequired`), the handler invoked `clearInFlight()` and `writeJSON(w, 500)` but lacked a `return` statement. This missing return allowed execution to fall through to speed checks and database persistence (`u.store.UpdateJobLocation`), attempting to write a location update and emitting a second `200 OK` header after sending a 500 error. Added missing `return` statement immediately following `writeJSON(500)`. Added `requireTierHook` to `UserService` and built regression test `TestUserServiceHandlers/UpdateJobLocation_RequireTier_InternalError_Regression_Guard` in `handlers_test.go` using a real active job in MongoDB and a `dbWriteCalled` spy, proving no fallthrough or database writes occur on 500 responses. (Commits `89d1f0423315851a25479564996b0620a7ce34f6` and `15b6559b2f1271ff0cb858215a369832a694682c`).
* **Verified Internal mTLS Certificate SAN Fix (`-copy_extensions copy`)**: Documented root cause of `x509: certificate relies on legacy Common Name field, use SANs instead` 502 Bad Gateway errors across `api-gateway` proxied routes. Updated internal mTLS certificate signing instructions in `docs/DEPLOYMENT.md` §5.3 to pass `-copy_extensions copy` during `openssl x509 -req` execution, ensuring Subject Alternative Name (SAN) extensions are copied from CSRs into signed service certificates.
* **Verified Production Release Promotion & End-to-End Automated Deployment (`main` at `15b6559`)**: Promoted `logic-exploitation` to `main` via fast-forward merge at commit `15b6559b2f1271ff0cb858215a369832a694682c`. GitHub Actions `build-and-publish.yml` built microservice images, published them to GHCR, and updated `saas-core-deploy` image tags. The self-hosted runner `saas-vm-runner` on `quickdelivery-vm` automatically picked up the deployment update, pulled containers, executed `docker compose up -d --remove-orphans`, and verified API Gateway `/health` HTTP 200 OK end-to-end.
* **Verified Job Completion Wiring (POST /users/jobs/complete)**: Added `completeJob` method to `EmployeeJobsProvider` (`frontend/lib/providers/employee_jobs_provider.dart`) and wired "Complete Job" action button to `EmployeeJobsScreen` (`frontend/lib/screens/employee_jobs_screen.dart`) rendered exclusively for active jobs. Differentiated COD vs non-COD flows: COD jobs require explicit cash collection confirmation dialog copy and pass `cashCollected: true` (triggering owner wallet fee deduction), whereas non-COD jobs pass `cashCollected: false` with standard completion prompt. Integrated double-submission loading state, inline friendly error handling (`friendlyErrorMessage`), and immediate state updates. Built 5 widget tests (`frontend/test/employee_jobs_screen_test.dart`) covering button visibility, COD vs non-COD confirmation dialog copy, payload parameter distinction, provider execution on confirm, and inline error banner rendering. Verified via `flutter analyze` (0 issues) and `flutter test` (100% pass, 51/51 test cases). (Commit `8f20aa6c78ec97ccf4adb605b57003743579880d`).
* **Verified Job Cancellation Wiring (POST /users/jobs/cancel)**: Added `cancelJob` to `MarketplaceProvider` (`frontend/lib/providers/marketplace_provider.dart`) and `OwnerProvider` (`frontend/lib/providers/owner_provider.dart`). Implemented reusable `CancelJobDialog` (`frontend/lib/widgets/cancel_job_dialog.dart`) requiring a non-empty cancellation reason. Enforced role-specific and state-specific backend authorization rules: Owners can cancel pending and active jobs on `home_screen.dart` (Owner Dashboard); Customers can cancel pending jobs on `job_status_screen.dart`, while active jobs present an "Open a Complaint Ticket" affordance with a `// TODO` note for the upcoming ticket wiring. Built 6 widget tests (`frontend/test/job_cancellation_test.dart`) covering button absence for completed/cancelled jobs, reason field validation, owner active job cancellation, customer active job complaint ticket affordance, customer pending job cancellation, and inline error banner rendering. Verified via `flutter analyze` (0 issues) and `flutter test` (100% pass, 57/57 test cases). (Commit `c9e9c9cdbc8bdce1194098ccb5c4d67a4f457fa1`).
* **Verified Device GPS Location Permission Infrastructure (Part 1 of 2 for Location Update Integration)**: Added `geolocator: ^12.0.0` dependency cleanly to `frontend/pubspec.yaml` (avoiding `web ^1.0.0` conflict with `web_socket_channel`). Configured foreground-only Android permission (`ACCESS_FINE_LOCATION`, `ACCESS_COARSE_LOCATION` in `AndroidManifest.xml`) and iOS permission (`NSLocationWhenInUseUsageDescription` in `Info.plist`). Implemented isolated permission helper utility `requestLocationPermission` (`frontend/lib/core/location_permission.dart`) returning `LocationPermissionResult` (`granted`, `denied`, `deniedForever`, `serviceDisabled`). Created unit test suite (`frontend/test/location_permission_test.dart`) using `MockGeolocatorPlatform` with `MockPlatformInterfaceMixin` covering all 7 permission state combinations. Verified via `flutter analyze` (0 issues) and `flutter test` (100% pass, 64/64 test cases). (Commit `0ee8e7858bcb87dd1e92bbcbc4c121155f673546`).
* **Verified Live Employee Location Ingestion (Part 2 of 2 for POST /users/jobs/location/update)**: Implemented `EmployeeLocationProvider` (`frontend/lib/providers/employee_location_provider.dart`) extending `ChangeNotifier`. Configured `LocationSettings(distanceFilter: 10)` to prevent location update spamming when stationary and enforced a 3.5s minimum interval gate between `POST /users/jobs/location/update` requests (leaving safety margin over the backend's 3-second rate limit floor). Quietly handled non-fatal rate limit and speed responses (`429` / `400 implausible_speed`) without surfacing error state or UI banners, while genuine network/auth/server errors set friendly error banners (`friendlyErrorMessage`). Integrated unobtrusive "Sharing live location" status indicator and inline permission explanation into `employee_jobs_screen.dart`. Managed stream subscription lifecycle cleanly via `stopTracking()` upon job completion, screen deactivation (`deactivate()`), or provider disposal. Built 5 unit/widget tests (`frontend/test/employee_location_tracking_test.dart`) covering interval throttling, 429/implausible_speed error suppression, genuine error handling, stream cancellation, and UI status indicator rendering. Verified via `flutter analyze` (0 issues) and `flutter test` (100% pass, 69/69 test cases). (Commit `6e860d8942e9a9c7c129f648d8d4adabbe7cbdf8`).
* **Verified Public Platform Config Wiring (GET /users/platform/config)**: Added `fetchPlatformConfig()` to `OwnerProvider` (`frontend/lib/providers/owner_provider.dart`), caching `platform_fee_percentage` in memory after first successful fetch and suppressing non-critical fetch errors silently without blocking UI workflows. Rendered user-facing platform fee percentage as a transparency note (`key: Key('platform_fee_percentage_text')`) in `wallet_screen.dart`. Enforced strict security requirement ensuring internal `platform_wallet_id` is never rendered in UI text. Enhanced `PrimaryButton` text truncation handling via `Flexible` with `TextOverflow.ellipsis`. Built unit and widget test suite (`frontend/test/platform_config_test.dart`) verifying fee percentage rendering, security exclusion of internal wallet IDs, and non-blocking failure tolerance. Verified via `flutter analyze` (0 issues) and `flutter test` (100% pass, 72/72 test cases). (Commit `4e3cecc83e909953a7149d536564715d5d492cfe`).
* **Verified Shared Widget Audit & Process Rule**: Audited all 21 `PrimaryButton` call sites across 14 screens in `frontend/lib/screens/` following the shared widget enhancement in commit `4e3cecc83e909953a7149d536564715d5d492cfe`. Confirmed zero visual text truncations or regressions at standard 360dp mobile screen width. Documented the explicit shared widget process rule in `CLAUDE.md` under `## Auto-commit policy`: any change touching a shared widget (`frontend/lib/widgets/`) as a side effect must be called out separately in commit messages and task reports.
* **Established Architectural Scope Boundary for Support Agent Console (ADR-0013)**: Authored [ADR-0013](docs/adr/0013-support-agent-console-as-separate-client-application.md) establishing that `POST /chat/tickets/resolve` and agent token authentication (`GetAgentByToken`) are intentionally out of scope for the consumer mobile app (`frontend/`), reserved for a future, standalone Support Agent Console application (`omarmaarouf18/support-agent-console`). Cross-referenced the planned future repository boundary in `docs/REPOSITORY_MAP.md`. (Commit `856bba37c92dc605e53559c4d6465848c2bff1a8`).
* **Documented 11-Endpoint Integration Audit State in STATUS.md**: Documented Phase 13 in `docs/frontend/STATUS.md` recording 4 completed endpoints (`POST /users/jobs/complete` via `8f20aa6`/`73dc64c`, `POST /users/jobs/cancel` via `c9e9c9c`, `POST /users/jobs/location/update` via `0ee8e78`/`6e860d8`, `GET /users/platform/config` via `4e3cecc`), 1 deferred endpoint (`GET /users/jobs/mine`), 1 scope-excluded endpoint (`POST /chat/tickets/resolve` per ADR-0013), 6 remaining planned endpoints (`POST /chat/tickets` next), shared widget policy commit `c19fb0b`, and explicit post-wiring UI/UX design pass sequencing. (Commit `31164dc8c10c929c5261f110d96f8d5870124363`).
* **Verified Customer Job History Wiring (GET /users/jobs/mine)**: Added `fetchCustomerJobs` to `MarketplaceProvider` (`frontend/lib/providers/marketplace_provider.dart`), querying `/users/jobs/mine` using IDOR-safe Bearer token authentication (without explicit `user_id` query parameters). Built `CustomerJobsScreen` (`frontend/lib/screens/customer_jobs_screen.dart`, "My Orders") displaying customer order cards with status badges, payment methods, prices, cancellation reasons, navigation to `JobStatusScreen`, and pull-to-refresh. Wired navigation action button (`key: Key('my_orders_button')`) into `CustomerMarketplaceScreen`. Built 6 unit/widget tests (`frontend/test/customer_jobs_screen_test.dart`) covering loading, empty state, job card rendering, error banners, job status navigation, and marketplace screen navigation. Verified via `flutter analyze` (0 issues) and `flutter test` (100% pass, 78/78 test cases). (Commit `61bdf2703095bae8f63ddcd9757ff5e419d09170`).
* **Verified Support Complaint Ticket Creation Wiring (POST /chat/tickets)**: Added `createTicket` method to `ChatProvider` (`frontend/lib/providers/chat_provider.dart`) querying `POST /chat/tickets` using standard user Bearer JWT authentication. Created `CreateTicketDialog` (`frontend/lib/widgets/create_ticket_dialog.dart`) featuring context ID reference display, subject and issue details input fields with validation, submit loading state, and inline error banner. Resolved existing `TODO` in `JobStatusScreen` (`frontend/lib/screens/job_status_screen.dart`), wiring "Open a Complaint Ticket" button (`key: Key('open_complaint_ticket_button')`) directly to `CreateTicketDialog`. Built 5 widget tests (`frontend/test/create_ticket_test.dart`). Verified via `flutter analyze` (0 issues) and `flutter test` (100% pass, 83/83 test cases). (Commit `8db82ca238cad4d892725e19157a837af9b6ecf0`).
* **Verified Owner Employee List Wiring (GET /auth/employees)**: Added `fetchEmployees` method to `OwnerProvider` (`frontend/lib/providers/owner_provider.dart`) querying `GET /auth/employees` using owner Bearer JWT authentication. Integrated the Registered Employees roster (`key: Key('registered_employees_card')`) into `EmployeeScreen` (`frontend/lib/screens/employee_screen.dart`, "Manage Workers" tab) with status badges (`StatusBadge`), pull-to-refresh, empty states (`ThemedEmptyState`), loading indicators, error banners, and auto-refresh on employee registration and status toggle. Built 6 unit/widget tests (`frontend/test/owner_employees_test.dart`). Verified via `flutter analyze` (0 issues) and `flutter test` (100% pass, 89/89 test cases). (Commit `c413a4ccebda889011696502ad8b063c0ddf86e5`).
* **Reaffirmed KYB/KYE Reviewer Scope Boundary (ADR-0013)**: **[SCOPE-EXCLUDED PER ADR-0013]** Enforced strict ADR-0013 security boundary. KYC/KYB review queues, administrative review actions (`POST /auth/kyb-kye/review`), and document viewing (`GET /auth/documents/view`) belong to the standalone Support Agent Console rather than the consumer mobile binary (`frontend/`). A temporary mistaken restoration in commit `ab9073a` was re-reverted following explicit project owner reconfirmation. All reviewer screens (`kyb_kye_review_screen.dart`), dialogs (`document_viewer_dialog.dart`), navigation call sites in `home_screen.dart`, and reviewer provider methods are removed from the consumer binary to prevent admin workflow leakage.
* **Verified Frontend UX/UI Contrast Audit, Password Toggle & Consolidated Forgot Password (Phase 14)**: Audited `AppColors` and screen color usage for WCAG AA contrast compliance ($\ge 4.5:1$ body text, $\ge 3:1$ UI components), updating `AppColors.success` (`0xFF15803D`, 5.02:1 on white), `AppColors.warning` (`0xFFB45309`, 5.02:1 on white), `AppColors.outline` (`0xFF57585E`, 5.61:1 on scaffold), `AppColors.outlineVariant` (`0xFF8E8F95`, 3.34:1 for UI borders), `AppColors.surfaceDim` (`0xFFA6A7AD`), `home_screen.dart` blue status (`0xFF1D4ED8`, 7.24:1), and `otp_screen.dart` dev icon color (`0xFFB45309`). Extended `ThemedTextField` (`frontend/lib/widgets/themed_text_field.dart`) to support `isPasswordField: true` and `obscureText` toggling with an interactive show/hide eye icon button (`key: Key('password_toggle_button')`), applied consistently across `LoginScreen`, `SignupScreen`, `ForgotPasswordScreen`, and `EmployeeScreen`. Consolidated forgot-password flow in `ForgotPasswordScreen` (`frontend/lib/screens/forgot_password_screen.dart`) so Email, 6-Digit OTP, New Password, and Confirm Password fields are present on the SAME screen with a single "RESET PASSWORD" submit button invoking `POST /auth/reset-password`. Updated `ResetPasswordScreen` to delegate to `ForgotPasswordScreen`. Built 4 widget tests in `frontend/test/password_toggle_test.dart` and `frontend/test/forgot_password_screen_test.dart`. Verified via `flutter analyze` (0 issues) and `flutter test` (100% pass, 106/106 test cases).
* **Verified Responsive Location Picker Dialog & Dropdown Sizing Mobile Overflow Fix**: Fixed hardcoded container width/height (`width: 500, height: 550`) in `_openLocationPickerDialog` inside `customer_marketplace_screen.dart`. Replaced fixed dimensions with dynamic `MediaQuery` viewport calculations (`dialogWidth = math.min(500.0, screenSize.width * 0.9)`, `dialogHeight = math.min(550.0, screenSize.height * 0.8)`). Added explicit 360x800 mobile viewport regression tests in `customer_marketplace_screen_test.dart` and 330x480 narrow container tests in `location_picker_map_test.dart`. Audited all other dialogs (`ConfirmActionDialog`, `CancelJobDialog`, `CreateTicketDialog`) and confirmed no other hardcoded fixed-size dialog overflows exist. Verified via `flutter analyze` (0 issues), `flutter test` (119/119 full suite pass), and `make docs-check`.
* **Verified Service Model Owner Configuration Fields & UpdateService Handler (ADR-0014)**: Extended `Service` model (`services/user-service/internal/models/models.go`) with `PhotoURL`, `Address`, `WorkingHours`, and `CoverageRadiusKM`. Extended `CreateServiceRequest` and added `UpdateServiceRequest` DTOs. Built `UpdateService` handler in `user-service` (`services/user-service/internal/handlers/handlers.go`) and MongoDB store (`UpdateService`), supporting `PUT /users/services` and `POST /users/services/update` with KYC approval checks and IDOR tenant ownership protection. Built unit test suite (`services/user-service/internal/handlers/service_owner_config_test.go`) verifying service creation with new fields, updating existing service fields, backward compatibility for legacy payloads omitting new fields, and IDOR protection. Regenerated `docs/APPLICATION_MAP.md` via `make docs`. Verified via `gofmt -w .`, `go build ./...`, `go vet ./...`, `go test ./...`, and `make docs-check`. (Commit `405d0de263d2f8fd6e554800caa565b7cf311238`).
* **Verified Phase 5 Performance Perception & Skeleton Loading States**: Implemented `SkeletonLoader` reusable shimmer animation primitive in `frontend/lib/widgets/skeleton_loader.dart` built with design system tokens (`AppColors`, `AppRadius`, `AppMotion`), alongside 4 exact card geometry skeleton loaders (`MarketplaceCardSkeleton`, `HomeDashboardSkeleton`, `EmployeeJobCardSkeleton`, `WalletScreenSkeleton`) mirroring real content cards across `customer_marketplace_screen.dart`, `home_screen.dart`, `employee_jobs_screen.dart`, and `wallet_screen.dart`. Wired smooth `AnimatedSwitcher` (`AppMotion.durationMedium`, `AppMotion.curveStateChange`) cross-fade transitions during initial async data loading while preserving pull-to-refresh `RefreshIndicator` for subsequent updates. Created unit & state transition widget test suite (`frontend/test/skeleton_loader_test.dart` 9/9 pass, full suite 186/186 pass). Verified via `flutter analyze` (0 issues), `flutter test` (100% pass, 186/186 pass), and live backend health check against `https://api.logiclinkeg.tech/health` (HTTP 200 OK). Updated `docs/frontend/UI_UX_ENTERPRISE_ROADMAP.md`, `docs/frontend/DESIGN_SYSTEM.md`, and `docs/frontend/STATUS.md`.
* **Verified Self-Service Profile Update & Frequent Addresses (PATCH /auth/user)**: Extended `User` model (`services/auth-service/internal/models/models.go`) with `FrequentAddresses []string` and added `UpdateProfileRequest` DTO. Built `PATCH /auth/user` handler in `auth-service` (`services/auth-service/internal/handlers/auth.go`) allowing authenticated users to update their `username`, `phone`, and `frequent_addresses` (capped at max 10 entries). Enforced strict IDOR protection by deriving target user ID solely from validated JWT claims (rejecting mismatched payload user IDs with 403 Forbidden) and stripping/ignoring sensitive field smuggling (`email`, `password`, `role`, `kyc_status`). Created test suite in `services/auth-service/internal/handlers/user_profile_test.go`. Regenerated `docs/APPLICATION_MAP.md` via `make docs`. Verified via `gofmt -w .`, `go build ./...`, `go vet ./...`, `go test ./...`, `gosec`, and `make docs-check`. (Commit `ad55855bf797c7c4acbf840a2a42f8a0c8483ca2`).
* **Verified Owner Configuration Screen & PUT /users/services Frontend Integration (ADR-0014)**: Built real `OwnerConfigurationScreen` (`frontend/lib/screens/owner_configuration_screen.dart`) replacing placeholder in `SettingsScreen`. Pre-populates business name, category, address, working hours, coverage radius, base price, price per KM, and photo URL. Added `updateOwnerServiceConfig` method to `OwnerProvider` calling `PUT /users/services` via `ApiClient`. Uses `ThemedErrorBanner` for error feedback and SnackBar for success. Verified via `dart format`, `flutter analyze` (0 issues), `flutter test` (145/145 full suite pass), and `make docs-check`. (Commit `8f00e83fb70604c7d9c2bd15382e4323f86dc284`).
* **Verified My Account Screen & PATCH /auth/user Frontend Integration (ADR-0014)**: Built real `MyAccountScreen` (`frontend/lib/screens/my_account_screen.dart`) replacing placeholder in `SettingsScreen`. Pre-populates username, email (display-only read-only field), phone, and frequent addresses (interactive list editor with 10-item cap validation). Added `updateOwnProfile` method to `AuthProvider` calling `PATCH /auth/user` via `ApiClient.patch`. Uses `ThemedErrorBanner` for error feedback and SnackBar for success. Verified via `dart format`, `flutter analyze` (0 issues), `flutter test` (150/150 full suite pass), and `make docs-check`. (Commit `fbbaa3ed9eb52db5da97034085982cee9e345356`).
* **Verified Customer Service Settings Ticket Wiring (ADR-0014)**: Wired Customer Service row in `SettingsScreen` (`frontend/lib/screens/settings_screen.dart`) to open `CreateTicketDialog` (`frontend/lib/widgets/create_ticket_dialog.dart`). Made `contextId` optional in `CreateTicketDialog` to support general support tickets without context IDs while maintaining backward compatibility for job-specific tickets in `JobStatusScreen`. Verified via `dart format`, `flutter analyze` (0 issues), `flutter test` (150/150 full suite pass), and `make docs-check`. (Commit `0c3977aac8d665f55c16422737c00f30f9d6c2a7`).
* **External Security & Architecture Findings Remediation (Resolution Summary)**: Addressed external audit report (`security-and-architecture-findings.md` on branch `logic-exploitation` @ `30ae8ce`). 9 of 10 findings resolved (Findings #1–#8 code-remediated with regression test suites; Finding #9 documented in `docs/REPOSITORY_MAP.md` as an accepted monorepo relative-path constraint); 1 finding (Finding #10: Team-Scale Readiness) explicitly marked **Open / Deferred** for future governance work. Full commit SHA matrix and verification details are recorded in [docs/changelog/security-fixes.md](file:///mnt/windows_data/CS%20tools/Antigravity/SaaS%20prototype/docs/changelog/security-fixes.md).
* **Verified Strict CD Pre-Flight Validation, Structural Sync & Rollback (ADR-0015)**: Added `--check-env` pre-flight validation flag across all 5 Go microservices (`api-gateway`, `auth-service`, `chat-service`, `notification-service`, `user-service`) running `config.Load()` and exiting 0/1 without starting the HTTP server or requiring DB/TLS cert files. Created production reference files in `infrastructure/deploy/` (`docker-compose.prod.yml` containing canonical environment variable blocks and `deploy.yml` featuring pre-flight validation, all-5-service health checks, health-gated rollback to prior `docker-compose.yml`, and `.env` symlink unification). Rewrote `update-deployment-repo` job in `.github/workflows/build-and-publish.yml` to execute full structural sync from `infrastructure/deploy/` to `omarmaarouf18/saas-core-deploy` (eliminating env block drift). Authored [ADR-0015](docs/adr/0015-strict-cd-preflight-validation.md) documenting root causes of 3 production incidents. Verified via unit test suites, YAML validation, `make ci`, and cross-repository sync. (Commit `8f768a08059af871c2885ce143c7520685a84741`).
* **Verified Production Dockerfile Curl Installation & Health Check Fallback**: Updated `RUN apk --no-cache add ca-certificates` to `RUN apk --no-cache add ca-certificates curl` across all 5 microservices' production Dockerfiles (`api-gateway`, `auth-service`, `chat-service`, `notification-service`, `user-service`). Updated `infrastructure/deploy/deploy.yml` post-deploy health check step to support both `curl` and `wget` HTTP status verification against `https://localhost:${PORT}/health`. (Commit `4a1bd377352e426352bd704b4a8795530b589b7b`).
* **Verified Microservices mTLS Health Check Client Certificate Integration**: Updated `infrastructure/deploy/deploy.yml` post-deploy health check step to pass mounted mTLS client certificates (`--cert /app/certs/${SVC}.crt --key /app/certs/${SVC}.key`) on initial HTTPS health verification against `https://localhost:${PORT}/health`, eliminating HTTP 400 errors from mandatory mutual TLS (`tls.RequireAndVerifyClientCert`). (Commit `8e552163daa90cccd34eb849fa3188f311a40285`).
* **Verified Systemic Shared Widget Tap-Debounce Protection**: Converted `PrimaryButton` and `SecondaryButton` (`frontend/lib/widgets/`) to `StatefulWidget` incorporating a built-in 600ms tap-debounce guard alongside `isLoading` state handling to eliminate double-tap 429 rate-limiting races across all screens without modifying call sites. Softened fallback 429 rate-limit error message (`ErrorMessages.tooManyRequests`) in `frontend/lib/core/error_messages.dart` to `"That went through already — no need to tap again."`. Updated `docs/frontend/ARCHITECTURE.md`. Verified via `dart format`, `flutter analyze` (0 issues), and `flutter test` (159/159 full suite pass).
* **Verified Rate-Limiter Classification & Separation**: Created dedicated 30 req/min rate limiters for WebSocket connections (`chat:ws` in `services/chat-service/internal/handlers/chat.go`) and read-heavy user endpoints (`user:read` in `services/user-service/internal/handlers/handlers.go` for `GetOwnerJobs`, `GetCustomerJobs`, `GetLedger` at both call sites, `GetRatings`, `GetReconciliationQueue`), while preserving 5 req/min write limits on `WalletDeposit`, `RateJob`, `CancelJob`, `ProposePrice`, `RespondPrice`, `TrackJob`, `ResolveReconciliation`, and `HandleCreateTicket`. Verified via unit test suites (`TestHandleWebSocket_RateLimiting`, `TestGetJobsByOwner`, `TestGetJobsByCustomer`, `TestRateJob_RateLimiting`, `TestGetLedger_RateLimiting`, `TestGetReconciliationQueue`), `gofmt`, `go build`, and `go vet`. (Commit `131a8a8133b8a4b7f58ada9a92cb0ed0cc0e6e96`).
* **Verified Business Logic & Escrow Audit Report (Finding 1 & Finding 2)**: Produced formal business logic audit report `docs/BUSINESS_LOGIC_AUDIT.md` documenting confirmed findings in `services/user-service` job lifecycle and escrow financial logic. Detailed Critical Finding 1 (COD job cancel/complete race condition in `store.CancelJob`) and Medium Finding 2 (`AgreedPrice` omitted in `CancelJob` refund calculation, trapping owner funds in escrow). Documented 4 verified-sound security controls (atomic escrow release double-complete protection, backend KYC enforcement, tenant scope isolation, and employee IDOR protection) and explicit scope limits. Indexed in `README.md`. (Commit `7ed7579e91d299571706b54c067604462180ea31`).
* **Verified Settings KYC Role Gating, Redundant Refresh Button Cleanup, and Owner Arabic Localization (Bugs 1-3)**: Gated identity verification (KYC/KYB) in `settings_screen.dart` with `showKycRow = user != null && (user.role == 'owner' || user.role == 'employee') && user.kycStatus != 'approved'`, restricting it from customer-role accounts. Removed redundant refresh `IconButton` widgets from AppBar `actions` across 5 screens (`employee_jobs_screen.dart`, `home_screen.dart`, `customer_jobs_screen.dart`, `kyb_kye_review_screen.dart`, `owner_reconciliation_queue_screen.dart`) while retaining `RefreshIndicator` pull-to-refresh gestures. Localized ~90 hardcoded strings across owner and employee screens in `app_en.arb` and `app_ar.arb` (Egyptian colloquial Arabic). Added widget tests in `settings_screen_test.dart`, `employee_jobs_screen_audit_test.dart`, and `owner_home_screen_test.dart`. Verified via `flutter analyze` (0 issues), `flutter test` (167/167 pass 100%), live backend health check (`https://api.logiclinkeg.tech/health` -> `{"status":"ok"}`), and `.githooks/pre-push` gate passing cleanly.
* **Verified ADR-0017 Zero-Commission Subscription Revenue Model & Business Logic Audit Fixes**: Remediated Critical Finding 1 (added atomic status-guard CAS filter `status: {$in: [...]}` in `store.CancelJob` preventing COD cancel/complete race conditions) and Medium Finding 2 (fixed `CancelJob` refund math to check `job.AgreedPrice` before falling back to `LockedEscrowAmount`). Implemented ADR-0017 end-to-end: zero-commission COD cash collection logging with 0 wallet/escrow balance changes, 100% electronic credits model on escrow release, owner payout request data model (`PayoutRequest`) and endpoints (`POST /users/wallet/payout/request`, `GET /users/wallet/payout/requests`), and `ELECTRONIC_PAYMENTS_ENABLED` feature flag (default `false`) gating non-COD payments while leaving electronic payment path fully ready for future gateway integration. Updated ADR-0017 status to Accepted, updated `docs/DEPLOYMENT.md` with manual `platform_config` migration command, updated `docs/APPLICATION_MAP.md`, and updated unit test suites. Verified via `gofmt`, `go build`, `go vet`, and `go test -v ./...` (100% pass).
* **Verified Comprehensive Flutter Frontend Localization Pass (All Widget Properties & Natural Egyptian-Market Arabic)**: Audited all string-bearing Flutter widget properties (`title:`, `subtitle:`, `message:`, `label:`, `hintText:`, `labelText:`, `tooltip:`, `content:`, `errorText:`) across all files in `frontend/lib/screens` and `frontend/lib/widgets`, identifying 87 hardcoded strings missed by prior narrow single-quoted `Text('...')` regex. Refactored `status_badge.dart` static method `StatusBadgeConfig.getConfig(BuildContext context, String status)` to require `BuildContext` context, removed compile-time `const` constructors on localized runtime strings, and wired all 10 status labels (`statusCompleted`, `statusActive`, `statusAwaitingPrice`, `statusPending`, `statusCancelled`, `statusPendingApproval`, `statusApproved`, `statusRejected`, `statusUnverified`, `statusReconciliationRequired`, `statusUnknown`) via `context.l10n`. Extracted all strings into `app_en.arb` and `app_ar.arb` with natural Egyptian delivery-app market terminology (e.g. "الباقة الأساسية المجانية" for Free Basic Plan, "الباقة الاحترافية المدفوعة" for Professional Paid Plan, "سبب الإلغاء *" for Cancellation Reason *, etc.). Updated `docs/frontend/LOCALIZATION_AUDIT.md` to document the static/const method inspection protocol. Verified via `dart format` (clean), `flutter analyze` (0 issues), `flutter test` (167/167 pass 100%), and targeted grep proving zero hardcoded status string matches remain in `status_badge.dart`.
* **Verified ADR-0017 Migration Script Database Name Remediation (Finding #8 Alignment)**: Corrected `docs/DEPLOYMENT.md` §11.1 migration script and verification commands to target `user_db` (`${USER_MONGO_DATABASE:-user_db}`) rather than the obsolete shared database name `saas_platform`, aligning with Finding #8's database-per-service separation. Audited and updated `README.md` manual database ops procedures (pointing `users` collection to `auth_db` and `subscriptions` to `user_db`) and `mongodump` backup commands. Executed the corrected migration against `user_db` on MongoDB, verifying `user_db.platform_config` now returns `{ _id: "global", platform_fee_percentage: 0 }`.
* **Verified Design Token System Migration & Debt Cleanup (UI/UX Roadmap Phase 1)**: Replaced all 55 catalogued instances of hardcoded values across 16 files with canonical `AppColors`, `AppSpacing`, `AppRadius`, and `AppTypography` design tokens (`chat_screen.dart`, `customer_home_screen.dart`, `customer_job_map_screen.dart`, `customer_marketplace_screen.dart`, `employee_jobs_screen.dart`, `home_screen.dart`, `job_status_screen.dart`, `kyb_kye_review_screen.dart`, `notifications_screen.dart`, `owner_fleet_map_screen.dart`, `owner_history_screen.dart`, `owner_reconciliation_queue_screen.dart`, `rating_screen.dart`, `service_screen.dart`, `subscription_screen.dart`, `wallet_screen.dart`). Extended `theme.dart` with clean intermediate tokens (`AppSpacing.xxs/baseSm/xxl/xxxl`, `AppRadius.xxs/smMd/lgXl`). Re-ran `scratch/audit_debt.py` confirming **0** remaining debt findings. Updated `docs/frontend/DESIGN_SYSTEM.md`, `docs/frontend/UI_UX_ENTERPRISE_ROADMAP.md`, and `docs/frontend/STATUS.md`. Verified via `flutter analyze` (0 issues), `flutter test` (100% pass, 171/171 tests), and RTL `ar_EG` layout validation.
* **Verified Visual Hierarchy & Card Presentation Overhaul (UI/UX Roadmap Phase 4)**: Extended `ThemedCard` with explicit `elevation` parameter support. Standardized static/resting cards to `AppElevation.shadowLevel1List` (control panel, empty containers, job details, worker info, audit logs) and interactive/tappable cards to `AppElevation.shadowLevel2List` (marketplace service booking cards, price counter-offer card, worker registration form, worker status form, employee active job cards) across 5 target screens (`customer_marketplace_screen.dart`, `job_status_screen.dart`, `wallet_screen.dart`, `employee_screen.dart`, `employee_jobs_screen.dart`). Updated `docs/frontend/DESIGN_SYSTEM.md`, `docs/frontend/UI_UX_ENTERPRISE_ROADMAP.md`, and `docs/frontend/STATUS.md`. Verified via `scratch/audit_hierarchy.py` (0 remaining hierarchy findings), `flutter analyze` (0 issues), and `flutter test` (100% pass, 177/177 tests).
* **Verified Button & Badge Type Scale Correction**: Changed `PrimaryButton` and `SecondaryButton` label text styles from `AppTypography.titleMd` (18px) to `AppTypography.bodyLg` (16px bold) in `frontend/lib/widgets/primary_button.dart` and `secondary_button.dart`. Audited all `StatusBadge` call sites across screens (confirming `compact: true` is passed at tight call sites `employee_screen.dart`, `owner_reconciliation_queue_screen.dart`, and `wallet_screen.dart`, while standard `labelLg` 12px is used elsewhere). Added typography scale test assertions to `test/shared_widgets_test.dart`. Updated `docs/frontend/ARCHITECTURE.md` and `docs/frontend/DESIGN_SYSTEM.md`. Verified via `flutter analyze` (0 issues) and `flutter test` (100% pass, 192/192 tests).
* **Verified Real Email-Change Flow (POST /auth/email-change/request & POST /auth/email-change/confirm)**: Built a secure two-step identity change flow in `auth-service`. Endpoint `POST /auth/email-change/request` validates `new_email` format and case-insensitive uniqueness against existing users, rate-limits per IP and user ID, generates a 6-digit OTP, encrypts it via AES-256-GCM, stores a pending record in `pending_email_changes` (with 5-minute expiry), and dispatches the OTP code to the new email. Endpoint `POST /auth/email-change/confirm` validates the OTP via constant-time comparison, updates the user's email in MongoDB, consumes the pending record, and returns a fresh JWT token with the updated email claim. Added backend unit test suite in `services/auth-service/internal/handlers/email_change_test.go` covering happy path, duplicate/taken email conflict (409), current email request (400), wrong/expired OTP (401), rate-limiting (429), and unauthenticated requests (401). Extended `AuthProvider` (`frontend/lib/providers/auth_provider.dart`) with `requestEmailChange` and `confirmEmailChange` methods. Updated `MyAccountScreen` (`frontend/lib/screens/my_account_screen.dart`), replacing static "cannot be changed" text with a "Change Email" action button opening a two-step `EmailChangeDialog` (with dev_otp auto-fill support in local mode). Updated `myAccountEmailNote` and added localized strings in `app_en.arb` and `app_ar.arb`. Added widget tests in `frontend/test/my_account_screen_test.dart`. Updated `docs/APPLICATION_MAP.md` and `docs/changelog/new-features.md`. Verified via `go test ./...` in `auth-service` (100% pass), `flutter analyze` (0 issues), `flutter test` (100% pass, 194/194 tests), and `.githooks/pre-push` gate exit code 0.
* **Verified Atomic Single-Use Email Change OTP Consumption (`GetAndConsumePendingEmailChange`)**: Replaced non-atomic `FindOne` + separate `DeleteOne` sequence in `GetAndConsumePendingEmailChange` (`services/auth-service/internal/store/mongodb.go`) with a single atomic `FindOneAndDelete` MongoDB operation. The prior implementation suffered from a TOCTOU race condition where concurrent requests with the same valid OTP could both pass `FindOne` before either `DeleteOne` completed. Enforced single-attempt OTP consumption semantics. Added concurrency regression test `TestEmailChange_ConcurrentConfirmRace` in `services/auth-service/internal/handlers/email_change_test.go` firing 2 concurrent goroutines confirming with the same valid OTP. Verified via `go test -v -race -count=20 ./internal/handlers -run TestEmailChange_ConcurrentConfirmRace` (20/20 pass), `go test ./...` in `auth-service` (100% pass), `make docs-check`, and `.githooks/pre-push` gate exit code 0. (Commit `488723bda77c89b4a4387d8170cbab5c456b53f5`).
* **Verified Business Location Map Selection in Owner Configuration & Services Directory Distance Filter Fix (BUG 1 & BUG 2)**: Replaced hardcoded Cairo coordinates (`latitude: 30.0444, longitude: 31.2357`) in `OwnerConfigurationScreen` (`frontend/lib/screens/owner_configuration_screen.dart`) with interactive `LocationPickerMap` selection per ADR-0014. Extended `updateOwnerServiceConfig` in `OwnerProvider` (`frontend/lib/providers/owner_provider.dart`) to pass `latitude` and `longitude` in `PUT /users/services`. Pre-populates the map picker with the service's stored coordinates if editing, while falling back to Cairo as map center for new services (requiring pin confirmation before submission). Fixed BUG 2 in `CustomerMarketplaceScreen` (`frontend/lib/screens/customer_marketplace_screen.dart`) where `_nearBy` was hardcoded to `true`, hiding businesses outside Cairo. Made `_nearBy` a mutable state variable defaulting to `false` (returning all directory services across all locations when reached via the Services tab per ADR-0014), and added a visible `Nearby Only` toggle switch control allowing customers to optionally filter by distance. Added localization keys (`ownerConfigLocationLabel`, `ownerConfigLocationReq`, `customerMarketplaceFilterNearby`) in `app_en.arb` and `app_ar.arb`. Updated widget tests in `frontend/test/owner_configuration_screen_test.dart` and `frontend/test/customer_marketplace_screen_test.dart`. Verified via `dart format`, `flutter analyze` (0 issues), `flutter test` (100% pass, 196/196 tests), and `make docs-check`.
* **Verified KYC/KYE Document Upload Screen Refactor (`kyc_document_upload_screen.dart`)**: Decomposed monolithic 685-line `KycDocumentUploadScreen` (`frontend/lib/screens/kyc_document_upload_screen.dart`) into named section helper widgets (`_buildStatusBanner`, `_buildRejectionReasonBanner`, `_buildApprovedLockedBanner`, `_buildDocumentSlotCard`), adopted design-system shared widgets (`ThemedCard`, `ThemedSectionHeader`, `StatusBadge`, `PrimaryButton`, `SecondaryButton`, `ThemedErrorBanner`), consolidated source picker modal bottom sheet (`_showSourcePickerBottomSheet`), added `RefreshIndicator` pull-to-refresh, and integrated Phase 2/5 motion transitions (`AnimatedSwitcher`). Verified via `dart format .`, `flutter analyze` (0 issues), `flutter test test/kyc_document_upload_screen_test.dart` (5/5 pass), full suite `flutter test` (196/196 pass), and `make docs-check`.
* **Verified Standardized Graceful Shutdown Protocol Across All 5 Microservices**: Extended graceful shutdown handling to `api-gateway`, `chat-service`, and `notification-service`, unifying all 5 microservices on the canonical pattern from `auth-service` and `user-service`. On receiving `SIGINT` or `SIGTERM`, each service logs `[<SERVICE_TAG>] Shutting down...` and executes `http.Server.Shutdown()` with a 10-second context timeout (`context.WithTimeout(10*time.Second)`). Enhanced `Hub.Close()` in `chat-service` to iterate over active clients and close outbound `Send` channels, triggering `writePump` to transmit normal WebSocket close frames (`websocket.CloseMessage`) before closing socket connections. Enhanced `SSEHub.Close()` in `notification-service` to close active `Send` channels across `SSEHub.clients`, ensuring streaming handler loops terminate cleanly alongside HTTP context cancellation (`r.Context()`). Verified via `go build ./...`, `go vet ./...`, 100% test pass across all Go test suites, empirical SIGTERM test logs (`[GATEWAY] Shutting down...`, `[CHAT] Shutting down...`, `[NOTIF] Shutting down...`), and `make ci`.
* **[CORRECTED 2026-08-22 — DOC-DRIFT] Chat Service CAS Audit (Retry-Loop "Removal" Claim Was False)**: The original entry below claimed the 3-attempt retry loop with `10ms` sleep in `TestComplaintRoutingConcurrency` (`services/chat-service/internal/handlers/chat_test.go`) was removed. Direct code verification against the current branch shows that claim was **false**: the retry loop is still present at `chat_test.go:390-398` (`for attempt := 0; attempt < 3; attempt++` around `CreateTicketAndAssign` with `time.Sleep(10 * time.Millisecond)`). What commit `94fe6e4...` actually landed: reverting the redundant `SetMaxPoolSize(100)` in `NewMongoDB` (verified absent from `services/chat-service/internal/store/mongodb.go` today) and adding the atomic-CAS audit comment on `CreateTicketAndAssign`'s `FindOneAndUpdate` (verified present at `store/mongodb.go:272`). The retry-loop removal itself never landed or was lost. The loop's presence is now documented as fact; whether it should be removed (it can mask transient store errors inside a concurrency test whose routing is CAS-guaranteed) is left as a flagged follow-up rather than silently changed in a doc-correction pass. Original (partially incorrect) entry: audit of commit `1e49e77...` attributed earlier test failures to unauthenticated connection ordering in `connectTestMongoDB`, already resolved by commit `cc54b63...`. Verified via `go vet ./...` and `go test -v -race -count=20 ./...` at the time. (Commit `94fe6e43987396fb609c62185bb254db953a2a3f`; correction verified against working tree on 2026-08-22).
* **Verified Production Container Non-Root Security Hardening & HEALTHCHECK Directives**: Hardened all 5 production microservice Dockerfiles (`api-gateway`, `auth-service`, `chat-service`, `notification-service`, `user-service`) by adding a non-root system user `appuser` (UID 100) / `appgroup` (GID 101) and native Docker `HEALTHCHECK` directives (`--interval=10s --timeout=5s --start-period=5s --retries=3`) pointing to each service's `/health` endpoint over HTTPS/mTLS. In `auth-service/Dockerfile`, created `/app/data` with explicit `appuser:appgroup` ownership to eliminate permission denied errors on local document storage directory creation. Confirmed all 5 services bind unprivileged ports (`8080`, `3002`, `3001`, `3004`, `3003`). Verified via local `docker build --target prod`, `docker run` on shared network, `docker ps` (5/5 containers `healthy`), `docker exec id` (`uid=100(appuser)`), `go test ./...` across all 5 Go modules (100% pass), and `make docs-check`. (Commit `a7e7ecfb2fcf634a933fc1960f9baf8eddb85cf3`).
* **Verified Strict CD Rollback Hardening & Post-Rollback Health Verification**: Fixed silent rollback failure vulnerabilities in `infrastructure/deploy/deploy.yml`'s "Rollback on Health Failure" step by adding `set -euo pipefail`, checking `git show HEAD~1:docker-compose.yml` (exiting 1 on failure), checking `docker compose up` status (exiting 1 on failure with `::error::ROLLBACK COMMAND ITSELF FAILED...`), and re-running `verify_service_health` against restored containers. If post-rollback health checks fail, logs `::error::CRITICAL: ROLLBACK FAILED HEALTH CHECK TOO...` and exits 1. Updated `docs/adr/0015-strict-cd-preflight-validation.md` with post-audit addendum and `docs/changelog/infrastructure.md`. Verified via Python YAML syntax validation, shell flow trace, and `.githooks/pre-push` gate. (Commit `b2b2a380b977582a11989e8e767e597f13b18087`).
* **Verified Independent Per-Endpoint Rate Limiters & GetLedger Dual-Layer Restoration**: Refactored `user-service` (`services/user-service/internal/handlers/handlers.go`) to replace shared `readLimiter` (`user:read`) with dedicated independent rate limiters per read endpoint: `ownerJobsLimiter` (`user:owner_jobs`, 60 req/min), `customerJobsLimiter` (`user:customer_jobs`, 60 req/min), `ledgerLimiter` (`user:ledger`, 60 req/min), `ledgerIPLimiter` (`user:ledger_ip`, 60 req/min), `ratingsLimiter` (`user:ratings`, 30 req/min), and `reconciliationLimiter` (`user:reconciliation`, 30 req/min). Restored `GetLedger` dual-layer rate limiting: IP pre-check (`ledgerIPLimiter`, 60 req/min) runs before token resolution to guard against unauthenticated scraping, and tenant check (`ledgerLimiter`, 60 req/min) runs after resolution. Both layers operate on completely separate, independent budgets. Audited `chat-service`, `notification-service`, `auth-service`, and `api-gateway`, confirming no shared-bucket patterns exist elsewhere. Added automated test suite `services/user-service/internal/handlers/read_rate_limiters_test.go` (`TestGetLedger_IPRateLimitCheck`, `TestGetLedger_TenantRateLimitCheck`, and `TestReadRateLimiters_Independence`). Updated ADR-0016 addendum in `docs/adr/0016-tiered-rate-limits-for-ux-over-uniform-security-floor.md` and `docs/changelog/bug-fixes.md`. Verified via `go build ./...`, `go vet ./...`, `go test ./...` (100% pass across all modules), and `.githooks/pre-push` gate.
* **Verified Application-Level AES-256-GCM Encryption at Rest for KYB/KYE Identity Documents**: Hardened local disk storage in `auth-service` (`services/auth-service/internal/storage/storage.go`) by adding AES-256-GCM encryption at rest for all uploaded KYB/KYE identity documents. `LocalStorage.Upload()` generates a 12-byte random nonce per file and seals bytes (`cipher.AEAD.Seal`) before writing to disk. `LocalStorage.OpenFile()` transparently opens and decrypts (`cipher.AEAD.Open`) stored files, maintaining zero API-shape changes for reviewer-facing document viewing flows. Added dedicated `DOCUMENT_ENCRYPTION_KEY` environment variable and `Config.DocumentEncryptionKey` field in `config.go`, enforcing cryptographic key separation from `OTP_AES_KEY`, `DOCUMENT_SIGNING_SECRET`, and `JWTSecret`. Enforced fail-fast validation in production mode (`appEnv == "production"`). Built idempotent CLI migration tool `cmd/encrypt-documents/main.go` and executed it on existing repository sample documents (`services/auth-service/data/documents/kyb/...`), encrypting 8 plaintext files in place. Updated `infrastructure/.env.example`, `infrastructure/docker-compose.yml`, `infrastructure/deploy/docker-compose.prod.yml`, `docs/DEPLOYMENT.md`, and `docs/changelog/security-fixes.md`. Verified via `go test ./...` in `auth-service` (100% pass including `TestLocalStorage_EncryptionRoundTripAndDiskVerification` and `TestLocalStorage_ProductionModeMissingKey`), `make ci`, and `gosec` security scans. (Commit `b21628d916753a3eb753195e418cc297f2f99a99`).
* **Verified Client Application Semantic Versioning & Version-Gating Middleware (ADR-0018)**: Established real semver discipline end to end. Updated `build-apk.yml` to extract `version` from `pubspec.yaml` and publish `app-release.json` with real semver `"version"` and `"build_sha"`. Created MongoDB collection `platform_versions` and `version.Store` in `api-gateway` managing `latest_version`, `minimum_supported_version`, `enforce_minimum_version`, and `download_url`. Implemented `version.ParseSemVer` and `middleware.VersionGate` intercepting `X-App-Version` headers. Missing headers pass during grace period when `enforce_minimum_version == false`; out-of-date clients receive HTTP `426 Upgrade Required`. Added admin endpoints `GET / PUT /api/v1/admin/version-config`. Updated Flutter `ApiClient` to inject `X-App-Version` into all requests and intercept HTTP 426 to display `UpdateRequiredScreen` (`update_required_screen.dart`). Authored `docs/adr/0018-client-app-semantic-versioning-and-enforcement-gate.md`, updated `docs/DEPLOYMENT.md` Section 11.3, `docs/frontend/STATUS.md`, and `docs/changelog/infrastructure.md`. Verified via `go test ./...` in `api-gateway` (100% pass), `flutter analyze` (0 issues), `flutter test` (191/191 pass), `make ci`, `make docs-check`, and `.githooks/pre-push` gate exit code 0.
* **Verified ResetPassword Per-User Session Invalidation & Raw MongoDB Error Sanitization**: Added `RevokeAllUserTokens(userID string) error` to `shared/infra/jwtutil`, which sets a Redis key `jwt:invalidated_before:<user_id>` to the current Unix timestamp with an 8-day TTL (24h token lifetime + 7-day refresh window). Updated `ValidateToken` to check `jwt:invalidated_before:<claims.UserID>` and reject tokens issued prior to that timestamp with `jwtutil: token has been revoked`, failing closed on Redis errors. Updated `ResetPassword` in `auth-service` (`services/auth-service/internal/handlers/auth.go`) to invoke `jwtutil.RevokeAllUserTokens(user.ID)` immediately after successful password update in MongoDB. Replaced raw MongoDB error disclosure `"error": "failed to update password: " + err.Error()` with generic message `{"error": "failed to update password"}`, logging underlying errors server-side only. Added regression test suites `TestRevokeAllUserTokens` in `jwt_test.go`, and `TestResetPassword_SessionInvalidation` and `TestResetPassword_DBErrorGenericMessage` in `auth_test.go`. Verified via `go test ./...` in `shared/infra` and `services/auth-service` (100% pass), `go build ./...`, `go vet ./...`, and `gofmt -l .`. (Commit `d04e6a3830dc0df781babb8cf315c9faea4763d9`).
* **Verified Redis Sentinel High Availability Support & JWT Validation Graceful Degradation**: Extended `NewRedisClient` in `shared/infra/ratelimit` to support Redis Sentinel failover mode (`REDIS_SENTINEL_ADDRS`, `REDIS_SENTINEL_MASTER_NAME`, `REDIS_PASSWORD`, `REDIS_SENTINEL_PASSWORD`) while maintaining full backward compatibility for standalone Redis across all 5 services. Refined JWT revocation/denylist checks in `ValidateToken` (`shared/infra/jwtutil/jwt.go`) with thread-safe `redisHealthTracker` to absorb transient network blips and Sentinel failover windows (< 3 consecutive failures, < 5s blip window) via a single 500ms retry, while preserving fail-closed security guarantees for sustained outages and loud write-path failures in `RevokeAllUserTokens`. Verified via unit and regression test suites in `ratelimit_test.go` and `jwt_test.go`, and 100% test pass across all Go modules. (Commit `a606943d7760dca45274fff88c0eeafd688326cf`).
* **Verified Modularization of Monolithic user-service Handlers (`handlers.go` Decomposition)**: Decomposed the monolithic 3,247-line `services/user-service/internal/handlers/handlers.go` into responsibility-scoped domain files (`handlers.go` for struct/constructor/routes/helpers, `services_handlers.go`, `jobs_handlers.go`, `wallet_handlers.go`, `subscription_handlers.go`, `ratings_handlers.go`, `reconciliation_handlers.go`) with zero behavior changes, zero logic alterations, and 100% before-and-after test parity across all microservices and frontend test suites. (Commit `e0747d5557f3c4c3c21325578cc94ff17dd97738`).
* **Verified Stitch Visual Design Brief Comparison & Gap Audit**: Ingested and analyzed the Google Stitch design brief (`design/stitch-export/quick_delivery_ui_brief/`) across 18 screen folders and `velocity_logistics/DESIGN.md`. Conducted screen-by-screen visual gap analysis against all 28 Flutter screens in `frontend/lib/screens/`. Authored `docs/frontend/STITCH_VISUAL_AUDIT.md` cataloguing 65 concrete numbered findings across 17 mapped screen pairs, stylistic extrapolation rules for 11 unmapped screens, verified architectural constraints (Owner 2nd tab as "Employees", Courier Profile deferred), and established a 5-batch implementation roadmap (Batches A–E). Verified via `make docs-check`.
* **Verified Google Stitch Visual Redesign Implementation (Batches A–E)**: Implemented all 65 visual findings from the Google Stitch design brief across all 28 Flutter screens. Updated primary CTA button tokens (`PrimaryButton`) to Amber Gold (`#FFC107` / `AppColors.secondary`) with Deep Navy text (`#0D1321` / `AppColors.onSecondary`) while retaining Deep Navy structural headers/surfaces and crimson red (`AppColors.error`) destructive actions. Built reusable shared components: `OtpPinInput` (6-digit discrete PIN boxes with auto-advance and clipboard paste), `PillFilterBar` (horizontal scrollable category chips with active/inactive states and badge counts), `RouteTimeline` (2-point vertical route connector with dock/bay notes and metrics), and `ThemedCard` `onTap` ink integration. Completed all 5 implementation batches: Batch A (Auth & Onboarding), Batch B (Customer Marketplace & Tracking), Batch C (Tenant Owner Operations & Services), Batch D (Financial Management & Fleet Oversight), and Batch E (Employee Shell, Support & Communication). Verified via `flutter analyze` (0 issues), `flutter test` (100% pass, 259/259 tests), Arabic RTL validation (`ar_EG`), and live backend connectivity checks against `https://api.logiclinkeg.tech`.
* **Verified Stitch Unified UI Redesign — Batch 1: Authentication & Global Screens**: Fully redesigned visual composition of 5 screens (`login_screen.dart`, `signup_screen.dart`, `otp_screen.dart`, `forgot_password_screen.dart`, `update_required_screen.dart`) matching ground-truth Stitch references in `design/stitch-export/logistics_core_unified/`. Added `topAccentColor` and `topAccentHeight` support to `ThemedCard` and `caption` typography token to `theme.dart`. Implemented decorative top accent strips, circular brand headers, 2-card role selection, discrete OTP inputs with security badge, inline password reset with OTP auto-fill, and rich update screen with concentric pulse rings and "What's new" section. Preserved 100% of providers, functional behaviors, validation rules, navigation, localization (`context.l10n`), and error banners. Verified via `dart format`, `flutter analyze` (0 issues), `flutter test` (260/260 pass 100%), and `make docs-check` (exit 0).
* **Verified Stitch Unified UI Redesign — Batch 2: Customer Experience Screens**: Fully redesigned visual composition of 5 screens (`customer_home_screen.dart`, `customer_jobs_screen.dart`, `customer_marketplace_screen.dart`, `job_status_screen.dart`, `customer_job_map_screen.dart`) matching ground-truth Stitch references in `design/stitch-export/logistics_core_unified/` and authoritative final-fidelity designs for `job_status_final_fidelity` and `customer_job_map_final_fidelity`. In `customer_home_screen.dart`, built Stitch Welcome Banner (`#151B2A`), "Where to deliver?" search card, core services category grid (`category_tile_delivery`, `category_tile_transport`, `category_tile_shipping`, `category_tile_all`), and active order cards with progress indicator. In `customer_jobs_screen.dart`, implemented Stitch order matrix with tracking IDs (`#QD-...`), service titles, status badges, price metrics, live progress bars, search bar, and pill filter bar. In `customer_marketplace_screen.dart`, upgraded search & distance filter panel, location map picker, and bento service cards with verified ratings and booking modal. In `job_status_screen.dart`, built authoritative final-fidelity live status bar, track job hero banner, 4-stage fulfillment progress stepper, vertical route itinerary connector, assigned driver profile card, and price negotiation counter-offer panel. In `customer_job_map_screen.dart`, implemented full-screen map with custom pickup and gold driver markers, floating zoom/location controls, and bottom job details sheet. Preserved 100% of functional flows, state, validation, navigation, and localization. Verified via `dart format`, `flutter analyze` (0 issues), `flutter test` (260/260 pass 100%), `make docs-check` (exit 0), and `make ci` (pre-push gate exit code 0).
* **Verified Stitch Unified UI Redesign — Batch 3: Tenant Owner Operations & Services Screens**: Fully redesigned visual composition of all 7 target owner screens (`home_screen.dart`, `employee_screen.dart`, `service_screen.dart`, `owner_configuration_screen.dart`, `wallet_screen.dart`, `subscription_screen.dart`, `owner_reconciliation_queue_screen.dart`) matching ground-truth Stitch references in `design/stitch-export/logistics_core_unified/` and authoritative final-fidelity designs (`owner_service_management_final_fidelity` and `reconciliation_queue_final_fidelity`). In `home_screen.dart`, implemented Stitch Hero Welcome Banner (`#151B2A`), 3-metric Bento grid (Jobs, Fleet, Revenue), summary actions row, quick-access cards, service reputation summary, and Stitch order cards. In `employee_screen.dart`, upgraded employee roster cards with `#QD-` tracking IDs and status pills, register/freeze forms, and audit logs. In `service_screen.dart` (authoritative final fidelity), implemented KYC verification banner, bento service cards with 2-column pricing metrics, category icons, and location tags. In `owner_configuration_screen.dart`, structured profile into 3 Bento sections (Business Identity, Location & Operations, Pricing & Rates). In `wallet_screen.dart`, built Available Balance Hero card with gold typography and trend pill, total/withdrawable/escrow metric cards, and transaction ledger. In `subscription_screen.dart` and `owner_reconciliation_queue_screen.dart` (authoritative final fidelity), polished pricing tier cards and escrow reconciliation review cards. Preserved 100% of functional flows, state, validation, navigation (including invariant that Owner navigation Tab 2 is Employees), and localization. Verified via `dart format`, `flutter analyze` (0 issues), `flutter test` (260/260 pass 100%), `make docs-check` (exit 0), and `make ci` (pre-push gate exit code 0).
* **Verified Stitch Unified UI Redesign — Batch 4: Employee & Fleet Operations Screens**: Fully redesigned visual composition of all 4 target employee & fleet operations screens (`employee_home_screen.dart`, `employee_jobs_screen.dart`, `employee_history_screen.dart`, `owner_fleet_map_screen.dart`) matching ground-truth Stitch references in `design/stitch-export/logistics_core_unified/` and authoritative final-fidelity designs (`employee_jobs_final_fidelity` and `employee_history_final_fidelity`). In `employee_home_screen.dart`, preserved 3-tab employee shell (Jobs, History, Settings) with Quick Delivery brand header, KYC verification action, and notifications. In `employee_jobs_screen.dart` (authoritative final fidelity), implemented Stitch Welcome & Location status card ("GPS Live" / "GPS Off"), Current Active Job card with customer details, RouteTimeline, payment container (COD / Pre-paid), location tracking indicator/permission warning, chat/complete actions, queued job cards, and action simulator card. In `employee_history_screen.dart` (authoritative final fidelity), implemented Stitch Header, Recent Activity section, and historical job cards with timestamps, customer info, payment methods, and cancellation alerts. In `owner_fleet_map_screen.dart`, implemented Stitch floating fleet filter pills (All Fleet, On Route, Idle), interactive OpenStreetMap with courier markers, elevated map zoom/center controls, and live WebSocket streaming. Preserved 100% of functional flows, state, validation, navigation, and localization. Verified via `dart format`, `flutter analyze` (0 issues), `flutter test` (260/260 pass 100%), `make docs-check` (exit 0), and `make ci` (pre-push gate exit code 0).
* **Verified Stitch Unified UI Redesign — Batch 5: Shared, Support & Cross-Screen Consistency Screens**: Fully verified and harmonized visual composition across all 7 target shared/support screens (`kyc_document_upload_screen.dart`, `owner_history_screen.dart`, `settings_screen.dart`, `my_account_screen.dart`, `notifications_screen.dart`, `chat_screen.dart`, `rating_screen.dart`) matching ground-truth Stitch references in `design/stitch-export/logistics_core_unified/`. In `kyc_document_upload_screen.dart`, verified document slot cards with status badges (Approved/Uploaded/Pending/Rejected), rejection reason alert banners, and upload restrictions. In `owner_history_screen.dart`, verified 3-tab layout (Activity Log, Jobs, Ledger) with `#QD-` tracking IDs, `StatusBadge`, and filter pills. In `settings_screen.dart`, verified account overview card with role pill, SegmentedButton theme/language pickers, KYC access row, and action destinations. In `my_account_screen.dart`, verified read-only email with change modal, editable fields, address manager with 10-item cap, and primary update action. In `notifications_screen.dart`, verified category filter pills, operational status banner, unread notification cards with tag badges, and clear actions. In `chat_screen.dart`, verified Live connection indicator, sender usernames, chat bubbles, and message composer. In `rating_screen.dart`, implemented Stitch customer rating layout with driver profile card, headline, centered 5-star rating selector with gold icons (`#FFC107`), private feedback field, primary submit action (`Icons.send`), and blind status visualizer. Preserved 100% of functional flows, state, validation, navigation, and localization. Verified via `dart format`, `flutter analyze` (0 issues), `flutter test` (260/260 pass 100%), `make docs-check` (exit 0), and `make ci` (pre-push gate exit code 0).
* **Permanent Invariant — KYB/KYE Reviewer Screen Excluded from Consumer Mobile Binary (ADR-0013)**: The administrative reviewer interface (`kyb_kye_review_screen.dart`, `document_viewer_dialog.dart`, and associated reviewer methods) is permanently excluded from `frontend/lib/` per ADR-0013. This screen was intentionally removed twice during development (once in initial modularization and re-reverted after mistaken restoration) and belongs strictly to the standalone Support Agent Console repository (`omarmaarouf18/support-agent-console`). It MUST NEVER be re-added to `frontend/lib/`.
* **Verified Remediation of Stitch Mockup Freight & Warehousing Content Leak**: Audited all 27 Flutter screens touched by the Stitch redesign to eliminate generic B2B freight, warehousing, loading bay, and supply chain placeholder content inadvertently imported from the Stitch export. Replaced placeholder descriptions with authentic Egyptian consumer quick delivery, courier, and home services copy across `customer_home_screen.dart`, `employee_jobs_screen.dart`, `employee_history_screen.dart`, `service_screen.dart`, `job_status_screen.dart`, `update_required_screen.dart`, `app_en.arb`, `app_ar.arb`, and `test/shared_widgets_test.dart`. Verified via `flutter analyze` (0 issues), `flutter test` (100% pass, 260/260 tests passed), and literal grep proving zero remaining freight/warehousing/dock placeholder content in `frontend/lib/`.
* **Ground-Up UI Rebuild — `subscription_screen.dart`**: Performed 100% ground-up rebuild of `subscription_screen.dart` replacing the legacy widget tree with the authentic DOM structure from `design/stitch-export/logistics_core_unified/subscription_plans/code.html`. Implemented centered hero header, Deep Navy active plan container (`AppColors.primaryContainer`) with status pills and pending payment warning, responsive 2-column pricing card matrix via `LayoutBuilder`, Standard tier card with check/cancel feature matrix and outlined downgrade action, and Enterprise Pro card with highlighted 2px amber border, top-right `RECOMMENDED` badge, gold star iconography, feature list, and primary `arrow_forward` upgrade CTA. Preserved all state logic, provider calls, and tests. Verified via `dart format`, `flutter analyze` (0 issues), and `flutter test` (4/4 tests passed).
* **Ground-Up UI Rebuild — `my_account_screen.dart`**: Performed 100% ground-up rebuild of `my_account_screen.dart` replacing the legacy widget tree with the authentic DOM structure from `design/stitch-export/logistics_core_unified/my_account/code.html`. Implemented Stitch Display Header ("My Account" with operational subtitle), responsive 2-column grid (`LayoutBuilder` with 2:1 flex ratio on desktop, stacked on mobile), Profile Information card (`ThemedCard`) with read-only locked email field, inline EmailChangeDialog trigger button, editable username and phone fields, User Overview card with initialed avatar and status pill, and Frequent Addresses card with 10-item limit counter, new address input row, and deletion handlers. Preserved all providers, validation logic, IDOR error handling, address management, and tests. Verified via `dart format`, `flutter analyze` (0 issues), and `flutter test` (7/7 tests passed).
* **Ground-Up UI Rebuild — `notifications_screen.dart`**: Performed 100% ground-up rebuild of `notifications_screen.dart` replacing the legacy widget tree with the authentic DOM structure from `design/stitch-export/logistics_core_unified/notifications/code.html`. Implemented top connectivity banner (`System Status: Operational` / `Sync paused. Showing offline notifications`), horizontal PillFilterBar (`All`, `Jobs`, `System`, `Alerts`), grouped chronological sections (`Today`, `Yesterday`, `Earlier`), and Stitch notification cards (`ThemedCard`) with vertical Amber Gold unread indicator bar, circular icon badges (`AppColors.primaryContainer`, `AppColors.errorContainer`, `AppColors.surfaceContainerHigh`), uppercase category tag chips, relative timestamps, title/body layout, and individual delete/dismiss icon buttons. Preserved all providers, debounced card tap read state transitions, clear-all confirmation dialogs, empty states, and tests. Verified via `dart format`, `flutter analyze` (0 issues), and `flutter test` (5/5 tests passed).
* **Ground-Up UI Rebuild — `customer_job_map_screen.dart`**: Performed ground-up rebuild of `customer_job_map_screen.dart` (+344 -290, 69.7% deletion ratio) aligning with `design/stitch-export/logistics_core_unified/customer_job_map_final_fidelity/code.html`. Implemented OpenStreetMap live interactive canvas, Deep Navy pickup location marker with flag badge and drop shadow, Amber Gold active courier tracking marker with bike icon and courier ID chip, floating elevated map zoom/recenter controls (`Icons.add`, `Icons.remove`, `Icons.my_location`), live reconnection status banners (`ThemedErrorBanner` / `ThemedWarningBanner`), and Stitch semi-transparent bottom sheet overlay with drag handle, `#QD-` tracking ID, courier transit status subtitle, and primary `arrow_forward` CTA. Preserved all providers, WebSocket channel subscriptions, hydration workflows, and tests. Verified via `dart format`, `flutter analyze` (0 issues), and `flutter test` (6/6 tests passed).
* **Ground-Up UI Rebuild — `login_screen.dart`**: Performed ground-up rebuild of `login_screen.dart` (+253 -235, 67.1% deletion ratio) aligning with `design/stitch-export/logistics_core_unified/login/code.html`. Implemented top chrome utility bar (QD logotype badge, dark/light theme toggle, Arabic/English locale toggle), main Stitch auth card (`ThemedCard`) with 4px Amber Gold top accent stripe (`AppColors.secondary`), centered circular brand shipping icon badge (`AppColors.primaryContainer`), display headline ("QuickDelivery") and operational subtitle, email/username input with person icon, password row with inline "Forgot password?" link, password input with lock icon, full-width primary Amber Gold "Sign In" CTA button with `arrow_forward` trailing icon, and external footer navigation to signup. Preserved all auth providers, 2FA OTP navigation branching, error snackbars with retry handlers, form validators, and debounced navigation routes. Verified via `dart format`, `flutter analyze` (0 issues), and `flutter test` (3/3 unit tests, 260/260 suite-wide).
* **Ground-Up UI Rebuild — `signup_screen.dart`**: Performed ground-up rebuild of `signup_screen.dart` (+215 -198, 55.6% deletion ratio) aligning with `design/stitch-export/logistics_core_unified/signup/code.html`. Implemented main Stitch signup card (`ThemedCard`) with 4px top Amber Gold accent stripe (`AppColors.secondary`), centered circular brand shipping icon badge (`AppColors.primaryContainer`), display headline ("Create Account") and subtitle ("Join QuickDelivery to manage your deliveries efficiently."), full name / username input with person icon and automatic Arabic RTL rune detection, email input with mail icon, password input with lock icon and 6-char validator, 2-card interactive role selector matrix (Customer vs Business Owner with active border/tint states), primary Amber Gold "Create Account" CTA button with `arrow_forward` trailing icon, and external footer navigation to sign in. Preserved all auth providers, OTP 2FA flow, error snackbars, username length and character regex validators, and debounced navigation routes. Verified via `dart format`, `flutter analyze` (0 issues), and `flutter test` (11/11 tests across auth & widget suites, 260/260 suite-wide).
* **Ground-Up UI Rebuild — `otp_screen.dart`**: Performed ground-up rebuild of `otp_screen.dart` (+209 -168, 56.6% deletion ratio) aligning with `design/stitch-export/logistics_core_unified/otp_verification/code.html`. Implemented main Stitch verification card (`ThemedCard`) with 4px Amber Gold top accent stripe (`AppColors.secondary`), centered circular security shield badge (`AppColors.primaryContainer`, gold `Icons.security_outlined`), display headline ("Two-Factor Verification" / "Verify Your Account"), subtitle with target email address, 6-digit discrete PIN input matrix (`OtpPinInput`) with auto-advance and FormField 6-digit validation, local development mode auto-populated OTP code banner, primary Amber Gold "Verify Code" CTA button with `arrow_forward` trailing icon, secondary outlined "RESEND CODE" button with refresh icon, and enterprise trust protocol security footnote. Preserved all auth providers, OTP verification and resend logic, dev mode auto-population, and navigation routes. Verified via `dart format`, `flutter analyze` (0 issues), and `flutter test` (11/11 tests across auth & widget suites, 260/260 suite-wide).
* **Ground-Up UI Rebuild — `forgot_password_screen.dart`**: Performed ground-up rebuild of `forgot_password_screen.dart` (+249 -216, 57.3% deletion ratio) aligning with `design/stitch-export/logistics_core_unified/forgot_password/code.html`. Implemented main Stitch reset card (`ThemedCard`) with 4px Amber Gold top accent stripe (`AppColors.secondary`), centered circular lock reset badge (`AppColors.primaryContainer`, gold `Icons.lock_reset`), display headline ("Forgot Password?") and instructional subtitle, dev OTP disclosure banner, email input field with `Icons.email_outlined`, secondary "Send Code" request button (`Key('request_reset_code_button')`), 6-digit OTP code PIN field (`OtpPinInput`), new password and confirm new password fields with lock icons and match validation, primary Amber Gold "Reset Password" CTA button with `arrow_forward` trailing icon, and external footer navigation back to login. Preserved single-screen reset architecture, auth providers, dev mode auto-population, and form validators. Verified via `dart format`, `flutter analyze` (0 issues), and `flutter test` (7/7 unit tests across auth & forgot password suites, 260/260 suite-wide).
* **Ground-Up UI Rebuild — `update_required_screen.dart`**: Performed ground-up rebuild of `update_required_screen.dart` (+244 -217, 56.7% deletion ratio) aligning with `design/stitch-export/logistics_core_unified/update_required/code.html`. Implemented decorative dark gradient header (`AppColors.primaryContainer` to black) with concentric animated pulse rings and centered Amber Gold system update icon badge (`bg-secondary-container`, `Icons.system_update`), display headline ("Mandatory Update" / "App Update Required") and operational subtitle, "What's new:" feature card with security/routing/stability bullet points, installed/minimum/latest version matrix with error highlight badges, warning notice callout banner (`AppColors.errorContainer` with `Icons.warning_amber_rounded`), primary Amber Gold "Update Now" CTA button (`Key('update_now_button')` with `Icons.arrow_forward`), and target download URL caption. Preserved PopScope non-dismissible guard, locale provider integration, version parameters, and onUpdatePressed callbacks. Verified via `dart format`, `flutter analyze` (0 issues), and `flutter test` (1/1 unit test, 260/260 suite-wide).
* **Major Redesign & Bento Layout — `customer_home_screen.dart`**: Performed major redesign of `customer_home_screen.dart` (+397 -371, 48.7% deletion ratio) aligning with `design/stitch-export/logistics_core_unified/customer_home/code.html`. Implemented Deep Navy welcome hero container (`AppColors.primaryContainer`) with blurred ambient gold orb, display headline and subtitle, primary "Quick Book Now" CTA button (`Key('quick_book_now_button')`), "Where to deliver?" quick search card (`ThemedCard`, `Key('customer_quick_booking_card')`), 2x2 core services grid with dedicated icon containers (Delivery, Transport, Home Services, Browse All), and active jobs section with `#QD-` tracking badges, 2-phase progress indicator bar, origin/destination anchors, courier profile snippet, agreed/suggested pricing badge, and empty state fallbacks. Preserved 4-tab IndexedStack lazy hydration, notification bell with unread badge counter (`Key('notification_bell_button')`), category routing to `CustomerMarketplaceScreen`, debounced tap protection, and localization. Verified via `dart format`, `flutter analyze` (0 issues), and `flutter test` (6/6 tests across customer home and localization suites, 260/260 suite-wide).
* **Major Redesign & Matrix Layout — `customer_jobs_screen.dart`**: Performed major redesign of `customer_jobs_screen.dart` (+246 -221, 53.8% deletion ratio) aligning with `design/stitch-export/logistics_core_unified/my_jobs/code.html`. Implemented Stitch page header ("My Orders" / "Track and manage your delivery history."), search input bar (`ThemedTextField` with `Icons.search`), horizontal `PillFilterBar` with live badge counters (`All`, `In Transit`, `Completed`, `Cancelled`), and Stitch order cards (`ThemedCard`) with `#QD-` tracking IDs, service title, status badges (`StatusBadge`), payment method and pricing row, 2-phase progress indicator bar for active orders, cancellation reason callout containers for cancelled orders, and full empty/error/loading states with pull-to-refresh. Preserved embedded tab support (`isEmbeddedInTab`), `MarketplaceProvider` and `AuthProvider` integration, client-side query filtering, `JobStatusScreen` push navigation, and all test finders. Verified via `dart format`, `flutter analyze` (0 issues), and `flutter test` (6/6 unit tests, 260/260 suite-wide).
* **Major Redesign & Bento Layout — `customer_marketplace_screen.dart`**: Performed major redesign of `customer_marketplace_screen.dart` (+390 -391, 40.8% deletion ratio) aligning with `design/stitch-export/logistics_core_unified/marketplace/code.html`. Implemented Stitch search and filter control card (`ThemedCard`) with nearby distance switch (`Key('nearby_filter_switch')`), Map location picker launcher (`Key('choose_location_map_button')`), interactive LocationPickerMap dialog (`Key('location_picker_dialog')`, `Key('confirm_location_button')`), radius text field, category & sort-by dropdown selectors, and Stitch Bento/service cards (`ThemedCard`) with circular category icon containers, category badge chips, distance indicator, star ratings (`ServiceRatingWidget`), transparent dividers, breakdown pricing (`Base: $X + $Y/km`, `Est. Price: $Z`), compact primary "Book" actions, and interactive Cash-on-Delivery (COD) booking confirmation dialog with `ThemedWarningBanner`. Preserved embedded tab mode (`isEmbeddedInTab`), `MarketplaceProvider` and `AuthProvider` integration, `MarketplaceCardSkeleton` loading states with `AnimatedSwitcher`, and all test finders. Verified via `dart format`, `flutter analyze` (0 issues), and `flutter test` (4/4 unit tests, 260/260 suite-wide).
* **Targeted Presentation Refactor — `job_status_screen.dart`**: Performed targeted presentation refactor of `job_status_screen.dart` (+525 -487, 38.4% deletion ratio) aligning with `design/stitch-export/logistics_core_unified/job_status_final_fidelity/code.html`. Implemented status bar with colored pulse dot and `#QD-` order ID, Deep Navy Track Job hero banner with map icon and push to `CustomerJobMapScreen` (`Key('open_map_tracking_button')`), fulfillment progress stepper (`Pending`, `Assigned`, `In Transit`, `Completed`), Bento itinerary card with vertical route connector and payment/fare metrics, assigned courier driver profile card with verified badge and live chat trigger (`Key('open_chat_button')`), cancellation callout alerts, contextual lifecycle actions (RatingScreen trigger, CancelJobDialog modal, CreateTicketDialog modal), and full negotiable transport pricing matrix with 5-minute countdown timer (`Key('countdown_timer_text')`), expired window banner (`Key('negotiation_expired_banner')`), incoming proposal card (`Key('incoming_proposal_card')`, `Key('proposed_price_text')`, `Key('proposal_comparison_text')`), proposal acceptance/decline buttons, and ±50% counter-offer submission field (`Key('counter_offer_input')`, `Key('submit_proposal_button')`). Preserved background status polling, public profile username resolution, and all test finders. Verified via `dart format`, `flutter analyze` (0 issues), and `flutter test` (27/27 unit tests across lifecycle & negotiation suites, 260/260 suite-wide).
* **Targeted Presentation Refactor — `rating_screen.dart`**: Performed targeted presentation refactor of `rating_screen.dart` (+170 -146, 27.3% deletion ratio) aligning with `design/stitch-export/logistics_core_unified/customer_rating/code.html`. Implemented operator profile hero card with large circular avatar (`EntityAvatar`, radius 36) and monospace `#QD-` order identifier, centered rating question headline ("How was your delivery?"), 5 interactive Amber Gold stars (44px), multiline comments textarea (`ThemedTextField`), primary Amber Gold submit button with `send` icon (`PrimaryButton`), blind status visualizer card with lock-in status / waiting indicator and progress bar, and 3 educational info cards detailing unbiased feedback, reliability ranks, and 24h submission window. Preserved role discovery logic (`_determineParties`), rating polling (`_checkOtherPartyRatingStatus`), `MarketplaceProvider.rateJob` submission, error snackbars with retry handlers, and all test finders. Verified via `dart format`, `flutter analyze` (0 issues), and `flutter test` (260/260 tests suite-wide).
* **Ground-Up UI Rebuild — `home_screen.dart`**: Performed ground-up rebuild of `home_screen.dart` (+1080 -828, 68.3% deletion ratio) aligning with `design/stitch-export/logistics_core_unified/owner_dashboard/code.html`. Implemented Stitch owner top bar, Deep Navy welcome hero card with 3px gold accent stripe and interactive wallet balance badge (`Key('owner_dashboard_wallet_badge')`), Bento metrics grid (Active Jobs with "+12% vs last week" trend chip, Active Fleet with linear progress bar), inline summary bar (Subscription tier router `Key('owner_dashboard_sub_chip')`, Employee roster switch `Key('owner_dashboard_employees_chip')`, Escrow review queue `Key('owner_dashboard_escrow_chip')`), quick access management cards (`Key('owner_dashboard_wallet_card')`, `Key('owner_dashboard_services_card')`, `Key('owner_dashboard_config_card')`), Stitch urgent actions split with error/warning accent borders (Vehicle maintenance schedule trigger, Pending reconciliations review trigger), Fleet Overview active zone map preview banner, service reputation rating summary (`RatingSummaryCard`), and active owner jobs list (`ListView.builder`, `#QD-` tracking format, `StatusBadge`, `CancelJobDialog`). Preserved 4-tab IndexedStack state retention, multi-role dispatch (owner, employee, customer), and all test finders. Verified via `dart format`, `flutter analyze` (0 issues), and `flutter test` (11/11 unit tests, 260/260 suite-wide).
* **Major Redesign & Roster Layout — `employee_screen.dart`**: Performed major redesign of `employee_screen.dart` (+385 -358, 48.4% deletion ratio) aligning with `design/stitch-export/logistics_core_unified/employee_management/code.html`. Implemented Stitch employee roster header card with subtitle, search bar with query filtration (`Key('employee_search_field')`), horizontal `PillFilterBar` with live active/frozen count chips, responsive worker list (`Key('employees_list_view')`) with discrete status badges (`Key('employee_item_<id>')`), register new worker form (`ThemedCard`) with RTL rune detection and character bounds, freeze/unfreeze security toggle form (`ThemedCard`) with owner re-authentication password verification and destructive warning state (`Key('employee_toggle_submit_button')`), and 2-tab TabBarView integrating the immutable audit log trail with client IP tracking and formatted timestamps. Preserved employee session routing to `EmployeeJobsScreen`, `OwnerProvider` operations, and all test finders. Verified via `dart format`, `flutter analyze` (0 issues), and `flutter test` (6/6 unit tests, 260/260 suite-wide).
* **Ground-Up UI Rebuild — `service_screen.dart`**: Performed ground-up rebuild of `service_screen.dart` (+271 -250, 68.3% deletion ratio) aligning with `design/stitch-export/logistics_core_unified/owner_service_management_final_fidelity/code.html`. Implemented Stitch service management header with title and subtitle, KYC verification required guard container with lock icon and warning disclosure, Stitch service Bento cards (`ThemedCard`) with circular category icon containers, service title, live green active status dot, discrete 2-column pricing breakdown box (`Base Rate: $XX.00`, `Per KM: $X.XX`), GPS location coordinates readout, empty state fallback with "Create Service" CTA, and Floating Action Button (`Key('add_service_fab')`) guarded by KYC approval state. Preserved `OwnerProvider.fetchServices`, tenant ID filtering, `CreateServiceDialog` modal creation flows, and localization. Verified via `dart format`, `flutter analyze` (0 issues), and `flutter test` (11/11 tests across owner home and service navigation suites, 260/260 suite-wide).
* **Major Redesign & Form Layout — `owner_configuration_screen.dart`**: Performed major redesign of `owner_configuration_screen.dart` (+461 -428, 53.6% deletion ratio) aligning with `design/stitch-export/logistics_core_unified/business_configuration/code.html`. Implemented Stitch business configuration header, Business Identity section card (`ThemedCard`) with circular logo avatar preview and image picker (`Key('owner_config_pick_image_button')`, `Key('owner_config_photo_url_field')`), business name text field (`Key('owner_config_name_field')`), category dropdown (`Key('owner_config_category_dropdown')`), Location & Operations section card with headquarters address (`Key('owner_config_address_field')`), interactive LocationPickerMap dialog trigger and coordinate readout (`Key('owner_config_location_picker_button')`, `Key('owner_config_location_text')`), working hours field (`Key('owner_config_working_hours_field')`), coverage radius input (`Key('owner_config_radius_field')`), Pricing Structure section card with Base Price and Price Per KM fields (`Key('owner_config_base_price_field')`, `Key('owner_config_price_per_km_field')`), live 10KM delivery estimate calculator bar, and primary "Save Configuration" CTA button (`Key('owner_config_save_button')`). Preserved prepopulation logic, service mutations, validation, and error banners (`Key('owner_config_error_banner')`). Verified via `dart format`, `flutter analyze` (0 issues), and `flutter test` (8/8 unit tests, 260/260 suite-wide).
* **Major Redesign & Balance Hero — `wallet_screen.dart`**: Performed major redesign of `wallet_screen.dart` (+260 -225, 44.4% deletion ratio) aligning with `design/stitch-export/logistics_core_unified/wallet_transactions/code.html`. Implemented Stitch header with subtitle and Deposit Funds action (`DepositFundsDialog.show(context)`), Deep Navy Available Balance Hero card with 3px gold accent stripe and "+8.4% vs last mo" green trend chip, balance breakdown cards row (Total Portfolio, Withdrawable Balance, Locked in Escrow), primary Request Payout button (`Key('request_payout_button')`), platform fee percentage indicator (`Key('platform_fee_percentage_text')`, ensuring platform_wallet_id is strictly omitted from presentation), payout requests history section with status badges (`Key('payout_history_header')`, `Key('payout_tile_<id>')`, `Key('payout_status_badge_<id>')`), and transaction ledger section with directional inflow/outflow icons, timestamps, job tracking tags, and running balance readouts. Preserved `WalletScreenSkeleton` cross-fade on initial load, provider state, and all test finders. Verified via `dart format`, `flutter analyze` (0 issues), and `flutter test` (16/16 tests across payout, config & skeleton suites, 260/260 suite-wide).
* **Incremental Stitch Alignment — `owner_reconciliation_queue_screen.dart`**: Performed incremental Stitch alignment of `owner_reconciliation_queue_screen.dart` (+24 -7, 1.7% deletion ratio) refining headers, search field padding, and `PillFilterBar` styling against `design/stitch-export/logistics_core_unified/owner_reconciliation_queue_unified/code.html`. Preserved existing queue card architecture, confirmation modal flows (`ConfirmActionDialog`), `ReconciliationProvider.resolveJob` mutations, and all test finders. Verified via `dart format`, `flutter analyze` (0 issues), and `flutter test` (9/9 unit tests, 260/260 suite-wide).
* **Ground-Up UI Rebuild — `owner_fleet_map_screen.dart`**: Performed ground-up rebuild of `owner_fleet_map_screen.dart` (+356 -250, 65.1% deletion ratio) aligning with `design/stitch-export/logistics_core_unified/fleet_tracking_map/code.html`. Implemented Stitch header with localized Fleet Live Map title and status refresh action, interactive FlutterMap OSM tile canvas with custom Amber Gold courier pins and driver badge pills, floating fleet filter pills (All Fleet, On Route, Idle), empty fleet state alert notice (`ThemedCard`), interactive driver detail sheet with avatar and assigned job tracking tag, reconnection/error banners (`ThemedErrorBanner`), and floating map control buttons (zoom in, zoom out, recenter on fleet centroid). Preserved real-time WebSocket streaming, initial REST hydration, and all test finders. Verified via `dart format`, `flutter analyze` (0 issues), and `flutter test` (6/6 unit tests, 260/260 suite-wide).
* **Targeted Presentation Refactor — `employee_home_screen.dart`**: Performed targeted presentation refactor of `employee_home_screen.dart` (+105 -91, 37.8% deletion ratio) aligning with `design/stitch-export/logistics_core_unified/employee_home/code.html`. Implemented Stitch navigation shell with transparent AppBar, QuickDelivery logotype badge container (`Key('app_header_logo')`), tab dynamic title, KYC verification action button (`Key('employee_verification_button')`), notification bell with unread badge counter, lazy-hydrated 3-tab `IndexedStack` (EmployeeJobsScreen, EmployeeHistoryScreen, SettingsScreen), and 3-destination `NavigationBar` (`Key('employee_bottom_navigation_bar')`, `Key('employee_nav_tab_home')`, `Key('employee_nav_tab_history')`, `Key('employee_nav_tab_settings')`). Preserved background notification listener, `EmployeeJobsProvider` hydration, and all test finders. Verified via `dart format`, `flutter analyze` (0 issues), and `flutter test` (6/6 unit tests, 260/260 suite-wide).
* **Incremental Stitch Alignment — `employee_jobs_screen.dart`**: Performed incremental Stitch alignment of `employee_jobs_screen.dart` (+100 -89, 9.8% deletion ratio) refining worker greeting header, GPS live/off tracking badge chip, and `RouteTimeline` integration against `design/stitch-export/logistics_core_unified/employee_jobs_final_fidelity/code.html`. Preserved embedded mode (`isEmbeddedInTab`), skeleton loader (`EmployeeJobCardSkeleton`), confirmation modal (`ConfirmActionDialog`), and all test finders. Verified via `dart format`, `flutter analyze` (0 issues), and `flutter test` (14/14 tests across employee suites, 260/260 suite-wide).
* **Targeted Presentation Refactor — `employee_history_screen.dart`**: Performed targeted presentation refactor of `employee_history_screen.dart` (+114 -104, 36.1% deletion ratio) aligning with `design/stitch-export/logistics_core_unified/employee_history_final_fidelity/code.html`. Implemented Stitch header with localized Job History title and subtitle, "Recent Activity" section header (`ThemedSectionHeader`), Bento history cards (`ThemedCard`, `Key('employee_history_card_<id>')`) with monospace job IDs, `StatusBadge`, `RouteTimeline` (pickup, dropoff, distance/credits, completion timestamp), subordinate info chips (Customer, Payment, Escrow), cancellation reasons, and empty/error states (`ThemedEmptyState`, `ThemedErrorBanner`). Preserved embedded tab mode (`isEmbeddedInTab`), skeleton cross-fade (`EmployeeJobCardSkeleton`), pull-to-refresh, and all test finders. Verified via `dart format`, `flutter analyze` (0 issues), and `flutter test` (6/6 tests across employee home and history suites, 260/260 suite-wide).
* **Major Redesign & 3-Tab Architecture — `owner_history_screen.dart`**: Performed major redesign of `owner_history_screen.dart` (+291 -290, 45.2% deletion ratio) aligning with `design/stitch-export/logistics_core_unified/owner_history/code.html`. Implemented 3-sub-tab navigation architecture with Deep Navy header, activity audit log tab (`Key('history_tab_activity')`) with monospace action titles, actor IDs, and timestamps, completed & cancelled jobs tab (`Key('history_tab_jobs')`) with search input field (`Key('owner_jobs_search_field')`), `PillFilterBar` (`Key('owner_jobs_pill_filter_bar')`), `StatusBadge`, and cancellation reasons, and wallet ledger tab (`Key('history_tab_ledger')`) with directional inflow/outflow transaction badges, running balance readouts, and job tracking IDs. Preserved embedded mode (`isEmbeddedInTab`), empty/loading/error states, and all test finders. Verified via `dart format`, `flutter analyze` (0 issues), and `flutter test` (5/5 unit tests, 260/260 suite-wide).
* **Targeted Presentation Refactor — `chat_screen.dart`**: Performed targeted presentation refactor of `chat_screen.dart` (+207 -67, 20.4% deletion ratio) aligning with `design/stitch-export/logistics_core_unified/chat/code.html`. Implemented Stitch header with localized chat title, short `#` job tag, live/connecting/disconnected WebSocket status indicator, context job header strip with category icon and channel tag, message bubbles (receiver light slate bubble with top-left square corner, sender Deep Navy container bubble with top-right square corner and double checkmarks), metadata row with username and status, and sticky bottom input area with rounded text field and Amber Gold circular send button (`Icons.send_rounded`). Preserved auto-scrolling, error/access-denied banners, and all test finders. Verified via `dart format`, `flutter analyze` (0 issues), and `flutter test` (3/3 unit test suites, 260/260 suite-wide).
* **Incremental Stitch Alignment — `kyc_document_upload_screen.dart`**: Performed incremental Stitch alignment of `kyc_document_upload_screen.dart` (+70 -23, 3.6% deletion ratio) aligning with `design/stitch-export/logistics_core_unified/kyc_verification/code.html`. Implemented Stitch header with localized role verification title (`Owner Verification (KYB)` / `Employee Verification (KYE)`), refresh status action button, Stitch verification status card with colored left accent stripe (green for approved, red for rejected, gold for pending, navy for unverified), rejection reason callout banner (`ThemedErrorBanner`), approved locked banner (`ThemedSuccessBanner`), section header with badge icon (`ThemedSectionHeader`), responsive document slot cards (`ThemedCard`) for ID Front, ID Back, Selfie, and Business Proof (owner only) with file format constraints (JPEG, PNG, PDF up to 10MB), client-side size and format validations, live upload thumbnail preview with remove selection action, and animated upload/replace CTA buttons. Preserved `onPickFile` test harness override, `AuthProvider.uploadDocument` mutations, and all test finders. Verified via `dart format`, `flutter analyze` (0 issues), and `flutter test` (5/5 unit tests, 260/260 suite-wide).
* **Incremental Stitch Alignment — `settings_screen.dart`**: Performed incremental Stitch alignment of `settings_screen.dart` (+63 -11, 2.7% deletion ratio) aligning with `design/stitch-export/logistics_core_unified/settings/code.html`. Implemented Stitch header with localized Settings title, notification bell action (`NotificationsScreen`), top user profile summary card with large avatar (`EntityAvatar`, radius 28), username, email, and monospace uppercase role badge chip (`AppColors.primary`), Preferences section card (`ThemedCard`) with WCAG AA compliant theme mode selector (`SegmentedButton<ThemeMode>`) and language selector (`SegmentedButton<String>`), Account section card (`ThemedCard`) with role-gated configuration links (Owner Configuration for owners, My Account profile for customers, and KYC Identity Verification for unverified owners/employees), Support section card with customer service ticket launcher (`CreateTicketDialog`), and Danger Zone logout CTA (`PrimaryButton`, `isDestructive: true`, `Icons.logout`). Preserved embedded mode (`isEmbeddedInTab`), theme/locale provider synchronization, and all test finders. Verified via `dart format`, `flutter analyze` (0 issues), and `flutter test` (19/19 unit tests across settings and home suites, 260/260 suite-wide).
* **Cross-Screen Consistency Pass — Token & Radius Harmonization**: Audited all 28 rebuilt screens across `frontend/lib/screens/` and shared widgets in `frontend/lib/widgets/` for hardcoded token drift and radius consistency. Remediated hardcoded hex colors in `service_screen.dart` (`Color(0xFF79DB8D)` -> `AppColors.success`) and `update_required_screen.dart` (`Color(0xFF000000)` -> `AppColors.primary`). Replaced bare numeric radius literals with design tokens in `customer_home_screen.dart` (`AppRadius.smBorder`), `customer_job_map_screen.dart` (`AppRadius.xsBorder`), `customer_jobs_screen.dart` (`AppRadius.xxsBorder`), `home_screen.dart` (`AppRadius.smBorder`), and `chat_screen.dart` (`Radius.circular(AppRadius.xs)`). Verified shell consistency across owner, employee, and customer shells (`NavigationBar`, `AppBar`, `IndexedStack`). Verified via `dart format`, `flutter analyze` (0 issues), and full test suite `flutter test` (260/260 tests passing).
* **AppShell Composition Root Widget & Standalone Harness**: Created `AppShell` (`frontend/lib/widgets/app_shell.dart`) standardizing unified AppBar presentation, subtitle support, back navigation, actions, TabBar bottom slots, SafeArea, standard `AppSpacing` padding, FloatingActionButton, and `isEmbeddedInTab` behavior. Built dedicated standalone widget test suite (`frontend/test/app_shell_test.dart` 7/7 pass). Verified via `dart format`, `flutter analyze` (0 issues), `flutter test` (267/267 tests pass suite-wide), and composition-layer audit script.
* **ListScreenTemplate Generic Screen Template**: Created `ListScreenTemplate<T>` (`frontend/lib/widgets/list_screen_template.dart`) wrapping `AppShell` with standardized loading (`ThemedLoadingIndicator` / skeleton slot), empty (`ThemedEmptyState`), and error (`ThemedErrorBanner` + `onRetry`) states, `AnimatedSwitcher` transitions, optional pinned header/footer, pull-to-refresh integration, and standard item spacing/padding via `AppSpacing`. Built dedicated widget test suite (`frontend/test/list_screen_template_test.dart`). Verified via `dart format`, `flutter analyze` (0 issues), `flutter test` suite-wide, and composition-layer audit script.
* **Notifications Screen Composition Migration (`notifications_screen.dart`)**: Migrated `NotificationsScreen` to `AppShell` + `ListScreenTemplate` composition root, eliminating manual `Scaffold`, `AppBar`, and 4 `BoxDecoration` usages in favor of `ThemedCard`, `CircleAvatar`, and `ClipRRect`. Preserved 100% of providers, state variables, and test keys. Verified via `dart format`, `flutter analyze` (0 issues), `flutter test` (273/273 tests passing), and composition-layer audit script (Scaffold: 27, AppBar: 22, BoxDecoration: 119, toUpperCase: 36).
* **Owner Reconciliation Queue Screen Composition Migration (`owner_reconciliation_queue_screen.dart`)**: Migrated `OwnerReconciliationQueueScreen` to `AppShell` + `ListScreenTemplate` composition root, eliminating manual `Scaffold` and `AppBar` usages while preserving all provider calls, state variables, confirmation dialogs, and test keys. Verified via `dart format`, `flutter analyze` (0 issues), `flutter test` (273/273 tests passing), and composition-layer audit script (Scaffold: 26, AppBar: 21, BoxDecoration: 119, toUpperCase: 36).
* **Customer Marketplace Screen Composition Migration (`customer_marketplace_screen.dart`)**: Migrated `CustomerMarketplaceScreen` to `AppShell` + `ListScreenTemplate` composition root, eliminating manual `Scaffold`, `AppBar`, and `_buildServiceList` in favor of `ListScreenTemplate<MarketplaceService>`. Preserved 100% of providers, location picker map, search & filter bars, booking dialogs, and test finders. Verified via `dart format`, `flutter analyze` (0 issues), `flutter test` (274/274 tests passing), and composition-layer audit script (Scaffold: 25, AppBar: 20, BoxDecoration: 118, toUpperCase: 36).
* **Customer Jobs Screen Composition Migration (`customer_jobs_screen.dart`)**: Migrated `CustomerJobsScreen` to `AppShell` + `ListScreenTemplate` composition root, eliminating manual `Scaffold` and `AppBar` in favor of `ListScreenTemplate<Job>`. Preserved 100% of providers, search and pill filter bar, status badges, price readouts, and test finders. Verified via `dart format`, `flutter analyze` (0 issues), `flutter test` (274/274 tests passing), and composition-layer audit script (Scaffold: 24, AppBar: 19, BoxDecoration: 117, toUpperCase: 36).
* **Owner History Screen Composition Migration (`owner_history_screen.dart`)**: Migrated `OwnerHistoryScreen` to `AppShell` composition root with TabBar navigation and embedded tab support, standardizing layout rhythm and removing bespoke Scaffold wrappers. Preserved 100% of providers, 3 sub-tabs (Activity, Jobs, Ledger), audit log cards, ledger entries, and test finders. Verified via `dart format`, `flutter analyze` (0 issues), `flutter test` (274/274 tests passing), and composition-layer audit script (Scaffold: 23, AppBar: 18, BoxDecoration: 114, toUpperCase: 36).
* **Employee History Screen Composition Migration (`employee_history_screen.dart`)**: Migrated `EmployeeHistoryScreen` to `AppShell` + `ListScreenTemplate` composition root, eliminating manual `Scaffold`, `AppBar`, and `_buildHistoryList` in favor of `ListScreenTemplate<Job>` with scrollable `EmployeeJobCardSkeleton` loader (`employee_history_skeleton_list`), `ThemedErrorBanner` (`employee_history_error`), and `ThemedEmptyState` (`employee_history_empty_state`). Preserved 100% of providers, state variables, embedded tab mode (`isEmbeddedInTab`), pull-to-refresh, `RouteTimeline`, and test finders. Built dedicated widget test suite (`frontend/test/employee_history_screen_test.dart` 5/5 pass). Verified via `dart format`, `flutter analyze` (0 issues), `flutter test` (279/279 tests passing suite-wide), and composition-layer audit script (Scaffold: 22, AppBar: 17, BoxDecoration: 114, toUpperCase: 36).
* **Phase 2 Screen Composition Migration Closing Report (`docs/reports/P2_SCREEN_MIGRATION_REPORT.md`)**: Consolidated and documented the complete Phase 2 screen migrations across 4 core list/history screens (`customer_marketplace_screen.dart`, `customer_jobs_screen.dart`, `owner_history_screen.dart`, `employee_history_screen.dart`). Recorded full before/after progression arithmetic (Scaffold: 26 -> 22, AppBar: 21 -> 17, BoxDecoration: 119 -> 114), complete literal audit output, proactive commit disclosure, preserved key matrix, and test suite verification.
* **FormScreenTemplate Generic Form Screen Template (`frontend/lib/widgets/form_screen_template.dart`)**: Created `FormScreenTemplate` wrapping `AppShell` with standardized form field rhythm via `AppSpacing`, primary and secondary submit action slots (`PrimaryButton`, `SecondaryButton`), inline validation error banners (`ThemedErrorBanner`), submission loading state with form-disabling `IgnorePointer` and spinner animation, keyboard avoidance via `SingleChildScrollView` + `resizeToAvoidBottomInset`, optional card wrapper mode (`ThemedCard`), and embedded tab support (`isEmbeddedInTab`). Built dedicated standalone widget test suite (`frontend/test/form_screen_template_test.dart` 6/6 pass). Verified via `dart format`, `flutter analyze` (0 issues), `flutter test` (285/285 tests pass suite-wide), and composition-layer audit script (22/17/114/36/0/51).
* **Login Screen Composition Migration (`login_screen.dart`)**: Migrated `LoginScreen` to `AppShell` + `FormScreenTemplate` composition root, eliminating manual `Scaffold` and `SingleChildScrollView` in favor of `FormScreenTemplate`. Preserved 100% of providers (`AuthProvider`, `ThemeProvider`, `LocaleProvider`), state variables (`_formKey`, `_emailController`, `_passwordController`, `_lastNavTime`), debounce navigation logic, and test keys (`login_qd_logo`, `login_theme_toggle_button`, `login_lang_toggle_button`). Verified via `dart format`, `flutter analyze` (0 issues), `flutter test` (285/285 tests passing suite-wide), and composition-layer audit script (Scaffold: 21, AppBar: 17, BoxDecoration: 114, toUpperCase: 36).
* **Form-Archetype Composition Migration Batch (8 screens)**: Migrated the remaining form/config archetype screens to `AppShell`/`FormScreenTemplate` composition roots: `signup_screen.dart`, `forgot_password_screen.dart`, `otp_screen.dart` (transparent app bar + centered card preserved via `appBarBackgroundColor`/`appBarForegroundColor`), `settings_screen.dart` (embedded-tab mode + notification bell action preserved), `owner_configuration_screen.dart` (full-body loading state preserved), `kyc_document_upload_screen.dart` (refresh action + pull-to-refresh preserved), `service_screen.dart` (FAB + KYC guard + unauthenticated branch migrated to `AppShell`), and `my_account_screen.dart` (responsive 2-column grid preserved). All test keys, providers, validators, and navigation flows unchanged. Post-batch audit (scripts/frontend_audit.sh): Scaffold files 21 -> 13, AppBar files 17 -> 10, BoxDecoration 114, toUpperCase 36. Verified via `dart format`, `flutter analyze` (0 issues), targeted suites (37/37 across auth/forgot/settings/config/kyc/my-account/password tests), and full suite `flutter test` (288/288 pass).
* **DashboardScreenTemplate & Dashboard Shell Composition Migration (3 screens)**: Created `DashboardScreenTemplate` (`frontend/lib/widgets/dashboard_screen_template.dart`) capturing the shared tabbed-dashboard shell: canonical Quick Delivery brand logo badge (`DashboardScreenTemplate.brandLogo()`, key `app_header_logo`), transparent AppBar with dynamic tab title, lazy-hydrated `IndexedStack` tab body, and keyed `NavigationBar` wiring. Migrated `home_screen.dart` (owner 4-tab shell + non-owner profile branch via `AppShell`), `customer_home_screen.dart`, and `employee_home_screen.dart` onto it, removing three duplicated shell implementations (~90 lines each). All navigation keys (`owner_bottom_navigation_bar`, `customer_bottom_navigation_bar`, `employee_bottom_navigation_bar`, per-tab keys), notification bells, verification actions, and lazy `_visitedTabs` hydration preserved. Refined `scripts/frontend_audit.sh` to match widget construction via PCRE negative lookbehind so identifier substrings (e.g. `_buildOwnerScaffold(`) no longer false-positive; renamed home_screen helpers to `_buildOwnerShell`/`_buildNonOwnerShell` accordingly. Built dedicated test suite (`frontend/test/dashboard_screen_template_test.dart` 4/4 pass). Post-batch audit: Scaffold files 13 -> 10, AppBar files 10 -> 7. Verified via `dart format`, `flutter analyze` (0 issues), and full suite `flutter test` (292/292 pass).
* **AppShell-Only Composition Migration Batch (6 screens)**: Migrated screens with unique layouts that take `AppShell` directly without a list/form/dashboard template: `chat_screen.dart` (two-line title widget with live status indicator preserved via `titleWidget`), `wallet_screen.dart` (no-chrome shell matching original AppBar-less Scaffold), `subscription_screen.dart`, `update_required_screen.dart` (PopScope non-dismissible guard preserved), `employee_screen.dart` (TabBar-only header reproduced via AppShell `bottom:` slot on surface background), and `employee_jobs_screen.dart` (embedded-tab early-return preserved). All actions keys (`employee_verification_button`, `settings_button`), notification bells, and skeletons preserved. Post-batch audit: Scaffold files 10 -> 4, AppBar files 7 -> 4 (remaining: job_status, rating, owner_fleet_map, customer_job_map). Verified via `dart format`, `flutter analyze` (0 issues), and full suite `flutter test` (292/292 pass).
* **Detail & Map Screen Composition Migration Batch (4 screens, P2 completion)**: Migrated the final 4 screens to `AppShell` composition roots: `job_status_screen.dart` (refresh spinner-in-actions preserved), `rating_screen.dart` (transparent app bar preserved), `owner_fleet_map_screen.dart` and `customer_job_map_screen.dart` (navy map headers with refresh actions preserved). **P2 rollout target achieved: 0 files in screens/ construct `Scaffold(` or `AppBar(` directly — all 28 screens now compose exclusively through `AppShell`/templates.** Post-fix audit: Scaffold( files 4 -> 0, AppBar( files 4 -> 0; BoxDecoration 110; toUpperCase 36. Verified via `dart format`, `flutter analyze` (No issues found!), full suite `flutter test` (292/292 All tests passed).
* **P3 Centralized Uppercase Typography**: Added `AppTypography.labelUppercase` (the canonical TextStyle for small badges/chips/status tags) and `AppTypography.uppercaseLabel(String)` (single centralized uppercase transform) in `frontend/lib/core/theme.dart`. Migrated all 36 `.toUpperCase()` usages across 16 screen files onto `uppercaseLabel` via a balanced-paren backward-scanner rewrite; the single multiline method-chain site (`owner_fleet_map_screen.dart`) was repaired manually after scanner mis-handling and caught by `flutter analyze`. Post-fix audit: 0 `.toUpperCase()` occurrences in screens/ (target reached). Verified via `dart format`, `flutter analyze` (No issues found!), full suite `flutter test` (292/292 All tests passed).
* **P4 BoxDecoration Elimination via ThemedPanel**: Created `ThemedPanel` and `AnimatedThemedPanel` (`frontend/lib/widgets/themed_panel.dart`) — the single sanctioned decorated-container primitives for screens (tinted chips, circular icon badges, callouts, hero gradients), internally owning all `BoxDecoration` construction. Mechanically converted 109 `Container(decoration: BoxDecoration(...))` call sites plus 1 `AnimatedContainer` (signup role-selector, now `AnimatedThemedPanel` with implicit DecorationTween via `AnimatedWidgetBaseState`) across 23 screen files with zero unsupported-arg drops. Fixed initial animation repaint defect (state extended `ImplicitlyAnimatedWidgetState` without per-tick rebuilds; moved to `AnimatedWidgetBaseState`, caught by dedicated test). Shared-widget note per CLAUDE.md policy: this change ADDS a new shared widget rather than modifying an existing one; existing widgets' behavior untouched. Ran `dart fix --apply` (19 const-lint fixes) concurrently. Post-fix audit: BoxDecoration( = 0 in screens/. Verified via `dart format`, `flutter analyze` (No issues found!), `test/themed_panel_test.dart` (5/5 pass), full suite `flutter test` (297/297 All tests passed).
* **P5 Frontend Composition Gate (CI Enforcement)**: Created `scripts/frontend_composition_gate.sh` blocking `Scaffold(`/`AppBar(`/`BoxDecoration(`/`Color(0xFF`/`.toUpperCase()` inside `frontend/lib/screens/**` via PCRE negative-lookbehind construction matching, so the exact drift documented in FRONTEND_ARCHITECTURE_PLAN §0 (phases claimed Complete that had already regressed to hardcoded values) can never silently recur — any future violation fails the push/PR automatically instead of relying on human review or agent honesty. Enforced at both enforcement points per the strict parity principle: `.githooks/pre-push` (new gate step before flutter checks) and `.github/workflows/ci.yml` (new `Frontend Composition Gate` step before Flutter Analyze). Negative audit performed: temporarily appended a violation block to `wallet_screen.dart` → gate exited 1 listing all 4 pattern hits with file:line; restored via git checkout → gate exited 0.
* **P6 Component Library Page & Golden Tests**: Built `ComponentLibraryScreen` (`frontend/lib/screens/component_library_screen.dart`) — a debug-only Storybook-style catalog (assert-gated, `/component-library` route registered only under kDebugMode) rendering every shared widget in loading/error/empty/success/interactive states including the new templates and ThemedPanel variants. Added `test/golden_screens_test.dart` with 7 golden tests / 8 snapshot baselines covering the two P0 screens, list/form template representatives from P2, and the component library at three viewports (360x800 / 768x1024 / 1280x800), satisfying the responsive breakpoint check for proof-of-concept screens. To make glyph rendering deterministic and offline-safe, bundled Poppins (Regular/Medium/SemiBold/Bold/ExtraBold, OFL) under `assets/fonts/` and set `GoogleFonts.config.allowRuntimeFetching = false` in `main.dart`. Golden viewport pass surfaced and fixed two real overflow defects: `StatusBadge` (shared widget change per CLAUDE.md policy: Flexible+maxLines ellipsis inside its badge Row — call sites unchanged visually except truncation instead of overflow stripes on long statuses like `escrow_reconciliation_required`) and the reconciliation queue AppBar titleWidget Row (Flexible+ellipsis). Regenerate command documented: `flutter test --update-goldens test/golden_screens_test.dart`. Verified via generation run (7/7 pass), baseline verification run without --update-goldens (7/7 pass), full suite `flutter test` (304/304 All tests passed), `flutter analyze` (No issues found!), dart format clean, composition gate passing.
* **P7 RTL & Accessibility Hardening**: RTL audit across all 28 screens found zero directional violations (`EdgeInsets.only(left:/right:)`: 0; symmetric fromLTRB paddings only); fixed 2 trailing-edge alignments (home_screen job-card cancel action, wallet platform-fee note) from `Alignment.centerRight` to `AlignmentDirectional.centerEnd` so they mirror under ar_EG. A11y audit found 11 icon-only `IconButton`s without accessibility labels; added localized tooltips via 6 new arb keys (tooltipClose, tooltipOpenChat, tooltipRemoveAddress, tooltipZoomIn, tooltipZoomOut, tooltipRecenter; en+ar Egyptian colloquial), reaching 0 unlabeled icon-only buttons. Tooltips follow the codebase's null-safe `context.l10n` fallback convention; two widget-test harnesses were missing localization delegates present in the production MaterialApp and were aligned. Verified via `flutter analyze` (No issues found!) and full suite `flutter test` (304/304 All tests passed).






* **Verified Resilience Wrapper Streaming Fix (Proxied SSE Restoration)**: Fixed a Critical defect in `shared/infra/resilience` (`ResilienceClient.Do` and `ResilienceRoundTripper.RoundTrip`) where `defer cancel()` inside the circuit-breaker closure cancelled the per-attempt request context immediately after response headers were returned — before `httputil.ReverseProxy` read the body — truncating every streamed/large proxied response (SSE notification streams were dead through the API gateway). Replaced with a stoppable `time.AfterFunc` timer bounding only time-to-response-headers, plus a `cancelReadCloser` body wrapper that fires `cancel` on body `Close()`, keeping response bodies readable for their full caller-governed lifetime. Added regression tests (`TestResilienceRoundTripper_StreamingResponseSurvivesBodyRead`, `TestResilienceClient_StreamingResponseBodyReadable`) and a gateway integration test wiring `ResilienceRoundTripper` into the real reverse proxy (`TestProxyStreamingResponseWithResilientTransport`); all three reproduce the truncation on unfixed code (`context canceled` / `unexpected EOF`) and pass post-fix. Verified via `go test ./...` in `shared/infra` and `services/api-gateway` (100% pass) and `.githooks/pre-push` gate.
* **Verified Exact-Locked Escrow Release & Refund (Stranded Funds Fix)**: Fixed a HIGH-severity fund-entrapment defect in `CompleteJob` and `CancelJob` (`services/user-service/internal/handlers/jobs_handlers.go`). Both previously recomputed the release/refund amount from the service's CURRENT `TenantBasePrice`/`TenantPricePerKM` at settlement time and only capped it at `LockedEscrowAmount` — never floored. If an owner lowered service pricing after booking, the recomputed amount fell below the locked amount and the residual (`LockedEscrowAmount - amount`) was stranded in escrow forever on a terminal-status job with no recovery path. Both handlers now release/refund exactly `job.LockedEscrowAmount` (preserving ADR-0002 wallet isolation — no drawdown of other jobs' escrow — and leaving ADR-0007 delivery/shipping reconciliation flagging untouched; its prior floor/cap composition already resolved to exactly the locked amount). Added regression test `TestEscrowRepriceDown_ReleasesAndRefundsExactLockedAmount` covering repriced-down completion (payout 155.60 == locked) and cancellation (refund 80.00 == locked) with wallet-delta assertions. Updated the legacy `CompleteJob_Location_Recalculation` escrow-path expectation which had encoded the stranding behavior (10.0 recomputed) as correct. Verified via full `go test ./... -count=1` in `user-service` (100% pass).
* **Verified Post-Handshake WebSocket & SSE Stream Flood Guards**: Closed two unauthenticated-flood amplification vectors. In `chat-service` (`services/chat-service/internal/handlers/chat.go`), the connect-time `wsLimiter` (30/min) was previously the ONLY WebSocket limit — after handshake, every `message` triggered an outbound authz call plus a MongoDB persist and fan-out, and every `subscribe` another authz call, all unthrottled. Added per-client Redis-backed limiters checked inside `readPump` BEFORE authorization: `chat:wsmsg` (60 msg/min per client) and `chat:wssub` (30 subscribe/min per client), both replying a structured `rate_limited` error frame via a new non-blocking `sendWSError` helper when exceeded. In `notification-service` (`services/notification-service/internal/handlers/handlers.go`), the shared limiter was applied only to `Send` — `GET /stream` registered clients with no limit and no connection cap. Added `notification:stream` registration limiter (30/min per tenant, 429 JSON before SSE headers are set) plus an in-process concurrent-stream cap (`maxConcurrentStreamsPerTenant = 5`) with mutex-guarded slot acquire/release tied to the handler lifecycle. Verified via new tests `TestWebSocket_MessageRateLimiting` (70-message flood → rate_limited frames, only pre-limit messages persisted/broadcast), `TestStream_RegistrationRateLimiting` (31st open → 429), and `TestStream_ConcurrentStreamCapPerTenant` (6th concurrent stream → 429; slots released on close) — full `go test ./... -count=1` passes in both services.
* **Verified Atomic SETNX Idempotency Reservation & Disconnect-Safe Key Writes (TrackJob)**: Closed two residual TrackJob idempotency defects in `services/user-service`. (1) Concurrent-duplicate race: the replay check was GET-then-insert — two simultaneous requests sharing one key could both miss it and both create funded jobs. `reserveIdempotencyKey` now atomically claims the slot via Redis `SET NX` (`__pending__` marker, 24h TTL); losers either replay the winner's job (200) or receive an explicit `409 duplicate_request_in_progress` while the winner is still in flight. A single deferred guard releases a pending reservation on every validation-failure exit unless a job was actually persisted, replacing error-prone per-exit cleanup. (2) Dropped-key defect: `saveIdempotencyKey` wrote through the inbound request context, so a client disconnect between job creation and key-set silently discarded the key and client retries double-booked funded jobs. The function no longer accepts a request context at all — idempotency bookkeeping is unconditionally `context.Background()`. Verified via new regression tests: `TestSaveIdempotencyKey_SurvivesRequestContextCancellation` (deterministic) and `TestTrackJob_ConcurrentDuplicateIdempotencyKey` — the latter reproduced the race in 25/25 pre-fix runs (`-count=25`) and 0/25 post-fix runs.
* **Verified Fund-Endpoint Rate Limiters (CompleteJob, Reconciliation Resolve, Payouts)**: Closed four unbounded fund-related endpoints in `user-service` flagged by the independent code review: `CompleteJob` (escrow release) now enforces `user:complete_job` (30 req/min per authenticated requester), `ResolveReconciliation` enforces `user:reconciliation_resolve` (30 req/min per owner), and both `POST /users/wallet/payout/request` and `GET /users/wallet/payout/requests` enforce `user:payout` (30 req/min per owner). All checks run after JWT identity resolution and are identity-keyed, matching the existing per-endpoint limiter architecture (ADR-0016 tiering). Pre-fix repro demonstrated `RequestPayout` accepting 31 consecutive 201 Created requests with zero throttling. Verified via new flood tests `TestCompleteJob_RateLimiting`, `TestResolveReconciliation_RateLimiting`, `TestRequestPayout_RateLimiting`, `TestGetPayoutRequests_RateLimiting` — each asserting no 429 within the 30-call budget and a 429 on call 31.
* **Verified Escrow-Failure Warning Sanitization (Cross-Tenant Balance Disclosure Fix)**: Fixed two client-facing financial disclosure sites in `services/user-service/internal/handlers/jobs_handlers.go`. On escrow-lock failure, both `TrackJob` (unfunded booking response) and `RespondPrice` (price-acceptance failure) embedded the raw store error — `"insufficient withdrawable balance: have %.2f, need %.2f"` — in a `warning` JSON field, disclosing the tenant owner's exact wallet balance and the requested amount to customers/employees of other tenants. Both now return generic warnings ("escrow lock failed — owner must deposit funds…") while the underlying error remains server-side only. Verified via new regression tests `TestTrackJob_EscrowFailureWarningDoesNotLeakOwnerBalance` and `TestRespondPrice_EscrowFailureWarningDoesNotLeakOwnerBalance`, which captured the leak verbatim pre-fix (`"warning":"insufficient withdrawable balance: have 0.00, need 47.35"` in the literal response body) and assert its absence post-fix.
 Additionally, `NewUserService` limiter construction is now nil-safe: with a nil Redis client (certain test harnesses), all limiter fields are left nil instead of wrapping a nil-Redis `ratelimit.RateLimiter` that would panic on first `CheckAndRecord`, and the three new endpoint guards skip limiting when their limiter is absent.
* **Verified Service Write-Path Coordinate Validation (Spatial Index Poisoning Fix)**: `CreateService` and `UpdateService` in `services/user-service/internal/handlers/services_handlers.go` previously accepted arbitrary coordinates (e.g. latitude 9999) — only the read path (`ListServices`) validated. Out-of-range writes either poisoned the `2dsphere` proximity index silently on create (MongoDB rejecting the geo index entry while the handler still returned 201) or surfaced a raw MongoDB driver error on update that echoed the entire service document back to the client. Both write paths now enforce `isValidCoordinate` bounds (-90..90 / -180..180) immediately after request decoding, returning `400 invalid_coordinates` and shipping an `INVALID_COORDINATES_DETECTED` audit event, matching the existing ListServices pattern. Verified via new regression tests `TestCreateService_RejectsInvalidCoordinates` (pre-fix: 201 with persisted GeoJSON `[31, 9999]`; post-fix: 400) and `TestUpdateService_RejectsInvalidCoordinates` (pre-fix: 500 leaking the full document via driver error; post-fix: 400).
* **Verified Payout Rejection-Refund Store Capability (ADR-0018 Console Prerequisite)**: Closed the missing rejection path for owner payout requests. `CreatePayoutRequest` deducts funds at request time, but previously a rejected payout silently consumed the owner's money — the `rejected` status existed with no code path to reach it and no balance restoration. Added `MongoDB.RejectPayoutRequest(ctx, payoutID, reason)` in `services/user-service/internal/store/mongodb.go`: CAS-guarded via `FindOneAndUpdate` on `status == "requested"` (double-rejection and mutation of approved/paid requests are impossible), restores `withdrawable_balance` and `total_balance` atomically, records a `payout_refund` ledger entry (new `models.TxPayoutRefund` constant), and persists the rejection reason. Deliberately NO public endpoint: admin fulfillment/rejection remains deferred to the Support Agent Console per ADR-0018; this is the store capability that console will invoke. Verified via new store regression test `TestRejectPayoutRequest_RestoresBalancesAndLedgers` covering balance restoration (60→100), status/reason persistence, ledger entry presence, and double-reject CAS failure.
* **Verified GetJob Job-ID Resolution Fix & Map Hydration Contract (user_token Alias Collision)**: `GET /users/jobs/get` previously resolved the job ID from the `user_token` query param FIRST (falling back to `id`). The Flutter map-tracking provider sent both (`?id=<jobID>&user_token=<JWT>`), so the JWT string was looked up as a job ID and customer map hydration always received 404. The backend now resolves the job ID solely from `id` (`services/user-service/internal/handlers/jobs_handlers.go`); party authorization continues to require `requester_id`/`requester_token`. `MapTrackingProvider.hydrateCustomerJob` updated to send `{id, requester_id}` — matching the contract already used by job-status polling. Verified via new regression test `TestGetJob_UserTokenParamDoesNotShadowJobID` (pre-fix literal failure: "expected 200 when id and user_token are both supplied, got 404 {"error":"job not found"}"; post-fix asserts the corrected contract plus legacy-combination safety) and `flutter analyze` (0 issues) + `test/map_tracking_test.dart` (6/6 pass).
* **Verified CreateTicketDialog Ticket ID Display & Snackbar Ordering Fix**: Two defects in `frontend/lib/widgets/create_ticket_dialog.dart`'s success path. (1) The dialog read `res['id']`, but chat-service serializes `ComplaintTicket.ID` with JSON tag `ticket_id` — every success toast displayed "Ticket ID: " with an empty value. Now reads `res['ticket_id']`. (2) The dialog called `Navigator.pop(context)` BEFORE `ThemedSnackBar.showSuccess(context, …)`, deactivating the context so `ScaffoldMessenger.of(context)` resolved against an unmounted element and the success snackbar never rendered at all (for real users, not only tests); the snackbar now shows before pop. Verified via new widget test asserting the snackbar text contains the backend-returned `ticket_id` (`flutter test test/create_ticket_test.dart` → 6/6 pass; pre-fix literal failure: "Found 0 widgets with text containing Ticket ID: ticket-999"), plus `flutter analyze` (0 issues).
* **Verified Cancellation Reason in Customer Job History DTO**: `CustomerJobResponse` (`services/user-service/internal/models/models.go`) omitted `cancellation_reason`, so cancelled orders in the Flutter "My Orders" screen (which reads `job.cancellation_reason` from this endpoint's payload) could never display why an order was cancelled — contradicting the Phase 13 status claim. Added the field with `json:"cancellation_reason,omitempty"` and mapped it in `NewCustomerJobResponse` from the underlying Job record (the customer's own job, so no disclosure concern). Verified via new unit test `TestNewCustomerJobResponse_IncludesCancellationReason` — pre-fix literal failure: "expected cancellation_reason in marshaled DTO" with the JSON body shown; post-fix passes. Full user-service module suite passes.
* **Verified WebSocket Origin Policy Decoupling (Non-Browser Clients + Configurable Flutter Origin)**: Two coupled availability fixes for realtime channels. (1) `chat-service` (`services/chat-service/internal/handlers/chat.go`) enforced strict `Origin == allowedOrigin`, rejecting requests with NO Origin header — which is what non-browser clients (dart:io `WebSocket` on Android/iOS, CLI tooling) send — making mobile WS connections dependent on forging a header. Extracted the decision into `isOriginAllowed`: empty Origin admitted, present Origin must match exactly. Verified via new table test `TestIsOriginAllowed` (empty/match/scheme-mismatch/suffix-spoof/foreign). (2) The Flutter client hardcoded `headers: {'Origin': 'http://localhost:3000'}` in `ChatProvider` and `MapTrackingProvider`; setting the server's `ALLOWED_ORIGIN` in production would break every handshake with no client-side remedy. Replaced with `chatWsOrigin` (`String.fromEnvironment('CHAT_WS_ORIGIN', defaultValue: 'http://localhost:3000')`) in `frontend/lib/core/constants.dart`, pairable at build time via `--dart-define`. NOTE: end-to-end production pairing is NOT device-verified; verified via `go test ./...` in chat-service (100% pass), `flutter analyze` (0 issues), and full `flutter test` (289/289 pass).
* **Verified Customer Role Alias on Job History & Auth Comparison Hardening**: (1) `GET /users/jobs/mine` accepted only JWT role `"user"`, returning 403 to customers holding valid `"customer"`-role tokens issued by other flows; both aliases are now accepted (verified via new regression test `TestGetCustomerJobs_AcceptsCustomerRoleAlias` — pre-fix literal failure: "expected 200 for role=customer on /users/jobs/mine, got 403"). (2) auth-service hardening: the `X-Gateway-Secret` comparison in `getClientIP` now uses `subtle.ConstantTimeCompare` instead of `==` (behavior-preserving; no observable test possible), and `authenticateReviewer` gained the same empty-secret precondition as `GetUser`, since `ConstantTimeCompare` over two empty byte slices returns 1 and would authenticate any caller if the configured internal token were ever empty (config.Load rejects empty tokens at startup; this is defense-in-depth). Full auth-service and user-service suites pass.
* **A3-P3 rating_screen Coverage & Two 360dp Layout Bug Fixes**: First widget suite (6 tests) for the previously 0%-covered screen caught two real production layout bugs at its own target width: star-selector RenderFlex overflowed 38px right (five ~60px-wide IconButtons vs ~264dp card interior — now constrained to visual 44dp squares) and mobile info-cards overflowed bottom (~84px aspect-forced tiles under ~110px content — mobile path now intrinsic-height Column, wide path unchanged). Also routed the CTA through existing-but-unused ARB key `ratingSubmitBtn` (PrimaryButton `text:` was an A2 scanner blind spot) and replaced Stitch-mock party defaults with role-neutral labels for the auth-hydration race edge. Coverage: rating_screen 0.0%→92.5%, overall audited 73.2%→75.2%, suite 355→361. Test-infrastructure lessons encoded in the suite: zero-duration pump() never advances route transitions inside FakeAsync (use pumpAndSettle), tester.view must be pinned to 360x800 for mobile-designed screens, and scrollUntilVisible is required for below-fold CTAs. Verified: analyze clean, full suite 361/361 local; CI Gate run 32572993973 success on push ``734a86a03ef0a72f9d286f327201d5c0a215bb98`` with literal log line `🎉 361 tests passed.` (Code commit ``c133f0e4fa32915a7892013f7c4c204b19f9163d``.)
* **l10n Scanner text: Gap Closure — 39 Strings / 11 Files**: Widened `scripts/frontend_l10n_audit.py` with the `text:` property (PrimaryButton/SecondaryButton/Tab/_buildFeatureRow class) that had let hardcoded CTAs survive A2 despite existing ARB keys (e.g. 'Submit Rating' vs unused `ratingSubmitBtn`). Remediated 39 strings across 11 files: 9 exact-value key reuses + 24 new en+ar keys (subscription free/pro feature lists, job-status CTAs, employee tabs, booking/kyc/dialog buttons). Post-fix audit: zero non-exception flags. Hydration-race scan follow-up: rating_screen confirmed as the SOLE stuck-placeholder instance — all other screens read auth reactively at build or via post-frame refresh. Verified: analyze clean, full suite 361/361 local; CI Gate run recorded success on the push of ``0adc1b242fd84bf81ad7ac5059ed748dc46b66b5``. (Commit ``018c79551f978b3b67b7346b53ee08599d80d515``.)
* **A3 Test Coverage Pass — P1 Money & P2 Auth Closed**: Built re-runnable coverage auditor (`scripts/frontend_coverage_audit.py`, risk-tier lcov parser) plus reusable test infrastructure: dart:io HttpClient mock via HttpOverrides (request recording, canned routing — zero production changes; ApiClient constructs `HttpClient()` directly so the override seam works) and a FlutterSecureStorage method-channel in-memory mock. Corrected stale prior flags: logout_helper measures 100% and chat_screen 81.5%; the only true zero-coverage file is rating_screen (P3, deferred with reason). Coverage deltas: reconciliation_provider 2.9%→100%, employee_jobs_provider 4.3%→94.3%, marketplace_provider 18.6%→89.0%, auth_provider 25.1%→75.8%, core/api_client 24.7%→63.9% (401 single-flight refresh proven via gated concurrent requests, 426 onUpdateRequired hook asserted); overall audited 69.4%→73.2%; suite 305→355 tests. Behavioral find documented as contract: ALL auth flows surface backend error strings verbatim (safe by endpoint contract), unlike other providers' friendlyErrorMessage mapping — tests pin this to prevent drift. Deferred Still-OPEN: map_tracking_provider WS fake layer (P3), notifications/chat providers + data models + service_screen (P4). Verified locally: flutter analyze No issues found!, full suite 355/355 pass, literal before/after coverage table in changelog. (Code commit ``84bd5aa447099d7cd7bb0a2b26650feb3ebe26d9``, changelog commit ``9edb67e1c74cc8ba1be6825f74462c5a4d65a3ff``.)
* **Verified Golden Baseline Environment Contract & CI Green Restoration**: Root-caused every historical "Flutter Lint & Test" golden failure as a rendering-environment mismatch: baselines generated on Fedora 44 / Flutter 3.44.6 were compared in CI under an UNPINNED stable SDK (stable-3.47.1 at failure time) on ubuntu-24.04 — constant engine-version pixel diffs, byte-identical across unrelated commits because zero baseline bytes changed. Fixed by pinning `ci.yml` to `flutter-version: '3.44.6'` with an explanatory comment, adding manual `regen-goldens.yml` (renders baselines in the SAME pinned comparator env, uploads reviewed artifact, least-privilege), and preloading bundled Poppins via `FontLoader` scoped to the golden suite setUpAll (global `flutter_test_config.dart` binding-init reverted after it broke out-of-zone HTTP-client suites). Diagnostic proof: post-pin CI-rendered artifact was sha256-identical to all 12 existing baselines — engine version was the sole variable; no baseline commit needed. Standing rule documented (README/STATUS): any Flutter bump regenerates goldens in the same change, with WHY stated. Fallback tolerance comparator explicitly NOT used — exact match achieved. Verified IN GITHUB ACTIONS (not local only): run 32555159762 on ``e596b8092a03ed93952877ad718862764690386c`` all 8 jobs success with literal log line `🎉 305 tests passed.`; run 32555473575 on ``2f8af6d189bab7b397050dc4074916cf17628121`` "Flutter Lint & Test" success. Local suite 305/305 additionally confirmed. (Docs commit ``ee62e92091c7896a5adc02d783fb0074f7155128``.)
* **A2 Complete Frontend l10n Sweep (Full Audit Pass)**: Built re-runnable audit `scripts/frontend_l10n_audit.py` (multiline-aware full-text scanner over `frontend/lib/**` excluding l10n infra/theme) auditing string literals at Text/Text.rich/uppercaseLabel/provider-error/named-property call sites. Pre-fix: 241 literals audited, 202 flagged (168 real fixes + 34 exceptions); manual inspection beyond the scanner found 29 validator `return "..."` literals, 4 StatCard `value:` unit strings, 5 ternary-branch copy strings, and 2 KYC subtitle ternaries the same-line heuristic missed — all also fixed. Added 185 ARB keys (`app_en.arb` + Egyptian colloquial `app_ar.arb`) via idempotent injector `scripts/add_a2_l10n_keys.py`, ran `flutter gen-l10n`, replaced all flagged call sites across 27 screens/widgets/providers with `context.l10n`/`l10n` lookups preserving exact English output. Removed two dead provider-layer GPS error strings never rendered by any UI path (localized banners already cover those states). Post-fix audit: **33 flagged = exactly the documented accepted exceptions** (brand names 'Quick Delivery'/'QD', 4 `#QD-<id>` tracking badges, 27 debug-only component-library strings). Also root-caused a guaranteed golden failure: notifications fixtures used fixed timestamps against the real system clock (broken every day after generation date) — added injectable `clock` to `NotificationsScreen`, pinned it in the harness, and regenerated baselines with the full-file command (documented capture-ordering caveat). Verified via `flutter analyze` (No issues found!), `dart format --set-exit-if-changed lib/` (clean), composition gate (pass), full suite `flutter test` (305/305 All tests passed), and post-fix audit run showing only the 33 accepted exceptions.
* **Verified Collision-Proof Ledger & Record ID Generation (tx-%d Nanosecond Collisions)**: All UnixNano-based persisted record IDs in `user-service` (`tx-%d` ledger entries across Deposit/LockEscrow/ReleaseEscrowWithSplit/CreatePayoutRequest/RefundEscrow/RollbackEscrow/RejectPayoutRequest, plus `payout-%d`, `rate-%d`, `sub-%d`) collided whenever concurrent operations read the clock in the same nanosecond; since `TransactionLedger.ID` maps to `_id`, losing inserts aborted with duplicate-key errors that several write paths only log — silently dropping audit entries for real money movements. Replaced all 12 sites with crypto/rand-backed IDs (`newRecordID(prefix, suffix)` in store; `generateID()` for rating/subscription IDs). Reproduced pre-fix via `TestLedger_ConcurrentDepositNoIDCollision`: 79–101 duplicate tx IDs per 20000 concurrent generations (literal output in docs/changelog/bug-fixes.md); end-to-end layer seeds 400 concurrent deposits against live MongoDB asserting one persisted ledger entry each. Post-fix 5/5 consecutive runs pass; full user-service suite passes. (Commit `a2e20e55c9771a31ae29deee29bce5dfe72d07b2`).
* **Verified RefundEscrow Fallback-Path Compensating Reverts**: The non-transactional fallback in `MongoDB.RefundEscrow` (`user-service`) previously executed job CAS → wallet CAS → ledger insert with no undo between steps; a mid-sequence failure left the job cancelled with its escrow lock deducted and (on ledger failure) funds moved to withdrawable with no audit trail. Added `ReleaseEscrowWithSplit`-style compensating reverts gated to fallback mode (prior job status captured pre-mutation; job-only revert on wallet-step failure; wallet+job revert on ledger-step failure). Deterministic repro via namespace-rebind injection in `TestRefundEscrow_FallbackLedgerFailureRevertsFunds`; pre-fix literal failures recorded in docs/changelog/bug-fixes.md, post-fix 5/5 pass. (Commit `78d4d144a59ecaa7f0745f638bcf9ecaf3f1461a`).
* **Verified COD Actual-Cash Capture & Phantom Fee Display Removal**: `CompleteJob` (`user-service`) previously never recorded how much COD cash was actually collected (only a recomputed price estimate was logged) and displayed a percentage-based platform fee/net split on non-COD completions that contradicted `ReleaseEscrowWithSplit`'s real 100% zero-commission credit whenever `platform_config` carried a legacy non-zero value. COD completions now persist cent-rounded `actual_cash_amount` atomically with the status flip and echo it as `total_amount`; non-COD responses report `platform_fee: 0` / `net_to_tenant == released amount`, matching actual fund movement. Adds `store.UpsertPlatformConfig`. Verified via two regression tests with literal pre-fix failures recorded in docs/changelog/bug-fixes.md; full user-service suite passes. (Commit `0f031bf2a21a72515a367d86b589b4be54bf8d76`).
* **Verified Money-Precision Cent Boundaries & Explicit Minor-Units Deferral**: Client-supplied money entered user-service's financial pipeline as raw float64 with sub-cent residue (proposals, agreed prices, deposits, payouts — e.g. deposit `10.99999999` persisted verbatim; payouts logged rounded via `%.2f` while deducting unrounded). Added `roundMoney()` applied at all five ingestion boundaries (TrackJob proposal, ProposePrice, RespondPrice active price, WalletDeposit, RequestPayout). Full integer-minor-units migration is explicitly DEFERRED with rationale (cross-contract DB/API/frontend migration); covered-vs-deferred audit recorded in docs/changelog/bug-fixes.md. Verified via five regression tests with literal pre-fix failures; full suite passes. (Commit `09ad08bcbff6a74ad3b7a34c6ee8f72f29b822ce`).
* **Verified Authentication-Before-Throttle-Reservation in UpdateJobLocation**: The Redis throttle reservation (Lua EVAL on `loc:inflight:<job_id>`) ran before requester JWT resolution, letting unauthenticated callers holding a job ID continuously claim the reservation window and starve the assigned employee's updates with 429s. Auth now precedes the reservation; verified via hook-instrumented regression test (`TestUpdateJobLocation_NoThrottleReservationBeforeAuth`, pre-fix literal failure recorded in docs/changelog/security-fixes.md) plus full suite pass. (Commit `1ff251a8b1fe323e973d7a33ce6356d97a421dfd`).
* **Verified Subscription POST Rate Limiter & Stable Upsert ID**: `POST /users/subscription` was the sole state-changing endpoint without a per-endpoint limiter (flood repro: 31 calls, zero 429s), and its store upsert regenerated `_id` per call so repeat tier changes failed with immutable-field 500s. Added `user:subscription` limiter (30/min, identity-keyed, post-auth) and ID-preserving upsert. Verified via flood regression with literal pre-fix failure in docs/changelog/security-fixes.md; full suite passes. (Commit `5cddfb2e92f3071fad74716975d4a2661c6a2be7`).
* **Verified Server-Side Pagination for GetLedger & GetRatings**: Both read endpoints previously serialized every matching document (unbounded-response DoS surface). Now paged server-side (ledger 100/page cap 500; ratings 50/page cap 200) with clamped `?limit`/`?offset` client tuning and echoed page metadata; backward-compatible response keys. Verified via pagination regression tests with literal pre-fix failures recorded in docs/changelog/security-fixes.md; full suite passes. (Commit `617e26396b9f1e34e6269cc56e7b39834ae2ebb9`).
* **Verified HTTP Server ReadTimeout & IdleTimeout Across All 5 Microservices**: All servers previously capped only request headers (3s); bodies could trickle indefinitely and idle keep-alives were never reaped. All 5 `cmd/main.go` files now set `ReadTimeout: 30s` + `IdleTimeout: 120s`; `WriteTimeout` deliberately unset to preserve SSE/proxied streaming. Verified via literal grep of all five server literals plus build/vet/test. (Commit `f4c29f1b8d102d95d471f2851db810c0d17d85ea`).
* **Verified Account-Enumeration Oracle Closure (Login + ToggleEmployee)**: Login's user-not-found path performed zero bcrypt work (timing oracle ~0ms vs ~45ms); ToggleEmployee leaked account existence through distinct error wordings ("owner does not exist" vs "password does not match") and a 404-vs-400 status split for nonexistent vs foreign-tenant employees. All three closed: dummy-bcrypt timing parity on not-found paths, byte-identical failure responses per resource type. Verified via regression tests incl. measured timing parity (avg ~45ms both paths); two vacuous-repro traps (KYC-gate short-circuit, IP-lockout interaction) documented in docs/changelog/security-fixes.md; full auth-service suite passes. (Commit `d0cb4b2a0d938dd778dbbb8fb22d9bd01085361d`).
* **Verified Reviewer & Support-Agent Token Hashing at Rest**: Administrative bearer tokens were stored plaintext in both auth-service (`reviewers`) and chat-service (`support_agents`), with `SupportAgent.Token` also serialized via a live json tag. Both stores now persist SHA-256 digests (lookups hash first, one-time legacy fallback for migration); agent token is `json:"-"`. Verified via raw-document regression tests with literal pre-fix failures in docs/changelog/security-fixes.md; both service suites pass. (Commit `dba25a27aab3c21adf99de6e4458bfb41206d5d1`).
* **Reviewed Decision: ToggleEmployee Owner Re-Authentication Password in Request Body (Accepted)**: The freeze/unfreeze flow requires owner re-authentication (`owner_password` in the request body, bcrypt-verified server-side). Removal would require a coordinated backend+frontend contract change (step-up token flow) breaking the deployed pairing mid-parallel-development. Accepted with rationale: transported over TLS/mTLS only, never logged (audited all ToggleEmployee log lines — emails only), never persisted, never serialized back out, and now oracle-free after commit `b5ae0789...`. Flagged as future hardening: replace body password with a short-lived step-up token issued by a dedicated re-auth endpoint.
* **Verified FCM Client Stops Sending X-Internal-Token Externally**: The FCM push client attached the internal service-auth header unconditionally, including to the default fcm.googleapis.com endpoint. Header now gated on an explicit `FCMEndpointURL` relay being configured. Verified via white-box interception regression with literal pre-fix leak output in docs/changelog/security-fixes.md; full notification-service suite passes. (Commit `9167d87485430d872bbe24b28241ec356808a6a0`).
* **Verified Semver Prerelease Parsing in Client Version Gate**: `ParseSemVer` (`api-gateway`) failed on `-prerelease` suffixes (patch component `"3-beta"` broke `strconv.Atoi`), rejecting prerelease client builds at the `VersionGate`. Prerelease segment now stripped after build metadata; numeric core drives comparison. Verified via table regression with literal pre-fix failures; api-gateway suite passes. (Commit `218748d7100e7fce5d8b39b4fdeaad81810ad173`).
* **Verified [TRAFFIC] Access-Log Injection Sanitization (api-gateway)**: The decoded request path was interpolated verbatim into `[TRAFFIC]` lines under an incorrect `#nosec G706` claim; encoded `%0A`/`%0D` forged extra log lines. CR/LF now replaced before interpolation with corrected rationale. Verified via log-capture regression with literal pre-fix injection output; api-gateway suite passes. (Commit `9ab8ef3d9269ec95fe511233f5531165319e1488`).
* **Verified Device-Token Platform Whitelist**: `POST /auth/device-token` persisted arbitrary platform strings verbatim. Only `android|ios|web` accepted (empty keeps legacy android default); others rejected 400 pre-write. Verified via regression with literal pre-fix acceptance of `"windows-phone;DROP"`; auth-service suite passes. (Commit `2bd3755d99f094db5539a88003e982726bc87922`).

### Backend-Lows Pass — Explicitly Deferred Items (2026-08-22)

Deferred from this pass with rationale (all previously flagged lows):
* **Audit-log pagination (auth-service)**: same pattern as ledger/ratings pagination (`c6184303...`); mechanical follow-up.
* **Chat token-cache bound & stale comment**: cache is unbounded with a comment claiming different TTL; needs bounded-eviction design, not a drive-by.
* **[EXPLICIT FOLLOW-UP — CODE] Remove the retry loop in `TestComplaintRoutingConcurrency` (`services/chat-service/internal/handlers/chat_test.go:390-398`)**: this is real dead/misleading test code, separate from the doc correction recorded above (commit `8bf1b27...`). The 3-attempt loop with `time.Sleep(10 * time.Millisecond)` around `CreateTicketAndAssign` can mask genuine transient store failures inside a concurrency test whose agent routing is already CAS-guaranteed by `FindOneAndUpdate`. Removal must be done as its own change: repro-first evidence that the underlying failure class the loop was misdiagnosing no longer exists (unauthenticated connection ordering was fixed in `cc54b63...`), then single-call execution restored, then `-race -count=20` verification per the original entry's methodology.
* **NDJSON frame batching**: investigation found coalescing ALREADY implemented (`chat.go` write path queues frames joined by `\n`) — reported as satisfied rather than fixed.
* **SSE heartbeat / Last-Event-ID support (notification-service)**: genuine feature work (replay semantics, per-client event IDs) requiring design; not a low-risk patch.
* **hub.Run() shutdown path (chat-service)**: interacts with ADR-0005 Redis self-delivery dependency; deserves its own ADR addendum.
* **BroadcastLocation channel-prefix validation**: inbound calls are gated by `X-Internal-Token`; residual risk documented (internal callers can fan out to arbitrary channels). Suggested follow-up: strict `job:<id>`/`fleet:<id>` format validation in chat-service.
* **Query-string escaping in inter-service URLs**: all interpolated values are currently server-derived (JWT-resolved IDs); hardening sweep to `url.Values.Encode()` recommended but no exploitable path identified today.
* **XFF right-to-left parsing (gateway)**: current first-entry parsing is spoofable when behind a trusted proxy chain; correct fix requires a trusted-proxy-count policy decision — flagged for an ADR-level change, not a drive-by flip.
* **Request-body size caps beyond auth-service**: MaxBytesReader exists for auth uploads/profile; extending across all endpoints is a cross-cutting contract review.
* **Admin version-config credential model & version-store persistence (main.go:68)**: both require product decisions (dedicated admin credentials; Mongo-backed store enablement) before code changes.
* **Encrypt-documents rotation safety**: tool double-encrypts after key rotation (no magic-header marker); fix requires an on-disk format marker plus migration guidance.
* **Encrypt-documents rotation safety**: tool double-encrypts after key rotation (no magic-header marker); fix requires an on-disk format marker plus migration guidance.
* **Storage prefix separator guard**: classic incomplete-prefix pattern (`absBase` without trailing separator) — traversal itself is blocked twice; low-impact guard worth adding in a future touch of `storage.go`.

### Parallel-Session Infrastructure Isolation (Operational Lesson, 2026-08-22)

During this pass run in parallel with a frontend session, the shared `saas-mongo` container crash-looped (observed `RestartCount=44` in one day), producing `connection reset by peer` / server-selection-timeout failures inside test setup that mimic code flakes. **Standing guidance for any future parallel sessions**: give each worktree/session its own MongoDB and Redis containers (distinct host ports or separate docker-compose project names) instead of sharing one stack. When integration tests fail with transport-level errors against shared local infra, check `docker inspect -f '{{.RestartCount}}'` for the container FIRST and classify as environmental before treating it as a regression.

### Backend-Lows Pass Closure (2026-08-22)

Closed with CI verified on GitHub Actions: 16 code/decision items + doc correction landed (see delivery log in session report); Priority 1–3 fully closed, Priority 4 explicitly deferred above except the four quick wins fixed this pass (FCM header gating `9167d87...`, semver prerelease `218748d...`, TRAFFIC sanitization `9ab8ef3...`, device-platform whitelist `2bd3755...`). Final tip `bdf4bd5d773ef7088fe93555554038a8f2fd6093`: CI Gate run `32558009180` success (all jobs: Lint & Formatting, chat/user/notification/api-gateway/shared-infra Build & Test, Flutter Lint & Test).
* **Verified Rejected-Payout Compensating Revert (QA Audit Q2 Fund Stranding)**: `RejectPayoutRequest` consumed its status CAS before the wallet restore; restore failures stranded deducted funds permanently (stuck `rejected`, retry refused, no refund ledger entry). Restore failures now compensate the status flip back to `requested` via guarded CAS so rejections stay retryable; compensation failure logs CRITICAL. Found by independent QA audit, reproduced live pre-fix; regression test covers fail->revert->recover->reject-retry arc plus unchanged double-reject protection. Verified 5/5 runs + full user-service suite. (Commit `03d88c370cc665720208a106438bf6b8ff072d11`).
