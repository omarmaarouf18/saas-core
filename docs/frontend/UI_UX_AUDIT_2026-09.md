# UX Audit — Weak Areas Mapped to Named Design Patterns (2026-09)

> **Date**: September 25, 2026
> **Branch**: `logic-exploitation` @ `44734da...` (HEAD verified via `git rev-parse` this session)
> **Method**: Static read-through of actual widget code. No runtime, no `flutter test`, no CI — consistent with how `UI_UX_BEHAVIOR_AUDIT.md` was produced. Every finding cites a verified `file:line` range read this session; line numbers were spot-checked against the working tree before publication.
> **Scope**: 32 files under `frontend/lib/screens/` (verified via `ls` this session) = **31 production screens** + `component_library_screen.dart` (debug-only, excluded). Note: the task brief's screen list omits `job_status_screen.dart`; it exists in the tree and was audited with the customer group. `service_screen.dart` remains deleted (consolidated into `owner_configuration_screen.dart` per `a486414...`; zero references reconfirmed via grep this session) and was not audited.
> **Audit-only**: no application code was written, edited, or fixed for this task.

---

## 1. Non-duplication discipline (what was deliberately NOT re-reported)

All three prior audit docs were read first. The following are already tracked and are **excluded** below even where still open:

- `UI_UX_AUDIT.md` — all token/typography/spacer/RTL/l10n findings and their §7 remediation log (B1-F1, B4-F1, E2-F1, E4-F1, A3-F1, raw-button, scrim-token fixes).
- `UI_UX_BEHAVIOR_AUDIT.md` — every `C-01..C-11`, `O-01..O-07`, `E-01..E-09`, `X-01..X-03` symptom, including open-but-tracked items (my-account C-08/C-09/C-10, logout C-11, owner-config O-03/O-04, roster-tap O-05, wallet UTC O-06, payout-refresh O-07, notifications copy E-02, toast copy E-03, escrow-as-distance E-04, availability-toggle debounce E-05, telemetry E-06/E-07, COD $0.00 E-08, employee-rating entry E-09, raw spinners X-01, RTL arrow X-02, map touch targets X-03).
- `UI_UX_ENTERPRISE_ROADMAP.md` — Phases 0–6 completions (skeletons on marketplace/home/employee-jobs/wallet, empty/error/success states, motion, hierarchy, V2 decluttering).
- `STATUS.md` Known Gaps — A4 double-submit coverage (incl. settings-logout lock, benign read-only refresh buttons, dead `FormScreenTemplate` path), A5 error-handling deferrals (map-hydration silence, job_status poll silence, silent-empty dashboard/audit sections, no `runZonedGuarded`, 3 hardcoded-EN success strings), A6 teardown, mock-reply placeholder, notification deep-link bootstrap stub, V1/V2 overflow fixes, KYC scroll fix, dark-mode hardening, OTP LTR pinning, EMPLOYEE_FUNCTIONALITY_AUDIT items, ADR-0025 schedule editor, ADR-0027 cancellation-request flow, fleet-map names fix.
- Fixed-in-history symptoms (verified via `git show --stat` this session): fake search box (`b39a28a...`), filter sheet (`7eefbcf...`), Cairo-default location (`8fdefe8...`), clear-all-notifications gateway (`df2f933...`).

Severity scale matches prior audits: **P0** = lost money/data or broken core flow; **P1** = confuses/blocks users regularly; **P2** = inconsistent/unpolished but works; **P3** = cosmetic/minor. No P0 was found; 11 P1, 39 P2, 12 P3 (62 findings).

Pattern names are the exact established names from the brief: Skeleton Screens, Empty State, Inline Error with Retry, Confirmation Dialog, Inline/Live Validation, Optimistic UI, Progressive Disclosure, consistent dismiss pattern, Progress/Status Indicator, 44×44 touch target / thumb zone, one focal element, single in-sync entry point.

---

## 2. Findings table

| # | Screen (file) | Location (line range) | Pattern Violated | Established Pattern to Apply | Severity | Suggested Fix (concrete, one paragraph) |
|---|---|---|---|---|---|---|
| A1 | login (`login_screen.dart`; provider `auth_provider.dart`) | Screen 60–65; provider catch stores `_error` at 193–196 | Transient snackbar-only error | Inline Error with Retry | P1 | Add a persistent `ThemedErrorBanner(message: auth.error!, onRetry: _submit)` above the email field mirroring `forgot_password_screen.dart:137–143`, keeping the snackbar (which already passes `onRetry: _submit`) only as a transient supplement; today wrong-password/lockout explanations auto-dismiss in ~2s with no in-place reference, while the newer reset screens prove the inline pattern is the codebase standard. |
| A2 | signup (`signup_screen.dart`; provider `auth_provider.dart`) | Screen 59–64; provider catch at 158–161 | Transient snackbar-only error | Inline Error with Retry | P1 | Add a persistent `ThemedErrorBanner(message: auth.error!, onRetry: _submit)` above the username field mirroring `forgot_password_screen.dart:137–143`; signup failures (duplicate email, weak password, role mismatch) currently survive only in a 2s snackbar with no persistent in-place message, unlike the inline banners on all three reset screens. |
| A3 | otp (`otp_screen.dart`; provider `auth_provider.dart`) | Screen 53–58 (resend) + 81–86 (verify); provider catches at 233–236, 253–256 | Transient snackbar-only error (both paths) | Inline Error with Retry | P1 | Add a persistent `ThemedErrorBanner(message: auth.error!, onRetry: _submit)` (and for resend, `onRetry: _resendCode`) inside the OTP card above the PIN input, mirroring `reset_password_otp_screen.dart:170–179`; both verify and resend failures currently surface only via auto-dismissing snackbars, so expired-code and 429 lockout text disappears before the user can read the wait time or retry in place. |
| A4 | kyc upload (`kyc_document_upload_screen.dart`; provider `auth_provider.dart`) | Screen 67–70 + 627–631 (no error read); provider catch at 98–100 does `debugPrint` only, never stores `_error` | Swallowed error (never stored / never read) | Inline Error with Retry | P2 | Expose the profile-refresh failure (store it in the provider or return a bool/error from `fetchUserProfile`) and render a retryable `ThemedErrorBanner(message: ..., onRetry: _refreshUserData)` at the top of the KYC body; today both the post-frame auto-refresh (67–70) and the AppBar refresh button (627–631) fail with zero UI signal, leaving a stale KYC status with no retry path, while per-slot upload errors correctly use inline banners with retry at 523–536. |
| A5 | reset new (`reset_password_new_screen.dart`) | 66–72 (failure path only `setState`) + 135–143 (banner with `onRetry: null`) + 120–121 (`isVerificationFailure` def) | Error shown with no recovery action | Inline Error with Retry | P1 | When `_missingToken \|\| lastErrorStatusCode == 401`, keep the `resetSessionExpiredMsg` banner but add an explicit next action — a `SecondaryButton` "Back to email step" that pops back to (or pushes and clears to) `ForgotPasswordScreen` — instead of `onRetry: null`; today a user who sits past the 10-minute token clock taps Submit, gets "session expired, restart" with no restart button, and must discover the manual double-back themselves. |
| A6 | reset otp + reset new footers (`reset_password_otp_screen.dart`, `reset_password_new_screen.dart`) vs forgot password (`forgot_password_screen.dart`) | Outliers 306–322 + 239–255; majority pattern 218–234 | Same link, different destination | Single in-sync entry point (consistent nav copy/behavior) | P1 | Change the footers on screens 2–3 off the `"${signupHasAccount} ${signupSignIn}"` ("Already have an account? Sign In") copy — which on screen 1 correctly `pop()`s to Login but on screens 2–3 only `pop()`s one reset step backward — to a "Back" link (back icon + step-specific label such as "Back to email step" / "Back to code step"); keep the "Sign In" copy exclusively for navigation that actually lands on Login. |
| A7 | otp (`otp_screen.dart`) vs reset otp (`reset_password_otp_screen.dart`) | 291–294 (`onCompleted: _submit`) vs 203–208 (`onCompleted` only `didChange`, no verify call) | Same 6-digit verify, different completion contract | Single in-sync entry point | P2 | Pick one PIN-completion contract and apply it to both screens — preferably auto-verify on complete in the reset screen too (`onCompleted: (_) => _verifyCode()` guarded by the existing `PrimaryButton` in-flight lock) — since users trained by the login OTP auto-submit will paste/type 6 digits on the reset screen and wait for something that never happens until they find the Verify button. |
| A8 | otp (`otp_screen.dart`) vs reset otp (`reset_password_otp_screen.dart`) | 352–359 (`SecondaryButton(isOutlined: true, icon: refresh)`, full-width) vs 237–243 (filled `SecondaryButton`, `icon: send_outlined`, `Align(centerEnd)`) | Two equally-weighted CTAs | One focal element (demoted secondary styling) | P2 | Make the reset-screen resend match the OTP-screen resend — `isOutlined: true`, `icon: Icons.refresh`, same full-width placement — so Verify stays the single amber focal CTA; today the reset screen stacks two filled-amber buttons (Primary Verify at 225–231 plus filled Secondary Resend at 237–243) with identical weight competing as focals, while the login OTP screen correctly demotes Resend to outlined. |
| A9 | kyc upload (`kyc_document_upload_screen.dart`) | 557–566 (per-slot `PrimaryButton`) + 545–569 (slot switcher) + 670–676 (all 3–4 slots at once) | 3–4 identical full-width Upload CTAs | One focal element + Progressive Disclosure | P2 | Demote to one focal action per screen state — render `PrimaryButton` only for the first incomplete slot and `SecondaryButton(isOutlined: true)` for the remaining slots (or collapse completed slots behind a details toggle) — since a fresh owner sees 4 identical full-width amber Upload buttons and a fresh employee 3, giving equal top weight to actions the user must do sequentially anyway. |
| A10 | kyc upload (`kyc_document_upload_screen.dart`) | 67–70 (`_refreshUserData`, no loading flag) + 627–631 (refresh `IconButton`, no busy state) + 633 (`onRefresh`, same silent call) | Silent refresh, no signal | Progress/Status Indicator | P2 | Add a local `_isRefreshing` flag set around `fetchUserProfile`, showing a small `ThemedLoadingIndicator` (or swapping the AppBar refresh icon for an 18px spinner and disabling the button) until the future completes; today tapping refresh — or the silent post-frame auto-fetch on mount — produces zero feedback, so the user cannot tell a slow fetch from a dead button, unlike every auth submit/resend path which wires `isLoading` to its button. |
| C1 | customer home (`customer_home_screen.dart`) | 817–837 (fetch at 45–51; zero `isLoading`/`LoadingIndicator`/`Skeleton` refs in file, verified by grep) | No loading state at all | Skeleton Screens / loading indicator | P2 | Add an `isLoading` branch rendering `ThemedLoadingIndicator` (or a compact activity-card skeleton) when `marketplace.isLoading && activeJobs.isEmpty && fetchError == null`, keeping the existing `ThemedErrorBanner` path for failures; today every cold start flashes the `customerJobsEmpty` card while `fetchCustomerJobs` is in flight, unlike the sibling marketplace screen which shows `MarketplaceCardSkeleton`. |
| C2 | ticket chat (`ticket_chat_screen.dart`; provider `chat_provider.dart`) | Screen 282–296; provider `fetchChannelHistory` at 63–93 | Dead loading branch | Progress/Status Indicator | P3 | Set a dedicated history-loading flag in `fetchChannelHistory` (or gate the loader on `messages.isEmpty` with no error yet): the `chat.isLoading ? ThemedLoadingIndicator` ternary can never show during history load because that method never sets `_isLoading` (only `createTicket`/`uploadTicketAttachment` do), so every ticket open briefly renders the empty-conversation placeholder before history arrives. |
| C3 | chat (`chat_screen.dart`) | 280–287 (connect starts only after history resolves, 55–59) | Missing loading state (empty flash) | Progress/Status Indicator | P3 | Track history-fetch in-flight explicitly (same flag as C2) and show `ThemedLoadingIndicator` while history loads with zero messages and no error, reserving the empty state for a genuinely empty thread; today during history fetch `isConnecting` is still false so `messages.isEmpty && isConnecting` is false and the `ThemedEmptyState` flashes on every open. |
| C4 | ticket chat (`ticket_chat_screen.dart`; provider `chat_provider.dart`) | Provider store at 83–85; screen reads only `subscriptionError` at 273 (lines 270–312) | Stored error never read | Inline Error with Retry | P2 | Add the same `chat.error != null && subscriptionError == null` → `ThemedErrorBanner(onRetry: _connectAndLoad)` branch above the message stream, mirroring `chat_screen.dart:262–266`; today `fetchChannelHistory` stores `_error` on any non-403 failure (timeout/offline) but the screen never reads it, so a failed history load renders the empty-conversation widget with zero error signal and no retry. |
| C5 | marketplace (`customer_marketplace_screen.dart`; provider `marketplace_provider.dart`) | Provider 160–170 (rethrow, stores nothing); screen catch at 1324–1330 only sets `_loading = false` (widget at 1313–1344) | Failure renders as "no data" | Inline Error with Retry (distinguish failure from zero-count) | P3 | Distinguish failure from zero-count: keep an `_error` flag in `_ServiceRatingWidgetState`, render nothing (or a subtle retry affordance) on failure instead of the "no ratings" label — today a ratings outage renders `noRatingsLabel` on every card, indistinguishable from a genuinely unrated service — and consider caching one rating fetch per tenant rather than per-card fire-and-forget. |
| C6 | rating (`rating_screen.dart`; provider `marketplace_provider.dart`) | Screen catch at 125–129 (lines 96–130); visualizer at 457–515; AppBar refresh at 204–208 | Status-check failure masquerades as "waiting" | Inline Error with Retry | P3 | Store the fetch error from `_checkOtherPartyRatingStatus` and render an inline retryable notice in `_buildBlindStatusVisualizer` when the status check itself failed (distinct from the legitimate not-yet-rated state), wiring the existing refresh action as its `onRetry`; today the visualizer shows "waiting for other party" on network failure and the refresh button retries blindly with no error text. |
| C7 | ticket chat (`ticket_chat_screen.dart`) vs chat (`chat_screen.dart`) | 192–199 vs 99–107 (`onRetry: _sendMessage` present only in job chat) | Send failure without one-tap resend | Inline Error with Retry | P3 | Pass `onRetry: _sendMessage` to the ticket-chat send-failure snackbar for parity with `chat_screen`; text is preserved in both paths, but ticket chat offers no one-tap resend while the identical job-chat path does. |
| C8 | customer tickets (`customer_tickets_screen.dart`) + job status (`job_status_screen.dart`) vs marketplace + cancel dialog | Outliers 51–54 and 1261–1266 (bare `showDialog`); majority `customer_marketplace_screen.dart:755–758` and `cancel_job_dialog.dart:39–41` (`barrierDismissible: false`) | Dismiss gesture contradicts app pattern | Consistent dismiss pattern | P2 | Add `barrierDismissible: false` to both `CreateTicketDialog` show sites (relying on the existing Cancel button for dismissal), matching the booking/cancel majority pattern for dialogs containing user input; today a tap outside discards typed subject/description with no confirmation. |
| C9 | job status (`job_status_screen.dart`) + customer jobs + customer job map | Primary 717–721; siblings `customer_jobs_screen.dart:343–351`, `customer_job_map_screen.dart:265–294` | Static text for a minutes-long background process | Progress/Status Indicator | P3 | Add a lightweight animated indicator (e.g. pulsing dot or animated ellipsis on the matching-courier rows and the map waiting notice) that displays only while status is `pending_dispatch`, cleared on assignment; today the unbounded dispatch-searching state is static italic/body text in all three places even though polling/reconnect activity runs underneath. |
| C10 | job status (`job_status_screen.dart`) | 434–440 (in `_buildUnavailableBusyCard`) and 1163–1171 (in `_buildActionButtons`) | Two identical retry CTAs, same handler/copy/flag | Single in-sync entry point | P3 | Keep one: remove the action-row duplicate (or the in-card one) so the unavailable state has a single retry CTA; both are currently in sync (same `_retryBooking`, same copy, shared `_isRetrying`), so this is visual/structural redundancy, not behavioral divergence. |
| E1 | employee roster (`employee_screen.dart`) | 274–279, 707–708 | Bare centered spinner, no skeleton | Skeleton Screens | P3 | Add `EmployeeRosterSkeleton` / `AuditRowSkeleton` via `ListScreenTemplate.loadingWidget` or inline `AnimatedSwitcher` matching `_buildEmployeeCard` / audit card bounds, keeping `RefreshIndicator` for subsequent pulls; sibling `employee_history_screen.dart:79–87` already supplies `EmployeeJobCardSkeleton`, proving the pattern. |
| E2 | owner history (`owner_history_screen.dart`) | 160–161, 310–311, 456–457 (all three tabs) | Bare centered spinner, no skeleton | Skeleton Screens | P3 | Reuse existing skeleton primitives (`EmployeeJobCardSkeleton` geometry for jobs/activity rows, `WalletScreenSkeleton`-style rows for ledger) as `loadingWidget`, reserving the spinner for pull-to-refresh; proven pattern already exists in `employee_history_screen.dart:79–87`. |
| E3 | recon queue (`owner_reconciliation_queue_screen.dart`; `list_screen_template.dart`) | 189–207 (no `loadingWidget`); template default at 236–241 | Template-default spinner | Skeleton Screens | P3 | Supply a reconciliation-card skeleton matching `_buildReconciliationCard` bounds as `loadingWidget`; today initial load falls back to `ThemedLoadingIndicator` while sibling `employee_history_screen.dart:79–87` supplies a card skeleton. |
| E4 | employee roster (`employee_screen.dart`) | 304–317 | Filtered-empty bare text, no way out | Empty State (icon + text + next action) | P2 | Replace bare centered `Text(noWorkersMatchFilter)` with `ThemedEmptyState` (icon `search_off`, existing keys) plus `actionText` clearing `_searchController` and resetting `_statusFilter='all'`; today a filtered-to-empty roster offers no icon and no exit. |
| E5 | owner history (`owner_history_screen.dart`) | 373–386 (vs full empty cards at 360–372) | Filtered-empty bare text | Empty State | P2 | Same fix as E4: `ThemedEmptyState` with a clear-filter action resetting `_jobsSearchController` + `_jobsStatusFilter`, consistent with the unfiltered empty cards in the same file. |
| E6 | recon queue (`owner_reconciliation_queue_screen.dart`) | 280–293 (vs full empty state at 220–228) | Filtered-empty bare text | Empty State | P2 | Same fix: icon + clear-filter action resetting `_searchController` + `_selectedCategory='all'`; the truly-empty queue already has a full `ThemedEmptyState`, so only the filtered branch lags. |
| E7 | fleet map (`owner_fleet_map_screen.dart`) | 449–475 (`_buildEmptyFleetNotice`; AppBar refresh at 131–140) | Empty notice with no action | Empty State (icon + text + next action) | P2 | Add a Refresh/retry affordance calling `hydrateOwnerFleet` (same as the AppBar refresh); the notice has icon + text but no action, and it matters because this same notice currently also renders on hydration failure (see E9). |
| E8 | employee roster audit tab (`employee_screen.dart`; provider `owner_provider.dart`) | Provider store at 211–213 (method 197–218); screen 703–792, esp. 707–729 (never reads `ownerProvider.error`) | Failed fetch masquerades as "no events" | Inline Error with Retry | P1 | Add `ThemedErrorBanner(message: ownerProvider.error, onRetry: _refreshAuditLog)` above the list, mirroring `owner_history_screen.dart:168–174`; today `fetchAuditLog` stores `_error` on failure but `_buildAuditTrailTab` never reads it — it shows loading then falls through to the empty-state card. |
| E9 | fleet map (`owner_fleet_map_screen.dart`; provider `map_tracking_provider.dart`) | Provider stores at 166–168 and 245/294/309, discards at 128–131; screen 142–236, 574–587 reads `isLoading/markersList/isConnected/subscriptionError` but never `provider.error` | Hydration failure shows "no employees"; WS error shows generic warning | Inline Error with Retry | P1 | Surface `provider.error` as a `ThemedErrorBanner` with retry (`hydrateOwnerFleet` / reconnect) distinct from the true-empty notice (see E7); surface or log the swallowed available-employees sub-fetch at 128–131 instead of bare `debugPrint`. Today hydration failure renders `_buildEmptyFleetNotice` ("no employees transmitting") and WS `_error` renders only the generic `reconnectingTrackingStream` warning. |
| E10 | owner config (`owner_configuration_screen.dart`; provider `owner_provider.dart`) | Provider store at 234–236 (method 226–241); screen 134–179, 473–519 (submit-only `_errorMessage` at 485–492) | Fetch failure renders stale/empty form silently | Inline Error with Retry | P2 | Add a fetch-error banner with retry (`_loadAndPrepopulate`) above the sections when services load fails and no existing service matched; today `_loadAndPrepopulate` awaits `fetchServices()` but never checks `ownerProvider.error`, so the form renders regardless with stale/empty controllers and no fetch-error path. |
| E11 | owner config (`owner_configuration_screen.dart`) | 330–359 (client-side checks) + 485–492 (banner with `onRetry: _submitForm`) | Retry that can never succeed | Inline Error with Retry (retry must re-fire network, not re-validate) | P2 | Move location/radius/price emptiness into per-field inline validators and reserve the banner + retry for actual network/API errors from `createService`/`updateOwnerServiceConfig`; today validation failures (location null, radius/base/price parse) set a generic `_errorMessage` whose Retry re-submits unchanged inputs that cannot succeed. |
| E12 | owner dashboard (`home_screen.dart`) | 1283–1319 (approve/decline buttons); handler at 114–136; contrast `owner_reconciliation_queue_screen.dart:76–88` and direct-cancel `home_screen.dart:1333–1355` | Approve/decline fires with no confirmation | Confirmation Dialog (destructive styled + consequence) | P1 | Wrap both decisions in `ConfirmActionDialog` stating the consequence (approve → job cancelled + escrow refunded; decline → courier remains assigned), with `isDestructive: true` on approve; today approve cancels the job and triggers refund logic on one tap while sibling money-moving flows require a dialog. |
| E13 | owner dashboard (`home_screen.dart`) vs recon queue | 1333–1355 vs `owner_reconciliation_queue_screen.dart:79–84` (success toast distinguishes refund vs plain cancel only after the fact, 1344–1351) | No upfront escrow consequence | Confirmation Dialog (consequence description) | P2 | Pass `bodyText`/`titleText` overrides (existing `CancelJobDialog` ADR-0027 params) including `lockedEscrowAmount` and a refund note, matching the recon dialog's amount/target/role message; the dialog collects a reason today but never states the money consequence upfront. |
| E14 | employee roster (`employee_screen.dart`) vs recon queue | 648–696 (submit fires `toggleEmployee`) vs `owner_reconciliation_queue_screen.dart:76–88` | No consequence explanation | Confirmation Dialog (consequence description) | P2 | After successful validation, show `ConfirmActionDialog(isDestructive: <freezing>, message: <freeze blocks login/dispatch immediately / unfreeze restores>)` before calling `toggleEmployee`; today freeze/unfreeze fires on form submit with `isDestructive` styling but no dialog — owner password re-auth provides friction, not an explanation (login blocked, dispatch hidden, how to reverse). |
| E15 | owner config (`owner_configuration_screen.dart`; `themed_text_field.dart`) | 320–328 (validate only in `_submitForm`) + `themed_text_field.dart:116–131` (validator, no `autovalidateMode`) + 817–822, 893–898, 906–913 (missing/nullable validators) | Submit-only generic banner | Inline/Live Validation | P2 | Enable `AutovalidateMode.onUserInteraction`, add the missing inline validators (address/workingHours have none; radius explicitly returns null when empty at 906–913 then fails at submit with a generic banner), and make radius inline-required like base/price at 1270–1301; ~10 fields validate only on Save today. |
| E16 | fleet map (`owner_fleet_map_screen.dart`; provider `map_tracking_provider.dart`) | Provider 86–90 (clears `_employeeMarkers`, sets `isLoading`); screen refresh at 131–140, gate at 169–175 | Refresh wipes map to full-screen spinner | Optimistic UI (stale-while-revalidate) | P2 | Keep stale markers during refresh with a subtle non-blocking indicator (linear progress in the filter row), reserving the full-screen loader/skeleton for first-load only when `markersList` is empty; today every refresh (including AppBar refresh) clears the map to a spinner. |
| E17 | owner config (`owner_configuration_screen.dart`) | 475–519 + 1108–1182 (four cards ≈10 inputs; `per_day` = 7 rows × ~21 controls) | All sections expanded at once | Progressive Disclosure | P2 | Wrap sections in collapsible `ExpansionTile`s (collapse schedule/pricing when pre-populated) or a stepped flow (Identity → Location → Schedule → Pricing with per-step validation), and collapse per-day rows behind per-day expanders; today identity, location/ops, schedule, and pricing all render expanded in one scroll. |
| E18 | cancel dialog (`cancel_job_dialog.dart`) vs confirm dialog + location picker | 39–49 (`barrierDismissible: false`) vs `confirm_action_dialog.dart:34–44` + `owner_configuration_screen.dart:199–206` (dismissible default) | Outlier traps the user; majority lets tap-outside cancel | Consistent dismiss pattern | P2 | Align to the majority (dismissible cancel = safe default) or document why a reason-typed destructive dialog must trap; if kept modal, preserve the draft reason on discard so an accidental outside-tap (currently impossible) or Cancel press does not destroy typed input. |
| E19 | employee home + employee jobs (`employee_home_screen.dart`, `employee_jobs_screen.dart`) | `employee_home_screen.dart:50–56` (`job_alert` auto-triggers `_refreshData`, no signal) + `employee_jobs_screen.dart:237–255, 86–108` (skeleton only when empty, else silent swap) | Background refresh swaps content silently | Progress/Status Indicator | P2 | Add a transient non-blocking signal on background refresh (thin linear progress under the header or a "New dispatch received" toast when offers change), keeping `RefreshIndicator` for manual pulls; today the list silently swaps content under the courier. |
| E20 | filter chips (`pill_filter_bar.dart`) + 3 consuming screens | 106–168, esp. 111–112 (`height: 36`); usages `employee_screen.dart:249–270`, `owner_history_screen.dart:335–358`, `owner_reconciliation_queue_screen.dart:251–279` | 36px tap height | 44×44 touch target | P2 | Raise to `height: 44` or `constraints: minHeight: 44` (adjust vertical padding); shared-widget fix benefiting all three screens (call out separately per repo shared-widget rule if executed). |
| E21 | fleet map (`owner_fleet_map_screen.dart`) | 317–442, esp. 328–331, 381–384, 418–421 (`GestureDetector` + `ThemedPanel`, vertical padding `AppSpacing.xs` = 4px → ≈27px targets) | Sub-44px floating pills | 44×44 touch target | P2 | Add `constraints: BoxConstraints(minHeight: 44)` or increase vertical padding to meet 44 without changing the pill aesthetic. |
| E22 | employee jobs (`employee_jobs_screen.dart`) | 1016–1046 (details `InkWell`, vertical padding `AppSpacing.xxs` = 2px around 16px content → ≈20px tall) | Sub-44px details toggle | 44×44 touch target | P2 | Expand the hit area to 44×44 via transparent padding/constraints while keeping the compact visual. |
| E23 | employee roster (`employee_screen.dart`) | 185–345 (roster 194–332, register 414–561, freeze 563–701 — three identical `ThemedCard(radius:md, padding:lg)` + same header/divider) | Three equal-weight cards, differing importance | One focal element | P2 | Elevate the roster as focal (higher elevation, full width) and demote/collapse the freeze form (warning accent, collapsed disclosure) with register secondary; roster is primary/daily while freeze is dangerous/infrequent, yet all three share identical geometry. |
| E24 | owner config (`owner_configuration_screen.dart`) | 499–516 (cards at 595–767, 769–918, 959–1205, 1207–1337 — identical geometry + 36px-icon headers) | Four identical cards | One focal element | P2 | Differentiate (accent/elevation for critical pricing/identity, collapsed-by-default for optional schedule) and make the save context focal (sticky summary/dirty-state bar); pricing/identity are critical and schedule optional, yet all four share identical weight. |
| E25 | owner dashboard (`home_screen.dart`) | 1035–1070 (chip) vs 787–823 (roster chip); copy at ARB `app_en.arb:807–808` | Static mock chip promises a workflow that lands on the roster | Single in-sync entry point | P2 | Retarget or remove the "urgent maintenance" chip, or wire it to a real maintenance data source: two chips land on the same Employees tab via `onTabTapped(1)`, but the maintenance chip promises "Vehicle Maintenance Required / Van #402 engine warning" (localized copy, but static mock data — no maintenance screen, endpoint, or Van #402 source exists anywhere in `frontend/`). Do not promise a workflow that lands on the roster. |
| E26 | owner dashboard (`home_screen.dart`) vs recon queue + employee jobs | 114–136 (single `ownerCancelRequestResolved` for both decisions) vs `owner_reconciliation_queue_screen.dart:102–109` + ARB 312 vs 305–307 + `employee_jobs_screen.dart:1199,1215` (decision-specific copy) | One generic toast for opposite decisions | Single in-sync entry point (decision-specific copy) | P2 | Add `ownerCancelRequestApproved` / `ownerCancelRequestDeclined` strings and show the matching one (approve → cancelled/refunded; decline → courier remains assigned); recon resolve (release vs refund) and employee banners (pending vs rejected) are already decision-specific. |
| S1 | subscription (`subscription_screen.dart`; provider `owner_provider.dart`) | Screen 23–24 (no `initState`), 67–72 (reads tier, no loading branch), 183–189 (`pending_payment` banner); provider default `'free'` at `owner_provider.dart:13` | No loading state; default renders as fact | Skeleton Screens | P2 | On `initState`, call `fetchDashboardData` when the dashboard slice is unhydrated and render a `WalletScreenSkeleton`-class placeholder (or `ThemedLoadingIndicator`) while `ownerProvider.isLoading`, matching the wallet gate; today on a cold start (deep link, fresh provider) the screen renders the default `'free'` tier as fact — wrong plan card active, wrong CTA enabled, `pending_payment` banner hidden. |
| S2 | notifications (`notifications_screen.dart`; provider `notifications_provider.dart`; `list_screen_template.dart`) | Template call at 175–276 omits `isLoading`/`errorMessage`/`onRetry`; provider exposes `isLoadingHistory` at 42, 162–165, never consumed here; template loader at 236–242 | Empty state renders mid-fetch (with a pop action) | Skeleton Screens / template loading slot | P1 | Pass `isLoading: provider.isLoadingHistory` (and wire `errorMessage`/`onRetry` or keep the header banner) — a one-line change at the template invocation; today every cold start renders the empty state, with its "Back to Home" pop action, while `fetchHistory` is in flight, and a tap during load exits the screen (mini pop-trap). |
| S3 | my account (`my_account_screen.dart`) | 595–605 | Bare italic text, no icon/action | Empty State (icon + text + next action) | P3 | Replace with `ThemedEmptyState` (location icon, existing `myAccountNoAddresses` copy as description) whose action focuses the new-address field or adds the current input; the zero-addresses case is a bare italic `Text` unlike every `ThemedEmptyState` elsewhere. |
| S4 | my account (`my_account_screen.dart`; provider `auth_provider.dart`) | Provider catch at 98–100 (only `debugPrint`, stores nothing); screen 51–70, 176–177 proceeds to `_isInitialized = true` | Silently swallowed profile failure | Inline Error with Retry | P1 | Store `_error` in the provider catch and, in the screen, render the existing `ThemedErrorBanner` + retry (`fetchUserProfile` then repopulate) when the initial load fails with no fresh user; today the user edits a silently stale profile and the submit-time banner is the first signal anything was wrong. |
| S5 | settings (`settings_screen.dart`; provider `auth_provider.dart`) | Screen catch at 589–596 (always generic `disableTwoFactorPasswordError`); provider stores `_error` + `_lastErrorStatusCode` at 27, 553–558, never read here | Specific failure misreported as wrong password | Inline Error with Retry (read the stored error) | P1 | Read `auth.error`/`auth.lastErrorStatusCode` in the catch and show lockout-specific (retry-after) or connectivity copy in the existing inline banner, keeping the generic string only for true 401s; today a 429 lockout (the backend rate-limits this path per the A3 hardening) or offline error invites pointless retries that extend the lockout. |
| S6 | wallet (`wallet_screen.dart`; provider `owner_provider.dart`) | Provider catch at 580–584 (`_payoutRequests = []`); screen 79–94, 338–349 | Failure wipes last-good data | Inline Error with Retry (keep cache on failure) | P2 | On catch, keep the previous `_payoutRequests`, set `_error`, and let the banner + retry be the sole signal; today a payout-list fetch failure clears the cached list, so the screen shows the "no payouts" empty state *and* the error banner simultaneously — conflicting signals, with the empty state's withdraw CTA active on possibly stale balances. |
| S7 | subscription (`subscription_screen.dart`) | Flow at 26–65 (fires with no confirm); call sites 351–358 (downgrade to free), 477–488 (upgrade, `$19.99/mo` at 419) | One-tap billing change, no consequence text | Confirmation Dialog (destructive billing styled distinctly) | P1 | Route both buttons through `ConfirmActionDialog.show` (`isDestructive: true` for downgrade) stating price/effective-date/feature-loss consequences, firing `_changeSubscription` only on confirm; both directions of a money-affecting change are one-tap today — upgrade immediately charges, downgrade silently drops Pro features — with less ceremony than the payout two-step confirm or the clear-all gateway in this same app. |
| S8 | my account (`my_account_screen.dart`) | 100–107 (`_removeAddress` immediate), 635–644 (delete `IconButton`) | One-tap delete, no undo | Confirmation Dialog → undo affordance | P3 | Keep the red affordance but follow deletion with a `SnackBar` "Address removed — Undo" that reinserts at the stored index (no per-item dialog needed for this weight of data); today the typed text is destroyed and must be retyped. |
| S9 | my account (`my_account_screen.dart`) | 81–98 (`_addAddress`: empty tap silently returns at 84; cap error at 86–91), 119–124 (submit-time cap), 226–233 (distant top banner), 576–581 (field has no validator) | Validation feedback far from the field | Inline/Live Validation | P2 | Add an inline `validator`/helper error on the new-address field, surface the 10-cap message there via `AutovalidateMode.onUserInteraction`, and give the empty-add tap a field hint instead of `return`. Distinct from tracked C-08 (discarding typed text on save — this is about *where* feedback appears): both cap errors and the silent empty-add no-op surface (if at all) in the top form banner, screens away from the addresses card. |
| S10 | settings (`settings_screen.dart`) | 430–477 (switch), 460–476 (enable await, no lock) | Static control during round-trip; re-triggerable | Progress/Status Indicator | P2 | Add a local `_toggling2fa` flag that disables the switch and shows a progress indicator until the future resolves, mirroring the disable path's `_isLoading` handling; today enabling 2FA awaits `auth.toggleTwoFactor(true)` with the control static for the whole round-trip and rapid taps queue multiple PATCHes. Same defect class as tracked E-05 but a different, untracked control. |
| S11 | wallet (`wallet_screen.dart`) | 28–37 (`initState` fire-and-forget), 51–58 (skeleton gate), 326–349 and 366–393 (sections branch on `isEmpty` only) | Empty states render mid-fetch | Progress/Status Indicator (gate empties on loading) | P2 | Gate each empty state on `!ownerProvider.isLoading` (or add section-level shimmer) so loading never masquerades as empty; today `fetchPayoutRequests`/`fetchPlatformConfig` run without any per-section signal, and with cached dashboard data the user briefly sees "no payouts / no transactions" before the lists pop in. |
| S12 | email dialog (`email_change_dialog.dart`) vs deposit/payout/settings | 15–20 (`show()` leaves `barrierDismissible` default true); majority `deposit_funds_dialog.dart:18–24`, `payout_request_dialog.dart:18–24`, `settings_screen.dart:548–554` (`false`) | Outlier dismisses a two-step flow on outside tap | Consistent dismiss pattern | P2 | Set `barrierDismissible: false` to match the money and password dialogs, keeping the explicit close button as the single exit; the two-step email-change flow (request code → OTP with `_targetEmail` state) is the only sensitive dialog dismissible by outside tap, and an accidental tap discards step-2 state and forces restart. |
| S13 | email dialog (`email_change_dialog.dart`) vs deposit/payout | 142–147 (close pops unconditionally) vs `deposit_funds_dialog.dart:118–125`, `payout_request_dialog.dart:143–150` (close disabled while submitting) | Outlier allows mid-request dismissal | Consistent dismiss pattern | P2 | Disable the close button while `_isLoading`, as in deposit/payout; today the email dialog's close stays enabled during in-flight `requestEmailChange`/`confirmEmailChange` calls the siblings deliberately prevent. |
| S14 | notifications (`notifications_screen.dart`) | 518–533 (`padding: EdgeInsets.zero`, constraints min 24×24; icon `AppIconSize.sm` = 16px per `theme.dart:254`) | 24×24 dismiss target inside a navigation `InkWell` | 44×44 touch target | P2 | Restore ≥44px constraints (keep the 16px visual), matching the repo's own A8 banner-dismiss restoration to 48dp; the per-card dismiss overrides Material's 48px default down to 24×24 nested inside the card's own navigation tap area — inviting mis-taps in both directions (delete when opening, open when deleting). |
| S15 | wallet (`wallet_screen.dart`) | 59–67 (pull-to-refresh: all three fetches), 84–91 (banner retry: all three) vs 386–392 (ledger-empty refresh: dashboard only) | Three refresh paths, different coverage | Single in-sync entry point | P2 | Extract one `refreshAll()` routine and point all three call sites at it; pull and banner retry refetch dashboard + config + payouts while the ledger empty-state button refetches dashboard only — so a payout-list failure survives the one retry button placed next to it. Distinct from tracked O-07 (dialog-side refresh). |
| S16 | wallet (`wallet_screen.dart`) | 28–37 (init fetches config + payouts only, never `fetchDashboardData`), 51–58, 232–238 (hero renders `0.00` as fact) | Balances never self-hydrated; false zero | Skeleton Screens | P2 | Call `fetchDashboardData(auth.token!)` in `initState` (guarded like the refresh path) so first paint is skeleton, not a false zero; today if the wallet opens with a fresh provider (e.g. notification `payout_*` deep link at 144–150), balances/ledger are never fetched on init yet render as `$0.00` with no loading and no error. |

---

## 3. Top 5 Highest-Leverage Fixes

1. **S2 — pass `isLoading` into the notifications template call** — a one-line change that simultaneously fixes a loading flash, a dead-end empty state, and a mini pop-trap on a screen every role opens daily.
2. **A1/A2/A3 — inline error banners on login/signup/OTP** — one repeated three-line pattern copied from the already-shipped forgot-password screen, closing the auth flow's most common confusion (vanishing wrong-password/lockout text) for every new and returning user.
3. **S7 — confirmation dialogs on subscription upgrade/downgrade** — reuses the existing `ConfirmActionDialog` to add billing ceremony proportional to the money moved, the cheapest trust win in the app.
4. **E12 — confirm owner approve/decline of cancellation requests** — one dialog wrapping a call that cancels jobs and triggers refunds, closing the highest-stakes one-tap destructive action found in this pass.
5. **E8 — surface the stored audit-log error** — the provider already captures the failure; only the missing three-line banner read stands between owners and a screen that lies about having no audit events.

---

## 4. Explicitly verified as already-OK (checked, no finding)

- **Cancel-button parity (customer list vs detail)**: `customer_jobs_screen.dart:288` (`isPending \|\| isPendingDispatch`) matches `job_status_screen.dart:1219`; COD/non-COD success-message branching matches (`customer_jobs_screen.dart:428–429` vs `job_status_screen.dart:1240–1242`).
- **Mark-notification-read is already Optimistic UI with rollback** (`notifications_provider.dart:254–276`, same shape for dismiss 298–316 and clearAll 318–330) — no blocking spinner on this low-risk action.
- **Clear-all notifications has a destructive `ConfirmActionDialog`** (`notifications_screen.dart:191–198`); payout has a two-step confirm; deposit is non-destructive.
- **No stale duplicate service-management flow**: `service_screen.dart` + `create_service_dialog.dart` fully deleted, zero references in `frontend/`.
- **`$` hardcodes at `customer_home_screen.dart:803` / `customer_jobs_screen.dart:364`** match the documented C-03 pattern class and map to no checklist item — left to the tracked l10n follow-up, not filed here.

---

## 5. Open questions (unconfirmed — need product/device input, NOT findings)

- **Q1 (auth)**: `otp_pin_input.dart:169–171, 178–181` — PIN-box width can drop below 44px on ~320dp viewports, but the wrapper is a `TextField`, not a tap control, so checklist #10 applicability is uncertain; needs device measurement.
- **Q2 (auth)**: `signup_screen.dart:67–71` (`pushReplacement` to OTP) vs `login_screen.dart:74–78` (`push` to OTP) — same OTP screen, different back stacks (back from signup-OTP loses signup context); possibly an intentional no-back-to-signup guard; needs product confirmation before filing under #8.
- **Q3 (auth)**: `auth_provider.dart:77–79` — `_tryAutoLogin` catch is silent, leaving the user on Login with no message; likely benign (no screen exists yet to host an error); recorded, not filed.
- **Q4 (owner)**: `home_screen.dart:535–574` hero wallet badge (`InkWell`, no explicit 44px constraints) — visual size likely passes but unmeasured statically; verify on device.
- **Q5 (owner)**: schedule `open == close` (zero-length) has no client-side guard; `close < open` is legitimately overnight per the ADR-0025 evaluator, so no order-validation finding filed — confirm backend `ValidateServiceSchedule` copy suffices vs an inline hint.
- **Q6 (owner)**: `employee_home_screen.dart:90–97` shell fetch has no shell-level error surface (delegates to tab loaders); whether simultaneous failure of both tabs needs a shell banner vs per-tab banners is unconfirmed.
- **Q7 (shared)**: `owner_provider.dart:510–526` — `fetchPlatformConfig` swallows failures by design ("non-critical"), so the fee-transparency line silently vanishes; nothing is stored-but-unread, so it fails the strict #3 criterion — confirm product accepts invisible-fee vs a muted retry affordance.
- **Q8 (shared)**: `payout_request_dialog.dart:99, 234–246` — amount-vs-withdrawable validator snapshots `withdrawableBalance` via `listen: false` at dialog open; no live balance mutation path was pinnable statically — confirm whether the dialog needs a `Consumer` refresh or close/reopen guidance.
- **Q9 (shared)**: reachability scope for S1/S16 — if tab order guarantees dashboard hydration before subscription/wallet are reachable, those findings bite only on deep-link/notification paths; confirm the nav graph before prioritizing.
- **Q10 (shared)**: success-snackbar ordering is inconsistent (`my_account_screen.dart:139–144` show-then-pop vs `deposit_funds_dialog.dart:62–67` / `payout_request_dialog.dart:77–84` pop-then-show); both work today only because `ScaffoldMessenger` resolves app-level — confirm no route-level messenger is ever introduced, then standardize one order.
- **Q11 (shared)**: single-notification dismiss (`notifications_screen.dart:532`, provider 298–316) is one-tap server-side delete with failure-rollback but no user-undo, while bulk clear-all requires confirmation — asymmetric by convention; confirm whether an undo affordance is wanted or the asymmetry is accepted.

---

*End of audit. 62 new findings (11 P1 / 39 P2 / 12 P3), 11 open questions, 5 verified-OK non-findings. No application code touched.*
